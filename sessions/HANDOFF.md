# Session Handoffs

Newest entry at TOP. Every session appends one entry before ending (use `/end-session`).

## Entry Template
```
## YYYY-MM-DD — <short title>
**Did:** what was completed (files touched, roadmap items checked)
**State:** does it run? tests green? any known breakage?
**Next:** the single next task, with any context the next session needs
**Watch out:** gotchas discovered this session
```

---

## 2026-07-18 — PLANNING session: sequenced the UI extraction (no code changed)

**Did:** Jeremy confirmed the pending delete-routine recall fix playtests correctly, so the three uncommitted pieces (routines extraction, window-exposure fix, recall fix) are cleared to commit. Then planned the last unchecked Milestone 2 item, "Extract UI", which was a single ROADMAP line. Mapped the whole UI surface via Grep only (script.js never read whole): **~2,500 lines across 7 clusters**, including `createListItem` (268 lines), `attachModalEventListeners` (366), and a routine-UI cluster of ~854. Far too big for one session. Produced **`docs/UI_EXTRACTION_PLAN.md`** — function map with line ranges, 11-session sequence, per-session model recommendation, and standing hazards. ROADMAP.md's single line is now 10 ordered sub-items. See DECISIONS.md for the three decisions.

**Two findings worth knowing before touching any UI code:**
1. **`closeModal` is a shared primitive in the wrong place** — defined once, as `window.closeModal` at script.js:2671, buried inside the routine edit-form code, yet **all 18 inline `onclick="closeModal()"` sites** span five different clusters. Extracting any feature cluster before it would make that cluster depend on routine code. Hence session 1 of the plan is a small `js/ui/modal.js` foundation, which also adds a helper for the `window.*` exposure inline `onclick=` requires (making systematic what today's `deleteRoutine` bug showed is ad-hoc).
2. **The legacy inline form system is dead, and the 2026-07-17 `showForm` shadowing flag was masking that.** Both `showForm` declarations share the `DOMContentLoaded` scope, so the 5-line stub (~3541) hoists over the real implementation (~423) — `showForm('task')`/`showForm('habit')` are silent no-ops, including `initGame`'s call at line 172 (verified). The real one was inert anyway: `showTaskFormButton`/`showHabitFormButton`/`showRoutineFormButton` **do not exist in index.html** (verified, 0 occurrences). Only the modal system (FAB → `showFormModal`) is reachable. But the inline form markup is still in index.html (151, 191, 249) and `addTaskButton`/`addHabitButton` listeners (~3419/~3451) still run and still call `clearFormInputs()`. **Jeremy's call: delete it**, as session 0, before extraction starts.

**State:** ✅ No code changed — docs only (`docs/UI_EXTRACTION_PLAN.md` new; `docs/ROADMAP.md`, `docs/DECISIONS.md` updated). Tests not re-run for that reason; the suite stood at **13 suites, 244/244** from the previous session and nothing touched it. Claims in the plan were verified by re-Grep (toggle buttons absent, `closeModal` single definition, line-172 call site).

**Next:** **Session 0 of the plan — delete the dead legacy inline form system.** Remove `showForm` #1 + the stub, `clearFormInputs`, `enableFormControls`, the now-unused DOM consts at the top of script.js, the dead `addTaskButton`/`addHabitButton` listeners, and the inline form markup in index.html; fix the `initGame` line-172 call. Sonnet-level — root cause is fully documented, this is execution. Then session 1 (`js/ui/modal.js`).

**Watch out:**
- **Line numbers throughout `UI_EXTRACTION_PLAN.md` are as of commit `d2825e4` and will drift** as sessions land. Re-Grep before trusting any of them.
- Session 0 deletes markup from index.html, not just script.js — confirm with a live playtest that task/habit/routine creation via the FAB still works afterward, since that's the only remaining path.
- Two non-extraction items are still queued and unblocked by all of this: the streak-bonus refund asymmetry bugfix, and the day-of-week/monthly scheduling design (batched for a Fable session with the base-healing question).

## 2026-07-18 — Bugfix (same session, found live right after the previous fix): deleting a routine stranded its active sprites/agenda rows

**Did:** Jeremy tested the just-fixed Delete Routine button — it now deletes the routine, but any active habit/task belonging to it (spawned sprite + agenda row) stayed on the board. Root cause: `deleteRoutine` never recalled active instances the way deactivating a routine already does (`clearActiveInstancesForRoutine`). **Judgment call, not escalated:** delete is a superset of deactivate, so it now gets the same recall — implemented inside `Routines.deleteRoutine` (signature changed from `(routineId, definedRoutines)` to `(routineId, deps)`, `deps = { definedRoutines, activeItems, removeItem }`) so it's composed and unit-tested the same way `toggleRoutineActive` composes the recall, rather than ad-hoc wiring in the script.js wrapper. No XP/points/streak/damage side effects — pure removal, same as deactivation. Orphaned habit/task DEFINITIONS are deliberately left in place (inert, same as `removeHabitFromRoutine`/`removeTaskFromRoutine` already do) — purging those on delete would be a separate, bigger decision, not folded in here. See DECISIONS.md.

**Tests:** extended `test/routines.test.js`'s `deleteRoutine` suite (recall case + cross-routine isolation), updated the deps-based call signature.

**State:** ✅ Full Jest suite green in the sandbox: **13 suites, 244/244** (242 prior + 2 new). `node --check` clean on script.js and js/routines.js. **NOT yet live-playtested by Jeremy** — please confirm: create a routine, add a habit AND a task, activate it so both spawn, then delete the routine and check both the sprite and agenda row vanish immediately (no console errors, no leftover ghost instance).

**Next:** once confirmed, commit everything pending from this session together (three pieces: the routines extraction, the window-exposure fix, and this recall fix), then next Milestone 2 item is **Extract UI: forms, agenda list, popups, FAB menu**.

**Watch out:** `Routines.deleteRoutine`'s signature is now `(routineId, deps)`, NOT `(routineId, definedRoutines)` like it briefly was earlier this same session — if anything else ever calls it directly (nothing does today besides the script.js wrapper), it needs the full deps object.

## 2026-07-18 — Bugfix (same session, found live right after the routines extraction): Delete Routine button did nothing

**Did:** Jeremy playtested the routines extraction and hit this immediately: opening a routine's management modal and clicking "Delete Routine" did nothing. **Not caused by the extraction — pre-existing.** The modal's Delete button is built via inline `onclick="deleteRoutine(...)"` HTML, which runs in global scope, but `deleteRoutine` was only ever a closure-scoped function inside script.js's top-level `DOMContentLoaded` wrapper, never attached to `window` (unlike `closeModal`/`saveNewHabit`/`saveNewTask`/`saveEditedHabit`/`saveEditedTask`, which all are). Fixed by adding `window.deleteRoutine = deleteRoutine;`. Checked every other inline `onclick="..."` in script.js for the same gap — none found, `deleteRoutine` was the only one missing. See DECISIONS.md.

**State:** `node --check` clean. Not independently unit-testable (DOM/global-scope wiring, same category as `closeModal`) — **needs Jeremy to confirm live**: open a routine's management modal, click Delete Routine, confirm the dialog, and check it's actually removed (list + persistence).

**Next:** same as below — playtest this fix plus the rest of the routines extraction smoke test, then commit everything pending together.

**Watch out:** if a future session adds another modal button via an inline HTML `onclick="someFunction(...)"` string, remember it needs an explicit `window.someFunction = someFunction;` line — the whole file is wrapped in a `DOMContentLoaded` closure, so nothing is global by default.

## 2026-07-18 — Milestone 2 extraction #6: routines (Cowork session)

**Did:** Extracted `js/routines.js` per ARCHITECTURE.md's target layout. Scope split deliberately, logged in DECISIONS.md: only the DATA layer moved (routine-task spawn selection, definition CRUD, activation/deactivation + recall); the DOM-rendering functions (`renderDefinedRoutines`, `populateRoutinesWindow`, `showRoutineManagement`, `populateRoutineHabits`/`populateRoutineTasks`, `createRoutineFormHtml`, `attachRoutineManagementListeners`, `showAddItemToRoutineModal`, `updateRoutineDisplay`) stayed in script.js — pure DOM construction, belongs with the future "Extract UI" item. Extracted: `getRoutineTaskInstanceDueTime`, `selectTaskDefsToSpawn` (new — split out the selection half of `generateDailyRoutineTaskInstances`, mirroring `selectHabitDefsToSpawn`), `createRoutineTaskInstanceData`, `generateDailyRoutineTaskInstances` (id/position/DOM-admission via injected deps, same pattern as habits.js/damage.js), `createRoutineDefinition`, `deleteRoutine`, `removeHabitFromRoutine`, `removeTaskFromRoutine`, `createNewHabitInRoutine`, `createNewTaskInRoutine`, `editHabitInRoutine`, `editTaskInRoutine`, `addHabitToRoutine`, `selectActiveItemIdsToClearForRoutine`, `clearActiveInstancesForRoutine`, `toggleRoutineActive`. Functions that mixed state mutation with `alert`/`confirm` now return result objects (`{ok, reason}` etc.) and script.js wrappers own the actual dialogs/render/save calls. script.js keeps thin wrappers at every original call site — no call site changed, behavior-identical. Wired into index.html after habits.js.

**Not touched (flagged only, see DECISIONS.md):** `createRoutineFormHtml`'s modal handler has its own inline duplicate of `createRoutineDefinition`'s validation/construction logic (part of the known 2026-07-17 "showForm duplicate/shadowing" issue, also missing a `saveGame()` call) — left as-is, reconciling it belongs with the UI extraction or its own bugfix session.

**Tests:** `test/routines.test.js` (33 cases — instance creation, daily-spawn orchestration, routine CRUD, membership add/remove, habit/task-in-routine create+edit, deactivation recall selection, toggleRoutineActive including the slot-cap block). `test/routine-active-gating.test.js` and `test/routine-task-instances.test.js` (both pre-existing hand-maintained mirrors) now `require('../js/routines.js')` directly instead of keeping their own copies — same dedup habits.js did last session.

**State:** ✅ Full Jest suite green in the sandbox: **13 suites, 242/242** (209 prior + 33 new). `node --check` clean on `script.js` and `js/routines.js`. **NOT live-playtested this session** — no behavior should have changed, but per usual practice, worth a quick smoke test: create a routine, add a habit and a task, activate it (confirm both spawn immediately), deactivate it (confirm both are recalled with no XP/points/streak change), delete a routine, edit a habit/task inside a routine, and hit the "you can only have N active routines" cap.

**Docs updated same session:** ROADMAP.md (checked off the extraction), ARCHITECTURE.md (routines.js marked IMPLEMENTED, scope note), DECISIONS.md (scope-split reasoning, the alert/confirm-return-object pattern, and the untouched inline-duplicate flag). ROUTINES.md intentionally left untouched — no mechanic changed, only code moved.

**Next:** commit this extraction (command below), quick smoke test if Jeremy has a moment, then the next Milestone 2 item is **Extract UI: forms, agenda list, popups, FAB menu** — the largest remaining item, and the natural home for all the DOM-rendering routine/habit functions this session and the last one deliberately left behind. Two still-open non-extraction items sit in the ROADMAP queue too: the streak-bonus refund asymmetry bugfix and the day-of-week/monthly scheduling design — both flagged as their own sessions, not blocking the UI extraction.

**Watch out:**
- `toggleRoutineActive`'s slot-cap `alert` is now threaded through `deps.alert` rather than living in the script.js wrapper, unlike every other alert/confirm in this extraction (which stayed in the wrapper) — it's gating logic (whether the toggle proceeds), not a post-decision side effect. Worth remembering if a future session touches this function's signature.
- The inline routine-creation duplicate in `createRoutineFormHtml`'s modal handler (`case 'routine':`) was deliberately NOT reconciled with `Routines.createRoutineDefinition` this session — still duplicated, still missing its own `saveGame()` call. Don't mistake the new routines.js module for having fixed this.
- Cached script.js has bitten multiple prior sessions — hard-reload before doubting a fix.

## 2026-07-18 — Milestone 2 extraction #5: habits + streaks (Cowork session)

**Did:** Extracted `js/habits.js` per ARCHITECTURE.md's target layout. Pure: `getHabitInstanceDueTime` (timeOfDay bucket → due time), `selectHabitDefsToSpawn` (isActive-gated daily spawn selection — was already a hand-maintained mirror in `test/routine-active-gating.test.js`, now the real thing), `applyHabitCompletion`/`applyHabitUncompletion` (streak increment/decrement + bonus-threshold points), `resetStreakOnOverdue`. `createHabitInstanceData`/`generateDailyHabitInstances` take id generation, timeline position, and DOM admission (`addItemToGame`) as injected deps — same accessor-dep pattern damage.js/spawning.js established, since `itemIdCounter` and the DOM stay script.js-owned. script.js keeps thin wrappers at all five original call sites (`createHabitDefinition`, `completeItem`, `uncompleteItem`, `markAsOverdue`, `generateDailyHabitInstances`) — no call site changed, behavior-identical. Wired into index.html after progression.js.

**Two divergences found while mapping the code, both surfaced to Jeremy and deliberately NOT fixed here** (full detail in DECISIONS.md):
1. **Streak-bonus refund asymmetry (bug):** `completeItem` awards bonus points off the post-increment streak; `uncompleteItem` refunds off the post-decrement streak — crossing the bonus threshold then uncompleting nets +5 points the player shouldn't keep. Jeremy: preserve as-is, flag it. Reproduced exactly in `applyHabitCompletion`/`applyHabitUncompletion`, documented in the file header, and covered by an explicit test. New ROADMAP bugfix item added (own session).
2. **Streak bonus model vs. MECHANICS.md (design gap):** doc says "higher chance of double points" (probabilistic); code is a flat deterministic +5 at streak 3. Jeremy asked for a recommendation instead of picking a side — gave one (separate the visual streak from an economy bonus driven by a capped rolling-success-rate multiplier, works for negative habits too, bounded by construction) and it's logged, but NOT built — needs the day-of-week/monthly schedule migration first (shares one schemaVersion bump) and real shop prices to tune against (Milestone 3). Routed to a future batched Fable session alongside the still-open base-healing question.

**Tests:** `test/habits.test.js` (27 cases — due-time buckets, completion/uncompletion streak math including the asymmetry, overdue reset, instance creation, spawn-selection orchestration). `test/routine-active-gating.test.js` now `require('../js/habits.js')` for `selectHabitDefsToSpawn` instead of keeping its own copy (dedup, no behavior change — its `selectActiveItemIdsToClearForRoutine` mirror stays, that function isn't extracted yet).

**State:** ✅ Full Jest suite green in the sandbox: **12 suites, 209/209** (182 prior + 27 new). `node --check` clean on `script.js` and `js/habits.js`. **NOT live-playtested this session** — no behavior should have changed, but per usual practice, worth a quick smoke test (complete a habit, uncomplete it, let one go overdue and confirm streak/points/visuals match pre-extraction). **Jeremy confirmed the prior session's pending work (progression.js + routine-task fixes) is playtested and committed**, so this sits on a clean base — should be its own commit.

**Docs updated same session:** ROADMAP.md (checked off the extraction, added the two new items above), ARCHITECTURE.md (habits.js marked IMPLEMENTED), DECISIONS.md (both divergences, full reasoning). MECHANICS.md intentionally left untouched — no mechanic changed, only code moved.

**Next:** commit this extraction (command below), quick smoke test if Jeremy has a moment, then next Milestone 2 item is **Extract routines** (heroes, slots, `toggleRoutineActive`, `selectActiveItemIdsToClearForRoutine`, `clearActiveInstancesForRoutine`) — OR the day-of-week/monthly scheduling item, sequenced carefully since routines extraction and scheduling both touch the same generator functions habits.js just wrapped.

**Watch out:**
- `markAsOverdue` stays in script.js (also drives task-overdue logic, not habits-only) but now delegates its streak-reset branch to `Habits.resetStreakOnOverdue` — if a future routines/task extraction touches `markAsOverdue`, this delegation should carry forward.
- The streak-bonus asymmetry bug is now *documented and tested*, not fixed — don't mistake the new test coverage for a fix; it explicitly asserts the current (wrong) behavior.
- Cached script.js has bitten multiple prior sessions — hard-reload before doubting a fix.

## 2026-07-18 — Bugfix (same session, found live right after the isActive fix): activating a routine spawned nothing until reload

**Did:** Jeremy hit this immediately after the isActive-gating session below: create a routine, add a task, hit Activate — nothing showed up in the game area or agenda list. **Root cause: a gap in that same isActive fix.** The daily generators (`generateDailyHabitInstances`/`generateDailyRoutineTaskInstances`) only run from `initGame`, `restoreGameState`, and creation time. A routine starts `isActive: false`, so adding a task to it calls the generator while still inactive — correctly a no-op under the new gating. `toggleRoutineActive` then flips `isActive` to `true` but never called either generator itself, so nothing spawned until a reload or day-rollover.

**Fix:** `toggleRoutineActive` now calls both daily generators on the inactive→active transition — the direct symmetric counterpart to the immediate recall already added for the active→inactive transition. Updated docs/ROUTINES.md (the "does NOT retroactively spawn... next daily pass" line was this session's original, now-corrected design intent) and DECISIONS.md.

**State:** `node --check` clean, full Jest suite still green (11 suites, 182/182 — unchanged, this call-site fix isn't independently unit-tested; it's DOM orchestration like `Spawning.addItemToGame`, verified by playtest per ARCHITECTURE.md). **NOT yet live-playtested by Jeremy** — please confirm: create a routine, add a habit AND a task to it, hit Activate, and check both appear immediately as sprite + agenda row.

**Next:** same as below — playtest both the recall (deactivate) and spawn (activate) paths together, then commit everything pending, then move to habits + streaks extraction.

**Watch out:** same isActive-related items as the entry below still apply (recall only fires inside `toggleRoutineActive`; sub-task cascade is one level deep).

## 2026-07-18 — Routine `isActive` now gates spawning + recalls active enemies on deactivation (Cowork session)

**Did:** Closed the ROADMAP item found during the previous session's routine-tasks fix. `generateDailyHabitInstances` and `generateDailyRoutineTaskInstances` now build a set of definition ids belonging to a currently-`isActive` routine and skip anything not in it — this also makes a definition orphaned from its routine (via `removeHabitFromRoutine`/`removeTaskFromRoutine`) inert, same as it already was for tasks. Asked Jeremy the one open design question first: should deactivating a routine just stop future spawns, or also pull already-active enemies off the board? **He chose: also clear active enemies.** Added `selectActiveItemIdsToClearForRoutine` (pure: matches active items by `definitionId` against the routine's `habitDefinitionIds`/`taskDefinitionIds`, cascades sub-tasks of a matched routine task ahead of their parent) + `clearActiveInstancesForRoutine` (routineId), which runs each selected id through the existing `removeItem()` — no new DOM/removal path, no XP/points/streak/damage side effects, not recorded in `completedItems`. `toggleRoutineActive` now calls this on active→inactive transitions and also gained the `saveGame()` call it was missing (previously relied on the 5s autosave net).

**Docs updated same session:** docs/ROUTINES.md's "Activation & Deactivation" section now describes both the spawn-gating and the recall behavior (removed the old "NOT YET IMPLEMENTED" note). Decision + rejected alternative (leave active enemies running their course) logged in DECISIONS.md.

**Tests:** extended `test/routine-task-instances.test.js`'s `selectTaskDefsToSpawn` mirror with isActive gating (4 new cases; the `routine()` fixture helper now defaults `isActive: true` so the pre-existing dedupe tests didn't need touching). New `test/routine-active-gating.test.js` (16 cases): habit-side spawn-selection mirror with isActive gating, plus `selectActiveItemIdsToClearForRoutine` coverage (basic match, cross-routine isolation, manual-item isolation, sub-task cascade ordering, unrelated-parent non-cascade, missing-fields no-throw, empty-input).

**State:** ✅ **Full Jest suite green in the sandbox: 11 suites, 182/182** (162 prior + 20 new). `node --check` clean on script.js and every file in js/. **NOT live-playtested by Jeremy yet** — the recall path (`removeItem` DOM cleanup) should be confirmed in the browser: create/activate a routine with a habit and a task, let both spawn, deactivate the routine, and confirm both sprites and agenda rows vanish immediately with no XP/points awarded and no console errors; then reactivate and confirm nothing spawns until the next daily pass. **NOT committed** — sits on top of the not-yet-committed work from the last two sessions (progression.js extraction + the routine-tasks fix); all three should go in one commit round or be split carefully if Jeremy wants separate commits.

**Sandbox tooling note:** hit the ENOTEMPTY npm-install corruption for the first time this session — turned out to be because I was running `npm install` inside the **outputs folder itself**, which the platform makes files un-renamable/un-deletable in (by design, so Jeremy's deliverables can't vanish underneath him). That's fatal for `npm install`'s atomic rename-based package writes. Switched to `/tmp` in the sandbox (a real scratch dir, not host-mapped) and the install completed cleanly in one shot. Worth remembering: **any sandbox `npm install` must happen in `/tmp` (or similar), never under the outputs path**, even though outputs is the folder shell commands can otherwise read/write freely. Also hit the by-now-familiar `unrs-resolver` missing-native-binding issue (`test/setup.js ... not found`) — same fix as before (`npm i @unrs/resolver-binding-linux-x64-gnu --no-save`).

**Next:** commit all three pending pieces of work (commands below), then live-playtest the recall behavior described above. After that, next Milestone 2 item is **extract habits + streaks** (now unblocked — the isActive/frequency entanglement HANDOFF flagged last session is resolved).

**Watch out:**
- Recall only fires on the active→inactive *transition* inside `toggleRoutineActive`. If any other code path ever sets `routine.isActive = false` directly (I didn't find one, but the habits/routines extraction should re-check), it would skip the recall and leave stale enemies on the board.
- `selectActiveItemIdsToClearForRoutine` cascades only ONE level of sub-tasks (a sub-task's own children, if that ever becomes a thing). Not an issue today — sub-tasks can't currently have sub-tasks — but worth a second look if that changes.
- Cached script.js has bitten multiple prior sessions — hard-reload before doubting a fix.

## 2026-07-18 — Routine tasks never spawned (root-caused + fixed); definedTasks now persisted (Cowork session)

**Did:** Jeremy reported that a task added through a routine didn't show up on the board or in the agenda list. **Not a rendering bug — the spawn path was never built.** `createNewTaskInRoutine` created a task *definition*, pushed it to `definedTasks`, and called only `renderDefinedRoutines()`. Nothing anywhere converted a definition into a live item: there was no task equivalent of `createHabitInstanceData`/`generateDailyHabitInstances`. The task DID appear inside the routine management window, which is exactly why it looked like a display problem.

**Decision made with Jeremy: routine tasks recur DAILY**, matching routine habits (alternatives considered and rejected — spawn-on-activation, spawn-once-on-creation; rationale in DECISIONS.md). Added to script.js next to the habit equivalents: `getRoutineTaskInstanceDueTime` (parses the definition's `HH:MM` `defaultDueTime` — habits use coarse `timeOfDay` buckets instead, so it needed its own helper), `createRoutineTaskInstanceData`, and `generateDailyRoutineTaskInstances`. Wired into all three places `generateDailyHabitInstances` is called: `initGame`, `restoreGameState`, and at creation time in `createNewTaskInRoutine`. Instances are ordinary `type: 'task'` items carrying a `definitionId`, so damage/completion/sub-tasks/sorting treat them identically to a hand-made task.

**Dedupe had to differ from habits:** habits record "done today" on the definition (`lastCompletionDate`), but task definitions have no such field, so completion is read from `completedItems` matching `definitionId` + `originalDueDate` day. Only definitions referenced by some routine's `taskDefinitionIds` spawn — one orphaned by `removeTaskFromRoutine` stays in `definedTasks` but is inert.

**Second bug found while fixing the first: `definedTasks` was never persisted.** It's a bare `window.definedTasks`, and `getPersistableState()` saved `definedRoutines` (with its `taskDefinitionIds`) but not the definitions — so a refresh left those ids dangling against nothing. Added to save/restore as an additive field (no schemaVersion bump; old saves restore `[]`) and cleared by the dev reset button. Also added the missing `saveGame()` calls to `createNewTaskInRoutine` AND `createNewHabitInRoutine` (both previously relied on the 5s autosave net).

**Habits in routines were checked and are fine** — `createNewHabitInRoutine` → `generateDailyHabitInstances` → `addItemToGame`, which does sprite + `createListItem` + `sortAndRenderActiveList` + `saveGame`. Both sprite and agenda row render correctly.

**Also removed** a leftover `🔧 createTaskItemData returning:` debug log in script.js that the 2026-07-18 log-cleanup session missed (its own replacement comment sat 55 lines above it claiming it was gone).

**State:** ✅ **Full Jest suite green in the sandbox: 10 suites, 162/162** (148 prior + 14 new in `test/routine-task-instances.test.js`). `node --check script.js` clean. ✅ **Live-playtested by Jeremy and confirmed working** — routine tasks now spawn as sprite + agenda row. **NOT committed at time of writing** — note this sits on top of the also-uncommitted progression.js extraction from earlier the same day; both go in the same commit round.

**Next:** Next Milestone 2 item is **extract habits + streaks** — but see the isActive item below, which now has a roadmap entry and overlaps that extraction; consider doing the isActive/frequency fix first or folding it in, since both touch `generateDailyHabitInstances`.

**Watch out:**
- **`isActive` is inert and I deliberately did NOT fix it here.** `generateDailyHabitInstances` iterates ALL of `definedHabits` ignoring routine membership and active state, so a habit in a *deactivated* routine still spawns; `toggleRoutineActive` only flips the flag (no `saveGame()`, no re-render). ROUTINES.md says deactivated routines spawn no enemies. The new task generator mirrors the habit generator's behavior on purpose so the two don't diverge — **fix both together**, in or right before the habits extraction. New ROADMAP item + ROUTINES.md warning note added.
- **`generateDailyHabitInstances` only handles `frequency === 'daily'`.** Safe today ONLY because the create-habit form's frequency dropdown has exactly one option. The moment weekly/custom is added, those habits silently never spawn. Fold into the same session as the isActive fix.
- **Routine tasks are `type: 'task'` with a `definitionId`** — that field previously only appeared on habits. If any code branches on "has definitionId ⇒ it's a habit," it's now wrong. I checked the completion/damage paths and they branch on `item.type`, but the habits extraction should re-verify.
- `definedTasks` is still a `window.*` global rather than a module-scoped `let` like `definedHabits`/`definedRoutines` — inconsistent, and the reason its persistence was missed. Worth normalizing during the habits/routines extractions.

## 2026-07-18 — Milestone 2 extraction #4: progression (XP, levels, slots) (Cowork session)

**Did:** Extracted level-up math into `js/progression.js` — a pure `Progression.checkLevelUp({level, xp, slots}, LEVEL_XP_THRESHOLDS, ROUTINE_SLOTS_PER_LEVEL, maxLevel)` returning `{level, slots, leveledUp, levelsGained, slotsUnlocked}`. No DOM, no CONFIG-as-global dependency — everything comes in as an explicit argument (closer to clock.js's `dims`-parameter style than damage.js's deps-bag style, since nothing here needs to write script.js state). Replaces the old `checkPlayerLevelUp()`'s recursive self-call (which handled a single completion crossing more than one XP threshold) with a `while` loop that walks every crossed threshold in one pass. script.js's `checkPlayerLevelUp()` is now a thin wrapper: call `Progression.checkLevelUp`, apply the result to `playerLevel`/`routineSlots`, then the unchanged DOM side effects (`updatePlayerDisplays()`, `showLevelUpMessage()`, `updateRoutineDisplay()` only when a slot unlocked). All call sites unchanged. Wired into index.html after damage.js.

**Scope decision (logged in DECISIONS.md):** `playerPoints` stayed in script.js even though it's updated in the same `completeItem`/`uncompleteItem` code as XP — ARCHITECTURE.md's target layout puts points under the future `economy.js` (shop/pricing), and the shop is an unbuilt Milestone 3 item. Extracting points now would mean building economy.js ahead of the shop it exists to serve. `updatePlayerDisplays()` also stayed in script.js — it writes 4 HUD fields (xp/level/points/slots) together and splitting it would separate numbers that render side by side for no behavioral gain. Jeremy confirmed this scope before the session started.

New `test/progression.test.js` (9 cases): no-threshold-crossed, exact-threshold boundary (`>=`), single level-up, slot-unlock-only-when-higher-than-current, multi-level-up in one call (both level count and highest slot tier crossed), max-level clamp (including a huge XP overflow that must still stop exactly at the last threshold).

**Sandbox tooling note (new, worth keeping):** Jest failed in the sandbox this session with `Module <rootDir>/test/setup.js ... was not found` — looked identical to the long-standing missing-setup.js issue, but `test/setup.js` was present and valid. Root-caused to something new: **`jest-resolve`'s native module resolver (`unrs-resolver`) had no `@unrs/resolver-binding-linux-x64-gnu` binding installed** — the classic npm optional-dependency bug (npm/cli#4828) — so every module resolution jest-resolve attempted silently failed and got reported as "not found," including our own setup file. Fixed with `npm i @unrs/resolver-binding-linux-x64-gnu --no-save` in the sandbox scratchpad; suite ran clean after. This is a sandbox-environment quirk, not a repo defect — nothing in the actual repo changed for this.

**State:** ✅ **Full Jest suite green in the sandbox: 9 suites, 148/148 passing in 0.65s** (139 pre-existing + 9 new progression tests). `node --check` clean on script.js and js/progression.js. **NOT live-playtested by Jeremy yet** — this extraction shouldn't change any visible behavior (same math, same call sites), but hasn't been confirmed in the browser. **NOT committed.**

**Next:** Jeremy should hard-reload and confirm completing tasks/habits still levels up correctly (XP bar, level-up message, routine slot unlock at levels 3/5/7) with no visible change from before. If clean, commit (commands below). Next Milestone 2 item: **extract habits + streaks**.

**Watch out:**
- If a future sandbox session hits `Module <rootDir>/test/setup.js ... was not found` again despite the file clearly existing, try `npm i @unrs/resolver-binding-linux-x64-gnu --no-save` before assuming it's the old missing-setup.js problem — check `node -e "require('unrs-resolver').sync('somepath', {})"` for the "Cannot find native binding" error to confirm.
- `playerPoints` is now the one HUD number progression.js deliberately does NOT own — worth remembering when the habits/routines extractions touch `completeItem`/`uncompleteItem` next, since those functions still mix progression (XP/level), points (future economy.js), and habit streaks (future habits.js) together in one place. Don't assume the next extraction can cleanly lift one without brushing the others.
- Cached script.js has bitten multiple prior sessions — hard-reload before doubting a fix.

## 2026-07-18 — Milestone 2 extraction #3: damage / base health / game over (Cowork session)

**Did:** Extracted the damage system into `js/damage.js` (336 lines), the ROADMAP item whose scope had grown twice. It absorbs `damageBase`, `gameOver`, `updateBaseVisuals`, `computeDaysSurvived`, the offline catch-up code deferred from the clock.js extraction (`computeOfflineOverdueDamage`, `applyOfflineDamage`, `runOfflineCatchUp`), and `runLiveGapCatchUp`. Pure cores were split out so the logic is testable without a DOM: `resolveBaseImage`, `computeGapCatchUpHits`, `computeCatchUpDuration`, plus the already-pure `computeOfflineOverdueDamage` and a now-parameterised `computeDaysSurvived(runStartedAtMs, nowMs, msPerRealDay)`. script.js keeps thin wrappers, so every call site is unchanged. Wired into index.html after spawning.js.

**The one design call this session** (logged in DECISIONS.md): damage.js is the first module that WRITES script.js-owned state (`baseHealth`, `gameIsOver` — ~68 references between them), where clock/movement/spawning only read. It reaches that state through a `damageDeps()` accessor bag rather than taking ownership, extending spawning.js's `isGameOver()` precedent. Moving ownership is the right end state but would have turned a mechanical extraction into a semantic change to the game's central state in one session. `markAsOverdue` also deliberately stayed in script.js and arrives as a dep — it resets habit streaks, so it belongs with the future habits extraction.

**Both watch-out cleanups folded in:**
- Removed the `🛠️`/`🔧`/`📍` parentId debug logs from script.js, js/spawning.js AND the mirrored copies inside `test/subtask-creation.test.js` (which were the actual source of most of the noise). That suite's output went from **858 lines to 26**.
- Bumped **puppeteer `^21.0.0` → `^24.0.0`**, clearing all 9 npm-audit findings. This forced a source fix: v22 removed `page.waitForTimeout`, so `test/visual-tests.js` now uses a local `sleep(ms)` helper (4 call sites).

**Follow-up bugfix (same session, from Jeremy's playtest):** he noticed an overdue task whose sprite had the red box but whose agenda row had no red highlight. **Not caused by this extraction** — a pre-existing defect. `createListItem` never derived row styling from `isOverdue`; the class was only ever added by `markAsOverdue`, which early-returns when the item is *already* overdue. So it styles the row only on the TRANSITION into overdue, and any path that REBUILDS an already-overdue row loses it — the edit-due-date handler (which is what Jeremy actually hit), adding a sub-task to an overdue parent, and `restoreGameState` (re-applies the class, then rebuilds sub-tasked parents' rows right after, discarding it). The sprite was fine because `item.element` is never rebuilt, which is exactly why the two disagreed. **Fixed by deriving it in `createListItem`**, making row construction idempotent and covering all rebuild paths. Reproduced live in Chrome (edit → class gone), fixed, re-verified live (edit → class retained). 6 regression tests added to `test/create-list-item-branching.test.js`. This is the third bug in the "createListItem renders a fresh task rather than the item's current state" family — see DECISIONS.md.

**State:** ✅ **Full Jest suite green in the sandbox: 8 suites, 139/139 passing in 0.5s** (80 pre-existing + 53 new in `test/damage.test.js` + 6 new overdue-styling regressions). `node --check` clean on script.js and every file in js/. The `npm install --omit=optional` fast path from the last session worked — but note the background-install trick still gets reaped; `timeout 40 npm install jest@30` in the foreground is what actually worked. **Jeremy live-playtested the extraction and reported it working**; the overdue-highlight bug he found alongside it was reproduced and fixed live in Chrome. **NOT committed.**

**Next:** commit (commands below), then the next Milestone 2 item is **extract progression (XP, levels, slots)**.

**Watch out:**
- **Two of my new tests failed on first run and both were test bugs, not source bugs** — worth knowing what they revealed. One mixed time bases (`dueDateTime` at real `Date.now()` vs a damage clock at an epoch-relative constant). The other asserted damage after a 1-second absence: `OFFLINE_ANIMATION_THRESHOLD_MS` (30s) is far below `DAMAGE_INTERVAL_MS` (5min), so **any absence short enough to skip the catch-up animation is also too short to have accrued a single tick** — the instant-apply branch can only ever charge damage via the `animatable.length === 0` path. Not a bug, but non-obvious.
- **The puppeteer bump is UNVERIFIED.** The visual suite needs a Chromium download and can't run from the Cowork sandbox. Run `npm install && npm run test:visual` locally before trusting it.
- **`reproduce_subtask_bug.js`** (root level, stale debug script for the long-fixed sub-task bug, not referenced by any npm script) still calls `waitForTimeout` and will break under puppeteer 24. It's dead weight — deleting it is probably right.
- **`damageDeps()` is rebuilt on every call** because `BASE_WIDTH` and the DOM handles aren't resolved until `initGame()` runs. Cheap (an object literal), but don't "optimise" it into a module-level constant or it will capture undefined dims.
- Cached script.js has bitten two sessions running — hard-reload before doubting a fix. `fetch('/script.js',{cache:'reload'})` then `location.reload()` works well from the console.
- **A leftover test task named `REPRO-backdated` is in Jeremy's localStorage** from the live bug repro — delete it (or hit the dev reset button) whenever convenient.
- **Watch the `createListItem` family of bugs.** Three now (2026-07-17 habit-unsafe refs, 2026-07-17 hardcoded task editor, 2026-07-18 overdue styling), all the same shape: the row builder renders "a fresh task" instead of "this item's current state." When the UI extraction happens in Milestone 2, every visual that can be toggled after creation (overdue, high-streak, high-priority, completion) should be derived in the builder, not applied only at the transition.

## 2026-07-18 — Jest setup fixed (the "missing babel deps" diagnosis was wrong)

**Did:** Fixed the test-infrastructure gap that had blocked four sessions. **The standing diagnosis was wrong:** installing `jest` alone pulls `babel-jest` + `@babel/core` transitively (verified by installing only `jest` and inspecting node_modules), and nothing here needs babel anyway — all of `test/` is plain CommonJS on modern Node, no JSX/TS/ESM. No babel devDependencies, no `babel.config.js`. Four real defects fixed (detail + rationale in DECISIONS.md 2026-07-18):
1. **`npm test` never ran Jest** — it ran `node test/visual-tests.js` (puppeteer). Every prior handoff saying "run `npm test`" to check the unit suite was running the wrong thing. Now `test` = `jest`; visual tests are `npm run test:visual`.
2. **`devDependencies` was empty** → added `jest: ^30.4.2`.
3. **`test/setup.js` didn't exist** though `jest.config.js` has referenced it since July 2025, so Jest died before the first test. Created as a real documented file (guards unhandled rejections only; per-test global wiring stays in the test files on purpose).
4. **Coverage was on by default with 80% global thresholds** → would fail `npm test` with every assertion green, since `Spawning.addItemToGame` is deliberately DOM-only. Coverage is now opt-in (`npm run test:coverage`), thresholds dropped until Milestone 2 can meet them honestly.

Also removed the `transform: 'babel-jest'` override (it's Jest's default — almost certainly the source of the whole babel misdiagnosis), and moved `puppeteer`/`pixelmatch`/`pngjs` from `dependencies` to `optionalDependencies` (the game has zero runtime deps; those are visual-test-only and pull a full Chromium download, which is exactly what kept killing sandbox installs). **`npm install --omit=optional` is now the fast jest-only path for Cowork/CI.**

**State:** ✅ **VERIFIED GREEN on Jeremy's machine — the Jest suite ran for the first time ever: 7 suites, 80/80 tests passing in 1.6s.** (`npm install` added 352 packages in 12s.) All suites pass: clock (17), movement (10), spawning (8), live-gap-catchup (16), offline-catchup (16), create-list-item-branching (3), subtask-creation (14). **The warning that the old mirror tests might need fixing turned out to be unnecessary — `offline-catchup`, `create-list-item-branching` and `subtask-creation` all passed unmodified.** They were correct all along, just never runnable. Committed.

**Next:** next Milestone 2 item — extract damage/base-health/game-over (scope now also covers `runLiveGapCatchUp`, `computeDaysSurvived`, and the offline catch-up code deferred from clock.js). Two small optional cleanups noted below.

**Watch out:**
- **`git push` never ran.** The command was mistyped as `git pushcd ...` and errored out, so as of end of session there were unpushed local commits (`5535ed2`, `caf6537`, + the Jest-setup commit). Given the GitHub repo had to be recreated once already after being found deleted (DECISIONS.md 2026-07-17), **push before trusting anything to be backed up.**
- **`test/subtask-creation.test.js` is extremely noisy** — it triggers the `🛠️ createTaskItemData` / `📍 addItemToGame` console.logs hundreds of times, burying the actual results. The `addItemToGame` log now lives in `js/spawning.js`. Easy fix when convenient: silence console.log in `test/setup.js` (`jest.spyOn(console,'log').mockImplementation(()=>{})`), or drop the debug logs from the source now that the sub-task bug is long fixed.
- **`npm audit` reports 9 vulnerabilities (1 critical, 5 high)** — all from `puppeteer@21.11.0`, which npm flags as no longer supported (<22.8.2). It's now an `optionalDependency` used only by the visual tests, so it doesn't ship with the game, but bumping it (or dropping the visual tests) would clear the audit.
- The `--omit=optional` fast path is the one to use from Cowork; a full `npm install` downloads Chromium and will likely be killed in the sandbox.

## 2026-07-18 — Milestone 2 extraction #2 (spawning + movement), dev reset button, and three overdue-damage bugs from Jeremy's overnight test (Cowork session, live browser verification)

**Did:** Three pieces of work.

**Part 1 — Milestone 2 extraction #2: enemy spawning + movement.** Jeremy chose to do the whole roadmap line in one session rather than split it (ARCHITECTURE.md lists them as two modules). Created `js/movement.js` (`getSubTaskClusterOffset`, `calculateTimelineXWithClustering`, `getItemTopPosition`, plus `getVisibleEdges` which is now module-internal — it had no other caller) and `js/spawning.js` (`addItemToGame` plus a new pure `resolveEnemyVisual(itemData, dims)` → `{width, height, classes}`, split out so the sprite-size/CSS-class decision is unit-testable without a DOM). Both follow the clock.js pattern: state/dims passed explicitly instead of closures (`ctx = {activeItems, dims, canvasHeight, randomFn?}` for movement; a `deps` bag of collaborators for spawning), `CONFIG`/`Clock` stay globals, `module.exports` for Node. script.js keeps thin wrappers so all call sites are unchanged. Wired into index.html after clock.js. New `test/movement.test.js` + `test/spawning.test.js`.

**Part 2 — dev-only reset button** (Jeremy asked for it mid-session): `#resetTestButton` bottom-left (`.reset-test-button` in style.css), handler next to the restart handler. Unlike "Start New Base", it ALSO clears `definedHabits`/`definedRoutines` (initGame doesn't — it re-seeds habit instances from them), so it's a true clean slate: clears save, `initGame()`, persists the empty state. Guarded by `confirm()`. **Dev-only — remove or hide before shipping.**

**Part 3 — three overdue-damage bugs, all one root cause.** Jeremy left the tab open overnight with one overdue task and returned to a 0 HP base and "GAME OVER! Your Base Survived 22 Days." Root cause: whenever `lastDamageTickTime` is far in the past, `updateActiveItems` pays out one `DAMAGE_INTERVAL_MS` per 50ms tick. The `OFFLINE_DAMAGE_CAP_PER_ITEM` guard only ever covered the RELOAD path, so every route skipping a reload was unprotected. Fixes (rationale in DECISIONS.md 2026-07-18):
1. **Suspended loop (sleep/throttled tab):** `updateGame` records `lastLoopTickMs`; a gap ≥ `CONFIG.LIVE_GAP_THRESHOLD_MS` (new, 30s) calls new `runLiveGapCatchUp()` BEFORE `updateActiveItems()`, charging capped damage against the same `offlineDamageCharged` lifetime budget the reload path uses, marking anything that fell due during the gap, and parking each clock at the caught-up time. **Deliberate:** pending ticks come from `lastDamageTickTime` advanced in WHOLE intervals (remainder kept), NOT from the gap window — a window calc would `floor()` a background tab's ~60s wakeups to zero every time, making backgrounding a damage-evasion loophole. Test covers it.
2. **"22 Days":** `DAY_DURATION_MS` (60000) drove a `setInterval` incrementing `daysSurvived` every real MINUTE, and only while awake — so 22 min of runtime read as 22 days while the ~10 real hours counted for nothing. Removed `DAY_DURATION_MS` + `dayTimerInterval` entirely (BALANCE CHANGE, logged); added `CONFIG.MS_PER_REAL_DAY` + `computeDaysSurvived()` = `floor((now - runStartedAtMs)/MS_PER_REAL_DAY)`, frozen at death in `gameOver()`. New persisted scalar `runStartedAtMs` (no schemaVersion bump — additive, falls back to `save.savedAt` then `Date.now()`).
3. **Found while verifying #1 — creating a backdated task instantly drained the base.** `markAsOverdue` parks the clock at the DUE time, so a task created 3 hours overdue replayed 36 intervals immediately. Reproduced live (100 → 64), then fixed in `js/spawning.js`: after `markAsOverdue`, set `lastDamageTickTime = Date.now()`. The item didn't exist during that window, so nothing is owed; it still takes normal live damage from spawn on. Restore unaffected (it resets the same clock right after and back-charges separately).

**Also confirmed NOT bugs** (Jeremy flagged both): the midnight line is correctly hidden at 7:47 AM (`shouldShowMidnightLine` = `hours >= 20`), and the 11 AM sprite was correctly positioned (~3.2h out → 2–4h band → ~65% across, matching his screenshot).

**State:** `node --check` passes on script.js + config/movement/spawning/clock/persistence. **Jest could NOT be run in the sandbox this session** — `npm install` was repeatedly killed mid-install (background processes get reaped), so the repo's jest suite has NOT been executed. All new logic was instead verified with Node's built-in test runner against the REAL modules: 16/16 movement+spawning, 16/16 gap-catchup + day-count + spawn-overdue. The committed jest files (`test/movement.test.js`, `test/spawning.test.js`, `test/live-gap-catchup.test.js`) mirror those assertions but **have not themselves been run — Jeremy should run `npm test` locally.** Live-verified in Chrome: extraction #2 (parent + 2 sub-tasks spawn, cluster right/left, bottom-align, correct sprite sizes, `addItemToGame` firing from js/spawning.js, zero console errors), the reset button, the backdated-task fix (100 → 64 before, holds at 100 after), `runStartedAtMs` persisting, and a restored 3-day-old run yielding "3 Days". **NOT committed.**

**Next:** Jeremy runs `npm test` locally and does a real overnight/sleep test (leave ONE task overdue → expect ≤12 damage, not a dead base). Then commit (commands below). After that, the next Milestone 2 item is **extract damage/base-health/game-over** — note its scope has grown: it should now also absorb `runLiveGapCatchUp`, `computeDaysSurvived`, and the offline catch-up code deferred from clock.js.

**Watch out:**
- **The sleep-gap path (fix #1) was never reproduced live** — it needs an actual machine suspend, which can't be simulated from the browser. Math is unit-tested and the code path is short, but the overnight test is the real confirmation. If it still misbehaves, check `lastLoopTickMs` and that nothing resets the clock before `runLiveGapCatchUp` runs.
- **Cached script.js bit hard again.** A normal reload served a stale script.js and made the new fixes look absent (`runStartedAtMs` missing from the save), costing real debugging time. Hard-reload (Ctrl+Shift+R) before doubting a fix. Note `fetch('/script.js?cb=...')` returns **404** on this server (it treats the query string as part of the filename) — use `fetch('/script.js', {cache:'reload'})` instead.
- **`resetTestButton` is dev-only** — remove or hide before shipping.
- **jest/babel/test-setup gap in package.json is STILL unfixed** and is now actively blocking (sandbox can't install). Worth a small dedicated session: add jest + babel-jest + @babel/core + @babel/preset-env to devDependencies, a real (even empty) `test/setup.js`, and a `babel.config.js`.
- **Live damage remains uncapped by design** — the cap applies only to time the player was away. An item left overdue while you watch will still eventually kill the base.
- **Commit hygiene:** uncommitted again this session — two logical commits suggested below.

## 2026-07-17 — Milestone 2 extraction #1: clock.js (Cowork session, Fable planning + Sonnet execution)
**Did:** First Milestone 2 modularization task. Moved `calculateTimelinePosition` and `updateMidnightLine` out of script.js into new `js/clock.js` (also split midnight-line math into testable `calculateMidnightLinePosition`/`shouldShowMidnightLine`). The two functions previously closed over module-scoped `let GAME_SCREEN_WIDTH, BASE_WIDTH, ENEMY_WIDTH, HABIT_ENEMY_WIDTH` (DOM-derived in `initGame()`); the extracted versions take these explicitly as a `dims` object instead. script.js keeps `calculateTimelinePosition(item, currentTime)`/`updateMidnightLine(currentTime)` as thin wrappers building `dims` and delegating to `Clock.*` — every original call site is unchanged. Wired `js/clock.js` into `index.html` (after persistence.js, before IdCounter.js).

Two process decisions made with Jeremy on Fable before execution (both logged in DECISIONS.md): (1) cancelled the planned "convert to ES modules early in Milestone 2" step — staying with the global + `module.exports` pattern proven by config.js/persistence.js, no build step; ARCHITECTURE.md's refactor strategy amended. (2) clock.js's scope excludes offline catch-up even though ARCHITECTURE.md's target layout originally listed it there — that code is freshly playtested and tangled with damage/DOM-animation state, so it stays in script.js and will move to the future damage.js extraction instead; ARCHITECTURE.md's target layout comments updated to reflect this.

New `test/clock.test.js` (16 cases) — requires the real `js/clock.js` module directly (not a hand-maintained mirror; clock.js has real `module.exports`, unlike the pre-existing mirror-style tests). While writing tests, found and documented (did NOT fix, out of scope) a pre-existing dead branch: the "due next day or later → gameScreenWidth" early return in `calculateTimelinePosition` can never trigger for a real `Date`, since `nextMidnight` is always derived from the item's own due date and is therefore always after it.
**State:** `node --check` passes on script.js and js/clock.js. Full Jest suite in sandbox: 46/46 passing (30 pre-existing + 16 new clock tests). Sandbox test run again hit the pre-existing gap (package.json missing jest/babel deps, missing test/setup.js — logged before, still not fixed in-repo) — worked around with a local sandbox install + empty setup.js stub, same as prior sessions. **NOT yet live-playtested by Jeremy** (this extraction shouldn't change any visible behavior — same math, same call sites — but hasn't been confirmed in the browser). **NOT yet committed.**
**Next:** Jeremy should hard-reload and playtest that zombie positions and the 8pm+ midnight line still look/behave identically to before (no visible change expected). If clean, commit (commands below), then start the next Milestone 2 item: extract enemy spawning + movement.
**Watch out:**
- **clock.js's `updateMidnightLine` DOM wrapper takes the element as an optional third param** (falls back to `document.getElementById('midnightLine')` if omitted) — added specifically so tests can inject a fake element without a DOM. script.js's wrapper doesn't pass one, so it still resolves from the live DOM as before.
- **The "due next day or later" branch in `calculateTimelinePosition` is dead code** for real due dates (see DECISIONS.md) — not fixed this session, just documented. If a multi-day-out item ever looks visually wrong, this is the first place to check.
- **jest/babel/test-setup gap in package.json is still unfixed** — every session so far has worked around it in the sandbox rather than fixing the repo. Worth a small dedicated session at some point (add jest+babel-jest+@babel/core+@babel/preset-env to package.json devDependencies, add a real (even if empty) test/setup.js, add a babel.config.js).
- **Commit hygiene:** uncommitted this session — paste the commands below in Jeremy's terminal.

## 2026-07-17 — Offline catch-up on load implemented; Milestone 1 complete (Cowork session)
**Did:** The last Milestone 1 item. Decided the deferred offline-damage policy with Jeremy (on Fable): **per-item capped back-charge** — real elapsed overdue damage on restore, capped at `CONFIG.OFFLINE_DAMAGE_CAP_PER_ITEM = 12` HP. Design principle: punish the COUNT of neglected items, not the DURATION you were away (sleeping shouldn't be able to kill the base alone; enough neglected items still can). Rejected no-back-charge (rewards not opening the app) and full/reduced-rate back-charge (duration-scaled, near-guarantees base death overnight, teaches nothing).

Implementation (script.js + js/config.js + style.css):
- New `CONFIG` offline constants: `OFFLINE_DAMAGE_CAP_PER_ITEM` (12), `OFFLINE_MAX_MS` (3-day spec cap), `OFFLINE_CATCHUP_MAX_MS`/`MIN_MS`/`MS_PER_ITEM` (animation timing, ≤5s per spec), `OFFLINE_ANIMATION_THRESHOLD_MS` (skip animation under 30s absence).
- `restoreGameState()` now computes the offline window from the save's `savedAt`, captures each item's saved x position, and calls new `runOfflineCatchUp(entries, offlineMs)`: computes each item's current-time target position (parents before sub-tasks, so clustering resolves correctly), animates saved→target with a requestAnimationFrame ease-out (`.catching-up` CSS class disables the `.enemy` transition + adds a motion-trail filter), then applies back-charged damage via `applyOfflineDamage`. `updateActiveItems()` gets a new `offlineCatchUpActive` guard so the live loop doesn't fight the animation.
- New pure function `computeOfflineOverdueDamage(dueMs, nowMs, offlineMs, alreadyCharged)` — real elapsed ticks at the live rate, capped at the item's REMAINING budget (`cap - alreadyCharged`). Mirrored in `test/offline-catchup.test.js`.
- **Mid-session revision** (Jeremy asked "is this capped per day?"): caught that the cap was per OFFLINE WINDOW, not per item lifetime — repeated open/close cycles on the same still-overdue item across days could re-charge near the cap each time, drifting back toward "duration matters." Fixed by adding `item.offlineDamageCharged` (new field on task/habit item objects, defaults 0, persists automatically since it's just a plain property picked up by the existing save serializer) tracking a running lifetime total; `applyOfflineDamage` increments it after each hit. Test added: 5 simulated cross-day restores on one item sum to exactly the cap, not 5×cap.
- **Companion fix** (Jeremy asked whether users can fix an over-aggressive due date — they could edit it via `showEditTaskModal`, but doing so on an already-overdue task didn't clear its overdue state, so it kept ticking live damage regardless of the new date). Added `recomputeOverdueStateAfterEdit(item)` next to `markAsOverdue`: re-derives `isOverdue` from the current `dueDateTime` and either un-marks it (clears classes/position/damage-clock) or marks it overdue-from-now if the edit pulled the date into the past. Wired into the edit modal's save handler, which was also missing an explicit `saveGame()` call (now added — previously relied on the 5s autosave safety net).
**State:** `node --check script.js` passes. Full Jest suite in sandbox: 30/30 passing (3 suites — existing 2 unchanged, `test/offline-catchup.test.js` is new with 12 cases covering single-window cap math, no-double-charge with the live tick loop, and the lifetime-cap revision). **NOT yet live-playtested by Jeremy** — he relaunched the server but hadn't reported a test result before this session's design conversation moved to the per-day/due-date-edit questions and consumed the rest of the session. **NOT yet committed.**
**Next:** Jeremy should live-playtest before anything else: (a) create a task due ~1 min out, close the tab, wait 10-15 min, reopen — expect a brief animated sweep to the base + a small capped damage hit, not a huge one; (b) reopen within 30s of closing — no animation, instant position update, no damage; (c) with an overdue task, edit its due date forward via the pencil — zombie should un-camp from the base and stop taking damage. If all three check out, commit (commands below) and move to Milestone 2 (extract clock/timeline positioning — first item in the modularization order).
**Watch out:**
- **Lifetime cap depends on `item.offlineDamageCharged` surviving in old saves without the field** — reads always guard with `|| 0`, but worth confirming Jeremy's actual current localStorage save (schemaVersion 1, pre-dates this field) restores cleanly; should be fine since `restoreGameState` doesn't require the field, but hasn't been exercised against a REAL pre-existing save from his browser, only fresh sandbox data.
- **Cache-busting still applies** — hard-reload (Ctrl+Shift+R) before testing, per every prior session's note.
- **Habit-instance editor doesn't get the due-date-edit fix** — `recomputeOverdueStateAfterEdit` is only wired into `showEditTaskModal` (tasks). Habits' due time comes from frequency/timeOfDay via the definition editor, which already doesn't retroactively move a live instance (pre-existing, logged 2026-07-17 in the createListItem-refactor entry) — same gap, not addressed today since it's a different code path and would be a second task.
- **Commit hygiene:** uncommitted again this session — paste the commands below in Jeremy's terminal.

---

## 2026-07-17 — Fixed habit-modal bug, then refactored createListItem into task/habit branches (Cowork session, live browser verification)
**Did:** Two related pieces of work, both in Milestone 1's habit-modal bug and its root cause.

**Part 1 — root-caused and fixed** the "create-habit modal doesn't close on Save; re-click duplicates habit + sprite" bug (found in the prior persistence playtest). Root cause was NOT the modal/Save handler — it was two habit-unsafe references in `createListItem(itemData)`, each throwing for `type:'habit'` items and aborting `createHabitDefinition` before its trailing `closeModal()`:
- `script.js:687` `itemData.subTasks.forEach(...)` → habits never set `subTasks` (only `createTaskItemData` does). Fixed to `(itemData.subTasks || []).forEach(...)`.
- `script.js:791` `itemNameContainer.appendChild(streakSpan)` → `itemNameContainer` is a phantom (never declared). Fixed to `itemDetailsContainer.appendChild(streakSpan)` (streak badge sits next to the category badge).
The board sprite is drawn by `addItemToGame` BEFORE `createListItem`, so creation LOOKED successful but the list-item build threw → modal never closed → each re-click added another `definedHabits` entry + sprite. Bug #2 was masked by #1 (habits died at 687 first); found #2 only by re-testing after fixing #1. Both also fired on the boot restore path, so restored habits had sprites but no agenda rows.

**Part 2 — refactored `createListItem`** (Jeremy asked to address the "task-first, habit-unsafe" pattern flagged in part 1) into a shared shell + explicit `task`/`habit`/default branches, so the same class of bug can't hide in it again. Along the way found a THIRD habit hazard that part 1 hadn't reached: the edit pencil on every row was hardcoded to `showEditTaskModal` (a task-shaped editor with no habit fields) — never triggered in practice since habit rows never rendered before part 1's fix, but would have silently clobbered a habit's shape the first time someone clicked it. Added `showEditHabitInstanceModal()` (resolves the instance's `definitionId` back to `definedHabits`, opens the existing `showEditHabitForm` definition editor) and wired the habit branch's pencil to it. Also updated `saveEditedHabit` to sync any already-spawned instance (name/category/isNegative, sprite classes, list item, `saveGame()`) after saving the definition — it previously only updated `definedHabits`, leaving today's live row stale. Confirmed via code read that routines never reach `createListItem` (they're definitions that spawn tasks/habits, never items themselves) — no routine branch needed.

**State:** `node --check script.js` passes. Added `test/create-list-item-branching.test.js` (hand-maintained mirror, matching the existing test file's convention — script.js has no module.exports to `require` against, see docs/ARCHITECTURE.md): task renders with sub-task section + task editor and no streak badge; habit renders (using an object that omits `subTasks` entirely — the exact shape that used to throw) with streak badge + habit editor and no sub-task section; unrecognized type warns without throwing. Full suite: 17/17 passing. **Live-verified in Jeremy's browser** both times (Cowork drove Chrome against his `node server.js`): part 1 — reproduced the exact stack twice from the live console, then after both fixes + hard reload confirmed modal closes, no duplication, habits render as agenda rows for the first time, zero exceptions; part 2 — created one task + one habit, confirmed only the task shows "+ SUB-TASK" and only the habit shows "Streak: 0", task pencil → "Edit Task", habit pencil → "Edit Habit" pre-filled from the definition, edited the habit name and saved → agenda row updated immediately with no reload, confirmed both `definedHabits` and the live instance show the new name in the persisted save. Test data cleared from localStorage after each round — game left on a fresh empty save. **NOT yet committed** — commands below for Jeremy's terminal.
**Next:** Pick one: (a) offline catch-up on load (last Milestone 1 item — paused zombies animate to current position, max 5s; also owns the deferred "how much offline overdue damage applies" decision); (b) the `showForm` duplicate/shadowing bug (real `showForm` ~line 323 vs legacy stub ~line 3571 that only handles 'routine' and silently wins — confirmed present again this session; the inline `#habitForm` UI is dead because of it, though the modal path is what's actually used); (c) `uncompleteItem` doesn't restore sub-task sizing/classes (low priority).
**Watch out:**
- **Cache-busting:** `node server.js` + Chrome cached `script.js` — a normal reload served the OLD file and a fix appeared not to work. Had to hard-reload (Ctrl+Shift+R) each time. If a fix ever "doesn't take" in the browser, hard-reload before doubting the code; you can also `fetch('/script.js').then(r=>r.text())` and grep it to confirm what's actually being served.
- **localStorage cleanup is fighting the unload-flush:** clearing `deadline.save` then reloading does NOT stick, because persistence flushes on `beforeunload`/`visibilitychange` and rewrites the live in-memory state right before the page unloads. To force a clean slate from the console: write a valid empty save (`schemaVersion:1`, empty arrays), monkeypatch `Storage.prototype.setItem` to ignore `deadline.save` writes, THEN reload — the fresh page loads empty and its native setItem resumes normally.
- **`showEditTaskModal`'s sub-task edit path** (script.js ~730, inside `createListItem`'s task branch) still points every sub-task's pencil at the task editor — correct today since sub-tasks are tasks, but re-check if sub-tasks ever gain their own type.
- **Frequency/timeOfDay edits don't retroactively apply** to an already-spawned habit instance (same category as the deferred offline-catch-up decision) — only name/category/isNegative sync live; a frequency/timeOfDay change takes effect for tomorrow's instance.
- **Commit hygiene:** per the standing note, commit at the end of EVERY session. This session's edits (across both parts) are uncommitted — see below.

## 2026-07-17 — localStorage persistence implemented (Cowork session)
**Did:** Milestone 1 — localStorage persistence. Decision made with Jeremy (on Fable): **schemaVersion 1 saves the CURRENT in-memory shapes as-is**, not DATA_SCHEMA.md's target schema; shapes reconcile during Milestone 2 extractions via schemaVersion bumps + migrations (full rationale in DECISIONS.md 2026-07-17).
- New `js/persistence.js` (global `Persistence`, module.exports for Node like config.js): `requestSave()` debounced 500ms, `flush()`, `load()` with strict ISO-datetime Date reviver, `clear()`, migration chain scaffold, DOM refs (`element`/`listItemElement`) stripped by the serializer. Wired into index.html load order after config.js.
- script.js: added `getPersistableState()`/`saveGame()`/`restoreGameState()` (after `updateTaskCountDisplay`, ~line 229). Restore runs once after `initGame()` on boot: scalars → definitions → completedItems → active items re-added via `addItemToGame()` (rebuilds DOM; saved order keeps parents before sub-tasks) → re-apply overdue classes (`markAsOverdue` early-returns when `isOverdue` already true) → rebuild parent list items (createListItem resolves sub-task rows from `activeItems`, which weren't fully populated on first pass) → `generateDailyHabitInstances()` → refresh all displays → `gameOver()` if the save was a dead run.
- Save triggers: explicit `saveGame()` at 13 sites (addItemToGame, completeItem, uncompleteItem, removeItem, markAsOverdue, damageBase, gameOver, createHabitDefinition, createRoutineDefinition, deleteRoutine, restart handler) + 5s autosave safety net in `updateGame()` (`CONFIG.PERSISTENCE_AUTOSAVE_MS`) covering un-hooked edit paths (routine editors, task edit modal) + `flush()` on `visibilitychange:hidden` and `beforeunload` (visibilitychange matters — mobile Chrome doesn't reliably fire beforeunload). Restart button now clears the save before `initGame()` so a reload can't resurrect a dead run.
- **Offline overdue damage deliberately NOT charged on load** — restored overdue items get `lastDamageTickTime = Date.now()`; otherwise `updateActiveItems` deals 1 damage per 50ms game tick until "caught up" and an overnight save would insta-kill the base. That behavior belongs to the offline catch-up task.
- Docs: DECISIONS.md entry, DATA_SCHEMA.md "IMPLEMENTED" note, ROADMAP item checked.
**State:** `node --check` passes on script.js, js/persistence.js, js/config.js. Sandbox round-trip smoke test passed (Dates revived incl. `lastCompletionDate`/`originalDueDate`, DOM refs stripped, date-lookalike strings/ids untouched, `undefined` parentId drops out of JSON but stays falsy, unknown schemaVersion ignored, flush/clear paths). **Live-verified by Jeremy** — playtested refresh/complete/overdue/restart flows, "works well for now." Committed and pushed to GitHub as `f657ca8` (along with the previously-uncommitted config.js/parse-fix/sub-task-rendering work from the prior session — both had been sitting locally uncommitted; caught via a GitHub API check when Jeremy asked to verify the push, and a stale `.git/index.lock` blocked the first commit attempt — resolved with `Remove-Item .git\index.lock -Force`).

While playtesting, Jeremy found a new bug (not persistence-related, pre-existing UI issue): **the "create habit" modal doesn't close after Save.** Repro: open create-habit form → fill in → Save → modal stays open → if clicked again (or Save clicked again), it duplicates the habit definition and its on-screen sprite. Downstream effect: opening "add habit to routine" then shows two entries for what should be one habit. Not investigated or fixed this session — logged for next session per below.
**Next:** Pick one: (a) **habit-modal-doesn't-close-on-save bug** (new, found in this session's playtest — repro above; likely in the habit form's Save handler around `showCreateHabitForm`/`attachModalEventListeners`/`createHabitDefinition`, script.js ~2338-3113 — not yet grepped/root-caused); (b) offline catch-up on load (last Milestone 1 item — zombies animate to current position, max 5s; also owns the "how much offline overdue damage applies" decision this session deferred); (c) the `showForm` duplicate/shadowing bug (~line 229 real vs ~3547 legacy stub — the stub still silently wins; untouched today); (d) `uncompleteItem` doesn't restore sub-task sizing/classes (still open, low priority).
**Watch out:**
- If a restored save ever looks wrong, `localStorage.removeItem('deadline.save')` in DevTools console = clean slate. Corrupt/unknown saves are ignored automatically (console.warn) rather than crashing.
- The 5s autosave in `updateGame()` is a stopgap — when Milestone 2 extracts UI/form code, hook `saveGame()` into the edit paths directly and consider dropping the interval.
- `deadline.settings` (DATA_SCHEMA.md) is still unimplemented — only `deadline.save` exists.
- Habit instances for a NEW day only spawn on boot (`restoreGameState`/`initGame` → `generateDailyHabitInstances`) or habit creation — if the tab stays open across midnight, that's pre-existing behavior, not a persistence regression.
- **Commit hygiene:** two sessions' worth of work went uncommitted before this one got caught — worth committing at the end of EVERY session from now on (not just when Jeremy asks to "check" something), to avoid this recurring.
- **New habit-modal bug also means persistence will now duplicate-save any habit created via the buggy double-click path** — not a persistence bug itself, but worth keeping in mind: if a save ever shows a duplicate habit definition, this is almost certainly why.

## 2026-07-17 — Created js/config.js; discovered & fixed script.js was unparseable (Cowork session)
**Did:** Milestone 1 task 3 — created `js/config.js` (a `CONFIG` object) and moved every named gameplay constant out of script.js's "Game Settings" block into it, plus three hardcoded magic numbers that were clearly balance values (ENEMY_WIDTH/HABIT_ENEMY_WIDTH, initial baseHealth, the habit streak-bonus point bump). script.js keeps local `const NAME = CONFIG.NAME` aliases so every existing call site works unchanged — no balance values changed, only relocated. Wired `js/config.js` into index.html's script load order (before IdCounter/TaskManager/script.js). Verified via a standalone `node -e "require('./js/config.js')"` that every value matches the originals exactly, and via grep that all `CONFIG.*` references resolve.

While verifying, `node --check script.js` failed — turned out script.js has never been parseable JS in its currently-committed form (confirmed via git history that all of this predates today). Found and fixed 4 distinct structural defects, then re-ran node --check after each fix until it passed clean:
1. Dead-code `const itemIdCounter = new IdCounter(); const taskManager = new TaskManager(...)` collided with the real `let itemIdCounter` used everywhere else — the TaskManager/IdCounter pairing was never actually used (0 references to `taskManager.` anywhere). Deleted the dead lines.
2. Dead-code `const { gameIsOver } = window;` collided with the real `let gameIsOver` — `window.gameIsOver` is never set anywhere. Deleted.
3. `function completeItem(itemId) {` header was missing entirely; body was present and called from 3 sites. Re-inserted.
4. `function uncompleteItem(itemId) {` header was missing; body present, called from 1 site. Re-inserted.
5. `function showCreateSubTaskModal(parentId) {` header was missing; body present, called from `createSubTaskPrompt`. Re-inserted.

Full detail + evidence for each in DECISIONS.md. Updated ROADMAP.md (checked off the config.js item, noted the script.js fixes).

Jeremy then ran `node server.js` and loaded the game in a browser — first real playtest since script.js could parse. It rendered (health/XP/level/points HUD, church base, zombie sprites, agenda list all worked), but he immediately reproduced sub-task duplication: a sub-task of his first task showed up both correctly nested under its parent AND as its own standalone top-level task. Root cause: `itemIdCounter` starts at 0, so the first-ever task gets `id: 0`; every sub-task/parent check in script.js is a truthy test (`if (item.parentId)`, `if (!item.parentId)`, 10+ call sites), and `0` is falsy — so a sub-task of task 0 fails every one of those checks and renders as if it had no parent. The `38409ca` guard clauses from the earlier "verified fixed" conclusion are real, but nobody had hit id 0 in a live browser before today (script.js couldn't parse; the Jest test is a self-contained mirror that doesn't hit this case). **Fix:** `itemIdCounter = 0` → `itemIdCounter = 1` in `initGame()` — smallest change that guarantees no id is ever falsy, without touching the ~10 truthy-check call sites (rewriting those to explicit `!= null` checks is a good Milestone 2 cleanup candidate, logged in DECISIONS.md, not done now). `node --check script.js` still passes clean after this fix.
**State:** `node --check script.js` and `node --check js/config.js` both pass clean. Game confirmed loading and rendering in a real browser (Jeremy's screenshot). Sub-task duplication fix applied but **not yet re-verified live** — Jeremy should reload and retest creating a sub-task off the very first task to confirm this specific fix, plus general playtesting since this is the first time the file has run at all. Jest wasn't re-run this session (sandbox npm install kept getting interrupted by a tool timeout; the existing `test/subtask-creation.test.js` doesn't exercise the code touched today either way — self-contained mirror, not a `require` of script.js).
Jeremy confirmed the id-0 fix worked, then found two more sub-task rendering issues on the same playtest: the sub-task sprite was cropped instead of scaled down, and he wants sub-tasks to render clustered next to their parent (same vertical level, offset left/right) rather than floating at a random height. Both fixed:
- **Cropping:** root cause was a CSS specificity/`!important` conflict — legacy `.enemy.category-<X>` rules (style.css ~line 311) force `background-size: 128px 128px !important` on every enemy including 64px sub-tasks and 70px habits, beating the correctly-sized-but-non-`!important` `zombie-subtask`/`zombie-small` rules. Added `!important` to the smaller rules too so their higher selector specificity wins. Fixes both sub-tasks and habits (same bug, habits just hadn't been reported yet).
- **Clustering:** added `getSubTaskClusterOffset()` and `getItemTopPosition()` helpers in script.js (next to `calculateTimelinePosition`). Sub-tasks now render at their parent's exact vertical position and get a horizontal stagger (alternating +45/-45/+90/-90... per sibling, ordered by creation) added to their normal due-date timeline x. Wired into all 4 places script.js computes item x and both places it sets top.

Updated docs/MECHANICS.md's "Sub-tasks" section to describe this and flag it doesn't match PROJECT_SPEC.md's original "parent grows/shrinks" idea — that's unresolved, logged as a Milestone 3 open question, not decided today.

Jeremy then refined the clustering ask: sub-tasks should bottom-align with the parent ("shoes lined up on the same level") rather than top-align, and the parent should always render above an overlapping sub-task. Fixed both:
- **Bottom alignment:** `getItemTopPosition` was returning the parent's `top` directly, which lines up the tops (heads), not the bottoms (feet), since sub-tasks are shorter (64px vs 128px). Now returns `parentTop + (parentHeight - itemHeight)`, reading the parent's actual rendered height so it still works if parent height ever changes dynamically (e.g. the still-unresolved shrinking-parent idea).
- **Stacking order:** sub-task DOM elements are added after their parent, so with no explicit z-index they were winning the default stacking order and could cover it. Added `z-index: 10` to the base `.enemy` rule and `z-index: 5` to `.zombie-subtask` in style.css — existing higher values (`.enemy:hover` = 20, `.enemy.high-priority` = 15) still take precedence over this baseline.

Jeremy playtested again and found the clustering itself was inconsistent — one sub-task rendered almost directly behind the parent, another far away. Root cause: the ±45/±90px offset was being added on top of each sub-task's OWN due-date timeline position, and that formula maps hours-until-due nonlinearly across the whole screen width, so even a small due-time difference from the parent could put the base position tens/hundreds of px away — swamping the small cluster offset. Jeremy's clarified intent: sub-tasks should track the PARENT's position by default and only show their own due-date position when it's due significantly earlier (meaningfully more urgent). **Fix:** added `calculateTimelineXWithClustering()` in script.js, replacing `calculateTimelinePosition(...) + getSubTaskClusterOffset(...)` at all 3 non-overdue call sites. It compares the sub-task's own timeline x to its parent's current x; only if the sub-task's own position is 150px+ closer to the base (`SUBTASK_AHEAD_THRESHOLD_PX`, a tunable starting guess) does it use its own position — otherwise it clusters at `parentTask.x + offset`. Overdue handling (both go to `BASE_WIDTH + offset`) was already correct and untouched.

Jeremy playtested yet again — "still not right, almost the exact same as before." Found a 4th overdue-positioning site that was missed: `addItemToGame()` (the function that runs at actual creation time) has its own separate "already overdue at creation" check that still set `x = BASE_WIDTH` with no offset at all — distinct from the two overdue checks already fixed (`uncompleteItem`, `updateActiveItems`). Since the test tasks were due "5:00 PM" today and testing happened after 5pm, they were overdue *at the moment of creation*, so this exact unfixed branch fired every time the clustering fix was tested — making it look like nothing had changed. **Fix:** same one-line pattern, `itemData.x = BASE_WIDTH + getSubTaskClusterOffset(itemData);` at script.js ~line 467. Verified via `grep '\.x = BASE_WIDTH'` that all 3 sites in the file now consistently include the offset — this was the last one.

Jeremy tested with 3 sub-tasks (`test2`/`test3` due same time as parent, `test4far` due later) — still saw the 1st sub-task directly behind the parent and the 2nd "too far ahead." This time it was the offset MAGNITUDE, not the branching logic: the flat ±45px/±90px guess never accounted for actual sprite sizes — a 45px offset on a 128px-wide parent still overlaps it almost entirely (45+64=109 < 128), so the "1st sub-task" could never look separated. **Fix:** rewrote `getSubTaskClusterOffset` to compute real, non-overlapping spans from actual widths — right side starts at `ENEMY_WIDTH + gap` (clears parent's right edge), left side starts at `-(SUBTASK_ENEMY_WIDTH + gap)` (clears parent's left edge), each further same-side sibling adds another `SUBTASK_ENEMY_WIDTH + gap`. With current sizes (128/64/8px gap) that's +136, -72, +208, -144, +280... — verified by hand and a quick Node script that none of these spans overlap the parent or an earlier same-side sibling. Added `SUBTASK_ENEMY_WIDTH` (64), `SUBTASK_CLUSTER_GAP_PX` (8), and moved `SUBTASK_AHEAD_THRESHOLD_PX` (150) into `js/config.js` as proper named constants (previously a local script.js const); `addItemToGame`'s sub-task sizing now also reads `CONFIG.SUBTASK_ENEMY_WIDTH` instead of a separate hardcoded `64`, so the render size and the offset math can't drift apart.

Jeremy then spotted the final layer himself: the zombie PNGs have big transparent margins (visible art ≈ middle of the box), so box-edge offsets still looked wrong. Measured all 16 PNGs' alpha-channel bounding boxes (Pillow/numpy): visible art is 44%–92% of box width depending on category, not centered. Added `CONFIG.ZOMBIE_VISIBLE_MARGINS` (per-category measured margins) + `getVisibleEdges()` helper; `getSubTaskClusterOffset` now chains each sibling's VISIBLE edge 8px (`SUBTASK_CLUSTER_GAP_PX`) from the previous member's visible edge, handling mixed categories. Simulation-verified: exact 8px visual gaps, zero overlap, in Jeremy's test case and a worst-case category mix. If art is redrawn, re-measure margins (method in DECISIONS.md).

Jeremy confirmed live: "that works great for now." Sub-task rendering thread is closed.

**Next:** Pick one: (a) continue Milestone 1 — localStorage persistence (docs/DATA_SCHEMA.md) or offline catch-up; (b) the `showForm` duplicate/shadowing bug (script.js ~line 229 vs ~3368 — a later "Legacy" stub silently overrides the real form-switching logic, so `showForm('task')`/`showForm('habit')` currently no-op — found today, not yet fixed); or (c) `uncompleteItem()` doesn't restore sub-task sizing/classes (a completed-then-restored sub-task comes back full-size) — found today, not fixed, lower priority.
**Watch out:**
- **This is the first time anyone has run `node --check` against the full script.js, and the first real browser playtest.** Treat "the monolith runs" claims in older HANDOFF entries as unverified history, not current fact, until more of the game has been clicked through.
- **Falsy-id pattern:** script.js checks `parentId` (and possibly other id-like fields) with plain truthy tests in ~10 places. The `itemIdCounter` start-at-1 fix patches the immediate symptom but the underlying fragility remains — worth a real cleanup pass (`!= null` everywhere) during Milestone 2 extraction rather than leaving it as a landmine.
- The `.git/index.lock` issue recurred again this session (a previous session's stale lock) — Jeremy deleted it manually before this session started. If it recurs: never run `git` from the Cowork sandbox at all against this repo (not even read-only) — read `.git/HEAD`/`.git/refs/**` as plain text instead.
- Sandbox `npm install` for jest/babel-jest kept getting interrupted (tool call timeout at 45s, then background/nohup processes don't survive across separate bash calls in this sandbox — no process persistence between tool calls). If a future session needs to run the real Jest suite from Cowork, expect to retry the install 2-3 times or accept partial/flaky results; verifying via `node --check` + targeted `node -e` smoke tests was more reliable today.
- `showForm` duplicate (see Next) is a real functional bug sitting in committed code, low urgency but worth fixing before relying on task/habit form switching in a demo.

## 2026-07-17 — Verified sub-task bug fix + merged branches + moved off OneDrive (Cowork session)
**Did:** Two Milestone 1 tasks this session.

Task 1 — sub-task duplication bug: Read SUBTASK_BUG_REPRODUCTION_REPORT.md and grepped script.js for `addItemToGame`, `showCreateSubTaskModal`, `sortAndRenderActiveList`, `createListItem`. Found the guard clauses described in the report's "Next Steps for Fix" already present in current script.js. `git show 38409ca -- script.js` confirmed the fix landed in commit `38409ca` ("Fix: prevent duplicate regular task when adding subtask", Jul 29 2025) — the SAME commit that added the repro report, so the report's "Next Steps" section is stale. Ran `test/subtask-creation.test.js` in a sandbox scratchpad (copied package.json/jest.config.js/js/test, added a missing `test/setup.js` stub + babel config) — 14/14 tests pass. No code changed.

Task 2 — branch merge: `main` was an exact ancestor of `feature/sprite-system-cleanup` (10 commits ahead, 0 behind) — a clean fast-forward. Actually getting the merge pushed took most of the session: hit a recurring stale `.git/index.lock` on the OneDrive-synced folder (root cause: any git operation, including Cowork's own read-only checks, can leave a lock behind that OneDrive's/the bridge's process can't clean up); then a GitHub push auth loop (Windows Git Credential Manager had no `credential.helper` set, so every push re-triggered a fresh browser login instead of caching it — fixed with `git config --global credential.helper manager`); then discovered the GitHub remote `schooley02/Deadline` had been deleted at some point since July 2025 (404, 0 repos on the account) — recreated it fresh and re-pushed both branches with `git push -u`. Finally moved the whole project out of OneDrive to `C:\Users\jscho\Projects\Deadline` to eliminate the lock issue permanently, at which point `git checkout main && git merge feature/sprite-system-cleanup && git push origin main` completed cleanly (Fast-forward, `3042c4b..7af6ccb`). Verified via `git branch -vv`: both `main` and `feature/sprite-system-cleanup` at `7af6ccb`, both tracking origin, both clean.

Updated CLAUDE.md (new project path, dropped stale OneDrive guardrails, added the "never run git via the device bridge" rule), docs/ROADMAP.md, docs/DECISIONS.md.
**State:** `main` is now the current branch and has everything (doc/memory system, sprite/mobile-CSS work, verified sub-task fix). Both `main` and `feature/sprite-system-cleanup` pushed to GitHub. Repo lives at `C:\Users\jscho\Projects\Deadline` now, not OneDrive. Not launched/played this session.
**Next:** Milestone 1 task 3 — create `js/config.js` and migrate gameplay constants out of script.js.
**Watch out:**
- **Project moved.** Any future Cowork session needs to request folder access to `C:\Users\jscho\Projects\Deadline`, NOT the old OneDrive path.
- **Never run `git` via the Cowork device bridge against this repo**, even read-only commands — it can leave a stale `.git/index.lock` that blocks Jeremy's own terminal. Read `.git/HEAD` and `.git/refs/**` directly as plain text instead when you need to check branch/commit state from Cowork.
- `package.json` in this repo is actually the visual-regression-test manifest (puppeteer/pixelmatch/pngjs) — it does NOT list `jest`/`babel-jest` as devDependencies even though `jest.config.js` and `test/*.test.js` exist and expect them. Also missing: `test/setup.js` (referenced by `jest.config.js`'s `setupFilesAfterEnv`) and a babel config. Worth a small follow-up task to fix this properly (not just sandbox-scratchpad stubs).
- `test/subtask-creation.test.js` doesn't `require` script.js — it contains its own copy-pasted mirror of `createTaskItemData`/`addItemToGame`. Currently matches script.js, but can silently drift. Consider refactoring so the test imports the real functions once Milestone 2 extracts them into `js/` modules.
- Puppeteer's postinstall tries to download Chrome and fails in the sandbox (403) — installing with `PUPPETEER_SKIP_DOWNLOAD=true` avoids it.
- `feature/sprite-system-cleanup` still exists on GitHub, now identical to `main` — fine to delete whenever, not urgent.

## 2026-07-16 — Repo moved to permanent home + memory system merged (Cowork session)
**Did:** Audited both old folders (May MPE in OneDrive "Baseline - Old", July 2025 Claude Code repo in C:\Users\jscho\Deadline). Copied the July repo here (Claude\Projects\Deadline) — git history verified intact (fsck clean, checksums match). Merged in the docs system, CLAUDE.md, commands, agents, balance-tuning skill — all updated to reflect THIS codebase. Recovered the "lost" July specs from PROJECT_SPEC.md (exponential pricing = base × 1.5^qty; frozen routine recovery rules) into docs/ECONOMY.md and docs/ROUTINES.md. Added .gitignore, untracked node_modules.
**State:** Monolith runs (`node server.js`). node_modules NOT copied — run `npm install` before `npm test`. Branch: feature/sprite-system-cleanup (synced with origin at last July push). Known bug: sub-task duplication (see SUBTASK_BUG_REPRODUCTION_REPORT.md — root cause already identified at script.js ~line 1005-1011: addItemToGame adds subtask to activeItems AND the parent list refresh re-creates it).
**Next:** STEP 0 (one-time, on Jeremy's machine — the Cowork sandbox couldn't write the git index through OneDrive): `git rm -r --cached node_modules` then `git add .gitignore CLAUDE.md docs sessions .claude` then `git commit -m "M0: add memory system; untrack node_modules"`. THEN Milestone 1, task 1: fix the sub-task duplication bug. Read SUBTASK_BUG_REPRODUCTION_REPORT.md, grep script.js for `addItemToGame` and `createSubTaskPrompt`, fix, `npm test`, commit.
**Watch out:**
- NEVER read script.js (3,420 lines) or PROJECT_SPEC.md (2,700 lines) in full — Grep for what you need.
- This folder is OneDrive-synced. If git shows lock errors or phantom modified files, pause OneDrive sync. Recommend right-click folder → "Always keep on this device".
- `git status` may show many files modified due to the 2026-07-16 move (line endings/mode bits) — diff before assuming real changes; consider `git config core.fileMode false`.
- Old copies of this project exist at C:\Users\jscho\Deadline (original — Jeremy to delete after verifying this one) and OneDrive\...\Baseline - Old\Deadline (May-era design assets — KEEP, art sources live there).
