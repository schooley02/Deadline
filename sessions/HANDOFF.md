# Session Handoffs

Newest entry at TOP. Every session appends one entry before ending (use `/end-session`).

## Entry Template
```
## YYYY-MM-DD — <short title>
**Did:** what was completed (files touched, roadmap items checked)
**State:** does it run? tests green? any known breakage?
**Next:** the single next task, with any context the next session needs
**Watch out:** gotchas discovered this session
```

---

## 2026-07-17 — Milestone 2 extraction #1: clock.js (Cowork session, Fable planning + Sonnet execution)
**Did:** First Milestone 2 modularization task. Moved `calculateTimelinePosition` and `updateMidnightLine` out of script.js into new `js/clock.js` (also split midnight-line math into testable `calculateMidnightLinePosition`/`shouldShowMidnightLine`). The two functions previously closed over module-scoped `let GAME_SCREEN_WIDTH, BASE_WIDTH, ENEMY_WIDTH, HABIT_ENEMY_WIDTH` (DOM-derived in `initGame()`); the extracted versions take these explicitly as a `dims` object instead. script.js keeps `calculateTimelinePosition(item, currentTime)`/`updateMidnightLine(currentTime)` as thin wrappers building `dims` and delegating to `Clock.*` — every original call site is unchanged. Wired `js/clock.js` into `index.html` (after persistence.js, before IdCounter.js).

Two process decisions made with Jeremy on Fable before execution (both logged in DECISIONS.md): (1) cancelled the planned "convert to ES modules early in Milestone 2" step — staying with the global + `module.exports` pattern proven by config.js/persistence.js, no build step; ARCHITECTURE.md's refactor strategy amended. (2) clock.js's scope excludes offline catch-up even though ARCHITECTURE.md's target layout originally listed it there — that code is freshly playtested and tangled with damage/DOM-animation state, so it stays in script.js and will move to the future damage.js extraction instead; ARCHITECTURE.md's target layout comments updated to reflect this.

New `test/clock.test.js` (16 cases) — requires the real `js/clock.js` module directly (not a hand-maintained mirror; clock.js has real `module.exports`, unlike the pre-existing mirror-style tests). While writing tests, found and documented (did NOT fix, out of scope) a pre-existing dead branch: the "due next day or later → gameScreenWidth" early return in `calculateTimelinePosition` can never trigger for a real `Date`, since `nextMidnight` is always derived from the item's own due date and is therefore always after it.
**State:** `node --check` passes on script.js and js/clock.js. Full Jest suite in sandbox: 46/46 passing (30 pre-existing + 16 new clock tests). Sandbox test run again hit the pre-existing gap (package.json missing jest/babel deps, missing test/setup.js — logged before, still not fixed in-repo) — worked around with a local sandbox install + empty setup.js stub, same as prior sessions. **NOT yet live-playtested by Jeremy** (this extraction shouldn't change any visible behavior — same math, same call sites — but hasn't been confirmed in the browser). **NOT yet committed.**
**Next:** Jeremy should hard-reload and playtest that zombie positions and the 8pm+ midnight line still look/behave identically to before (no visible change expected). If clean, commit (commands below), then start the next Milestone 2 item: extract enemy spawning + movement.
**Watch out:**
- **clock.js's `updateMidnightLine` DOM wrapper takes the element as an optional third param** (falls back to `document.getElementById('midnightLine')` if omitted) — added specifically so tests can inject a fake element without a DOM. script.js's wrapper doesn't pass one, so it still resolves from the live DOM as before.
- **The "due next day or later" branch in `calculateTimelinePosition` is dead code** for real due dates (see DECISIONS.md) — not fixed this session, just documented. If a multi-day-out item ever looks visually wrong, this is the first place to check.
- **jest/babel/test-setup gap in package.json is still unfixed** — every session so far has worked around it in the sandbox rather than fixing the repo. Worth a small dedicated session at some point (add jest+babel-jest+@babel/core+@babel/preset-env to package.json devDependencies, add a real (even if empty) test/setup.js, add a babel.config.js).
- **Commit hygiene:** uncommitted this session — paste the commands below in Jeremy's terminal.

## 2026-07-17 — Offline catch-up on load implemented; Milestone 1 complete (Cowork session)
**Did:** The last Milestone 1 item. Decided the deferred offline-damage policy with Jeremy (on Fable): **per-item capped back-charge** — real elapsed overdue damage on restore, capped at `CONFIG.OFFLINE_DAMAGE_CAP_PER_ITEM = 12` HP. Design principle: punish the COUNT of neglected items, not the DURATION you were away (sleeping shouldn't be able to kill the base alone; enough neglected items still can). Rejected no-back-charge (rewards not opening the app) and full/reduced-rate back-charge (duration-scaled, near-guarantees base death overnight, teaches nothing).

Implementation (script.js + js/config.js + style.css):
- New `CONFIG` offline constants: `OFFLINE_DAMAGE_CAP_PER_ITEM` (12), `OFFLINE_MAX_MS` (3-day spec cap), `OFFLINE_CATCHUP_MAX_MS`/`MIN_MS`/`MS_PER_ITEM` (animation timing, ≤5s per spec), `OFFLINE_ANIMATION_THRESHOLD_MS` (skip animation under 30s absence).
- `restoreGameState()` now computes the offline window from the save's `savedAt`, captures each item's saved x position, and calls new `runOfflineCatchUp(entries, offlineMs)`: computes each item's current-time target position (parents before sub-tasks, so clustering resolves correctly), animates saved→target with a requestAnimationFrame ease-out (`.catching-up` CSS class disables the `.enemy` transition + adds a motion-trail filter), then applies back-charged damage via `applyOfflineDamage`. `updateActiveItems()` gets a new `offlineCatchUpActive` guard so the live loop doesn't fight the animation.
- New pure function `computeOfflineOverdueDamage(dueMs, nowMs, offlineMs, alreadyCharged)` — real elapsed ticks at the live rate, capped at the item's REMAINING budget (`cap - alreadyCharged`). Mirrored in `test/offline-catchup.test.js`.
- **Mid-session revision** (Jeremy asked "is this capped per day?"): caught that the cap was per OFFLINE WINDOW, not per item lifetime — repeated open/close cycles on the same still-overdue item across days could re-charge near the cap each time, drifting back toward "duration matters." Fixed by adding `item.offlineDamageCharged` (new field on task/habit item objects, defaults 0, persists automatically since it's just a plain property picked up by the existing save serializer) tracking a running lifetime total; `applyOfflineDamage` increments it after each hit. Test added: 5 simulated cross-day restores on one item sum to exactly the cap, not 5×cap.
- **Companion fix** (Jeremy asked whether users can fix an over-aggressive due date — they could edit it via `showEditTaskModal`, but doing so on an already-overdue task didn't clear its overdue state, so it kept ticking live damage regardless of the new date). Added `recomputeOverdueStateAfterEdit(item)` next to `markAsOverdue`: re-derives `isOverdue` from the current `dueDateTime` and either un-marks it (clears classes/position/damage-clock) or marks it overdue-from-now if the edit pulled the date into the past. Wired into the edit modal's save handler, which was also missing an explicit `saveGame()` call (now added — previously relied on the 5s autosave safety net).
**State:** `node --check script.js` passes. Full Jest suite in sandbox: 30/30 passing (3 suites — existing 2 unchanged, `test/offline-catchup.test.js` is new with 12 cases covering single-window cap math, no-double-charge with the live tick loop, and the lifetime-cap revision). **NOT yet live-playtested by Jeremy** — he relaunched the server but hadn't reported a test result before this session's design conversation moved to the per-day/due-date-edit questions and consumed the rest of the session. **NOT yet committed.**
**Next:** Jeremy should live-playtest before anything else: (a) create a task due ~1 min out, close the tab, wait 10-15 min, reopen — expect a brief animated sweep to the base + a small capped damage hit, not a huge one; (b) reopen within 30s of closing — no animation, instant position update, no damage; (c) with an overdue task, edit its due date forward via the pencil — zombie should un-camp from the base and stop taking damage. If all three check out, commit (commands below) and move to Milestone 2 (extract clock/timeline positioning — first item in the modularization order).
**Watch out:**
- **Lifetime cap depends on `item.offlineDamageCharged` surviving in old saves without the field** — reads always guard with `|| 0`, but worth confirming Jeremy's actual current localStorage save (schemaVersion 1, pre-dates this field) restores cleanly; should be fine since `restoreGameState` doesn't require the field, but hasn't been exercised against a REAL pre-existing save from his browser, only fresh sandbox data.
- **Cache-busting still applies** — hard-reload (Ctrl+Shift+R) before testing, per every prior session's note.
- **Habit-instance editor doesn't get the due-date-edit fix** — `recomputeOverdueStateAfterEdit` is only wired into `showEditTaskModal` (tasks). Habits' due time comes from frequency/timeOfDay via the definition editor, which already doesn't retroactively move a live instance (pre-existing, logged 2026-07-17 in the createListItem-refactor entry) — same gap, not addressed today since it's a different code path and would be a second task.
- **Commit hygiene:** uncommitted again this session — paste the commands below in Jeremy's terminal.

---

## 2026-07-17 — Fixed habit-modal bug, then refactored createListItem into task/habit branches (Cowork session, live browser verification)
**Did:** Two related pieces of work, both in Milestone 1's habit-modal bug and its root cause.

**Part 1 — root-caused and fixed** the "create-habit modal doesn't close on Save; re-click duplicates habit + sprite" bug (found in the prior persistence playtest). Root cause was NOT the modal/Save handler — it was two habit-unsafe references in `createListItem(itemData)`, each throwing for `type:'habit'` items and aborting `createHabitDefinition` before its trailing `closeModal()`:
- `script.js:687` `itemData.subTasks.forEach(...)` → habits never set `subTasks` (only `createTaskItemData` does). Fixed to `(itemData.subTasks || []).forEach(...)`.
- `script.js:791` `itemNameContainer.appendChild(streakSpan)` → `itemNameContainer` is a phantom (never declared). Fixed to `itemDetailsContainer.appendChild(streakSpan)` (streak badge sits next to the category badge).
The board sprite is drawn by `addItemToGame` BEFORE `createListItem`, so creation LOOKED successful but the list-item build threw → modal never closed → each re-click added another `definedHabits` entry + sprite. Bug #2 was masked by #1 (habits died at 687 first); found #2 only by re-testing after fixing #1. Both also fired on the boot restore path, so restored habits had sprites but no agenda rows.

**Part 2 — refactored `createListItem`** (Jeremy asked to address the "task-first, habit-unsafe" pattern flagged in part 1) into a shared shell + explicit `task`/`habit`/default branches, so the same class of bug can't hide in it again. Along the way found a THIRD habit hazard that part 1 hadn't reached: the edit pencil on every row was hardcoded to `showEditTaskModal` (a task-shaped editor with no habit fields) — never triggered in practice since habit rows never rendered before part 1's fix, but would have silently clobbered a habit's shape the first time someone clicked it. Added `showEditHabitInstanceModal()` (resolves the instance's `definitionId` back to `definedHabits`, opens the existing `showEditHabitForm` definition editor) and wired the habit branch's pencil to it. Also updated `saveEditedHabit` to sync any already-spawned instance (name/category/isNegative, sprite classes, list item, `saveGame()`) after saving the definition — it previously only updated `definedHabits`, leaving today's live row stale. Confirmed via code read that routines never reach `createListItem` (they're definitions that spawn tasks/habits, never items themselves) — no routine branch needed.

**State:** `node --check script.js` passes. Added `test/create-list-item-branching.test.js` (hand-maintained mirror, matching the existing test file's convention — script.js has no module.exports to `require` against, see docs/ARCHITECTURE.md): task renders with sub-task section + task editor and no streak badge; habit renders (using an object that omits `subTasks` entirely — the exact shape that used to throw) with streak badge + habit editor and no sub-task section; unrecognized type warns without throwing. Full suite: 17/17 passing. **Live-verified in Jeremy's browser** both times (Cowork drove Chrome against his `node server.js`): part 1 — reproduced the exact stack twice from the live console, then after both fixes + hard reload confirmed modal closes, no duplication, habits render as agenda rows for the first time, zero exceptions; part 2 — created one task + one habit, confirmed only the task shows "+ SUB-TASK" and only the habit shows "Streak: 0", task pencil → "Edit Task", habit pencil → "Edit Habit" pre-filled from the definition, edited the habit name and saved → agenda row updated immediately with no reload, confirmed both `definedHabits` and the live instance show the new name in the persisted save. Test data cleared from localStorage after each round — game left on a fresh empty save. **NOT yet committed** — commands below for Jeremy's terminal.
**Next:** Pick one: (a) offline catch-up on load (last Milestone 1 item — paused zombies animate to current position, max 5s; also owns the deferred "how much offline overdue damage applies" decision); (b) the `showForm` duplicate/shadowing bug (real `showForm` ~line 323 vs legacy stub ~line 3571 that only handles 'routine' and silently wins — confirmed present again this session; the inline `#habitForm` UI is dead because of it, though the modal path is what's actually used); (c) `uncompleteItem` doesn't restore sub-task sizing/classes (low priority).
**Watch out:**
- **Cache-busting:** `node server.js` + Chrome cached `script.js` — a normal reload served the OLD file and a fix appeared not to work. Had to hard-reload (Ctrl+Shift+R) each time. If a fix ever "doesn't take" in the browser, hard-reload before doubting the code; you can also `fetch('/script.js').then(r=>r.text())` and grep it to confirm what's actually being served.
- **localStorage cleanup is fighting the unload-flush:** clearing `deadline.save` then reloading does NOT stick, because persistence flushes on `beforeunload`/`visibilitychange` and rewrites the live in-memory state right before the page unloads. To force a clean slate from the console: write a valid empty save (`schemaVersion:1`, empty arrays), monkeypatch `Storage.prototype.setItem` to ignore `deadline.save` writes, THEN reload — the fresh page loads empty and its native setItem resumes normally.
- **`showEditTaskModal`'s sub-task edit path** (script.js ~730, inside `createListItem`'s task branch) still points every sub-task's pencil at the task editor — correct today since sub-tasks are tasks, but re-check if sub-tasks ever gain their own type.
- **Frequency/timeOfDay edits don't retroactively apply** to an already-spawned habit instance (same category as the deferred offline-catch-up decision) — only name/category/isNegative sync live; a frequency/timeOfDay change takes effect for tomorrow's instance.
- **Commit hygiene:** per the standing note, commit at the end of EVERY session. This session's edits (across both parts) are uncommitted — see below.

## 2026-07-17 — localStorage persistence implemented (Cowork session)
**Did:** Milestone 1 — localStorage persistence. Decision made with Jeremy (on Fable): **schemaVersion 1 saves the CURRENT in-memory shapes as-is**, not DATA_SCHEMA.md's target schema; shapes reconcile during Milestone 2 extractions via schemaVersion bumps + migrations (full rationale in DECISIONS.md 2026-07-17).
- New `js/persistence.js` (global `Persistence`, module.exports for Node like config.js): `requestSave()` debounced 500ms, `flush()`, `load()` with strict ISO-datetime Date reviver, `clear()`, migration chain scaffold, DOM refs (`element`/`listItemElement`) stripped by the serializer. Wired into index.html load order after config.js.
- script.js: added `getPersistableState()`/`saveGame()`/`restoreGameState()` (after `updateTaskCountDisplay`, ~line 229). Restore runs once after `initGame()` on boot: scalars → definitions → completedItems → active items re-added via `addItemToGame()` (rebuilds DOM; saved order keeps parents before sub-tasks) → re-apply overdue classes (`markAsOverdue` early-returns when `isOverdue` already true) → rebuild parent list items (createListItem resolves sub-task rows from `activeItems`, which weren't fully populated on first pass) → `generateDailyHabitInstances()` → refresh all displays → `gameOver()` if the save was a dead run.
- Save triggers: explicit `saveGame()` at 13 sites (addItemToGame, completeItem, uncompleteItem, removeItem, markAsOverdue, damageBase, gameOver, createHabitDefinition, createRoutineDefinition, deleteRoutine, restart handler) + 5s autosave safety net in `updateGame()` (`CONFIG.PERSISTENCE_AUTOSAVE_MS`) covering un-hooked edit paths (routine editors, task edit modal) + `flush()` on `visibilitychange:hidden` and `beforeunload` (visibilitychange matters — mobile Chrome doesn't reliably fire beforeunload). Restart button now clears the save before `initGame()` so a reload can't resurrect a dead run.
- **Offline overdue damage deliberately NOT charged on load** — restored overdue items get `lastDamageTickTime = Date.now()`; otherwise `updateActiveItems` deals 1 damage per 50ms game tick until "caught up" and an overnight save would insta-kill the base. That behavior belongs to the offline catch-up task.
- Docs: DECISIONS.md entry, DATA_SCHEMA.md "IMPLEMENTED" note, ROADMAP item checked.
**State:** `node --check` passes on script.js, js/persistence.js, js/config.js. Sandbox round-trip smoke test passed (Dates revived incl. `lastCompletionDate`/`originalDueDate`, DOM refs stripped, date-lookalike strings/ids untouched, `undefined` parentId drops out of JSON but stays falsy, unknown schemaVersion ignored, flush/clear paths). **Live-verified by Jeremy** — playtested refresh/complete/overdue/restart flows, "works well for now." Committed and pushed to GitHub as `f657ca8` (along with the previously-uncommitted config.js/parse-fix/sub-task-rendering work from the prior session — both had been sitting locally uncommitted; caught via a GitHub API check when Jeremy asked to verify the push, and a stale `.git/index.lock` blocked the first commit attempt — resolved with `Remove-Item .git\index.lock -Force`).

While playtesting, Jeremy found a new bug (not persistence-related, pre-existing UI issue): **the "create habit" modal doesn't close after Save.** Repro: open create-habit form → fill in → Save → modal stays open → if clicked again (or Save clicked again), it duplicates the habit definition and its on-screen sprite. Downstream effect: opening "add habit to routine" then shows two entries for what should be one habit. Not investigated or fixed this session — logged for next session per below.
**Next:** Pick one: (a) **habit-modal-doesn't-close-on-save bug** (new, found in this session's playtest — repro above; likely in the habit form's Save handler around `showCreateHabitForm`/`attachModalEventListeners`/`createHabitDefinition`, script.js ~2338-3113 — not yet grepped/root-caused); (b) offline catch-up on load (last Milestone 1 item — zombies animate to current position, max 5s; also owns the "how much offline overdue damage applies" decision this session deferred); (c) the `showForm` duplicate/shadowing bug (~line 229 real vs ~3547 legacy stub — the stub still silently wins; untouched today); (d) `uncompleteItem` doesn't restore sub-task sizing/classes (still open, low priority).
**Watch out:**
- If a restored save ever looks wrong, `localStorage.removeItem('deadline.save')` in DevTools console = clean slate. Corrupt/unknown saves are ignored automatically (console.warn) rather than crashing.
- The 5s autosave in `updateGame()` is a stopgap — when Milestone 2 extracts UI/form code, hook `saveGame()` into the edit paths directly and consider dropping the interval.
- `deadline.settings` (DATA_SCHEMA.md) is still unimplemented — only `deadline.save` exists.
- Habit instances for a NEW day only spawn on boot (`restoreGameState`/`initGame` → `generateDailyHabitInstances`) or habit creation — if the tab stays open across midnight, that's pre-existing behavior, not a persistence regression.
- **Commit hygiene:** two sessions' worth of work went uncommitted before this one got caught — worth committing at the end of EVERY session from now on (not just when Jeremy asks to "check" something), to avoid this recurring.
- **New habit-modal bug also means persistence will now duplicate-save any habit created via the buggy double-click path** — not a persistence bug itself, but worth keeping in mind: if a save ever shows a duplicate habit definition, this is almost certainly why.

## 2026-07-17 — Created js/config.js; discovered & fixed script.js was unparseable (Cowork session)
**Did:** Milestone 1 task 3 — created `js/config.js` (a `CONFIG` object) and moved every named gameplay constant out of script.js's "Game Settings" block into it, plus three hardcoded magic numbers that were clearly balance values (ENEMY_WIDTH/HABIT_ENEMY_WIDTH, initial baseHealth, the habit streak-bonus point bump). script.js keeps local `const NAME = CONFIG.NAME` aliases so every existing call site works unchanged — no balance values changed, only relocated. Wired `js/config.js` into index.html's script load order (before IdCounter/TaskManager/script.js). Verified via a standalone `node -e "require('./js/config.js')"` that every value matches the originals exactly, and via grep that all `CONFIG.*` references resolve.

While verifying, `node --check script.js` failed — turned out script.js has never been parseable JS in its currently-committed form (confirmed via git history that all of this predates today). Found and fixed 4 distinct structural defects, then re-ran node --check after each fix until it passed clean:
1. Dead-code `const itemIdCounter = new IdCounter(); const taskManager = new TaskManager(...)` collided with the real `let itemIdCounter` used everywhere else — the TaskManager/IdCounter pairing was never actually used (0 references to `taskManager.` anywhere). Deleted the dead lines.
2. Dead-code `const { gameIsOver } = window;` collided with the real `let gameIsOver` — `window.gameIsOver` is never set anywhere. Deleted.
3. `function completeItem(itemId) {` header was missing entirely; body was present and called from 3 sites. Re-inserted.
4. `function uncompleteItem(itemId) {` header was missing; body present, called from 1 site. Re-inserted.
5. `function showCreateSubTaskModal(parentId) {` header was missing; body present, called from `createSubTaskPrompt`. Re-inserted.

Full detail + evidence for each in DECISIONS.md. Updated ROADMAP.md (checked off the config.js item, noted the script.js fixes).

Jeremy then ran `node server.js` and loaded the game in a browser — first real playtest since script.js could parse. It rendered (health/XP/level/points HUD, church base, zombie sprites, agenda list all worked), but he immediately reproduced sub-task duplication: a sub-task of his first task showed up both correctly nested under its parent AND as its own standalone top-level task. Root cause: `itemIdCounter` starts at 0, so the first-ever task gets `id: 0`; every sub-task/parent check in script.js is a truthy test (`if (item.parentId)`, `if (!item.parentId)`, 10+ call sites), and `0` is falsy — so a sub-task of task 0 fails every one of those checks and renders as if it had no parent. The `38409ca` guard clauses from the earlier "verified fixed" conclusion are real, but nobody had hit id 0 in a live browser before today (script.js couldn't parse; the Jest test is a self-contained mirror that doesn't hit this case). **Fix:** `itemIdCounter = 0` → `itemIdCounter = 1` in `initGame()` — smallest change that guarantees no id is ever falsy, without touching the ~10 truthy-check call sites (rewriting those to explicit `!= null` checks is a good Milestone 2 cleanup candidate, logged in DECISIONS.md, not done now). `node --check script.js` still passes clean after this fix.
**State:** `node --check script.js` and `node --check js/config.js` both pass clean. Game confirmed loading and rendering in a real browser (Jeremy's screenshot). Sub-task duplication fix applied but **not yet re-verified live** — Jeremy should reload and retest creating a sub-task off the very first task to confirm this specific fix, plus general playtesting since this is the first time the file has run at all. Jest wasn't re-run this session (sandbox npm install kept getting interrupted by a tool timeout; the existing `test/subtask-creation.test.js` doesn't exercise the code touched today either way — self-contained mirror, not a `require` of script.js).
Jeremy confirmed the id-0 fix worked, then found two more sub-task rendering issues on the same playtest: the sub-task sprite was cropped instead of scaled down, and he wants sub-tasks to render clustered next to their parent (same vertical level, offset left/right) rather than floating at a random height. Both fixed:
- **Cropping:** root cause was a CSS specificity/`!important` conflict — legacy `.enemy.category-<X>` rules (style.css ~line 311) force `background-size: 128px 128px !important` on every enemy including 64px sub-tasks and 70px habits, beating the correctly-sized-but-non-`!important` `zombie-subtask`/`zombie-small` rules. Added `!important` to the smaller rules too so their higher selector specificity wins. Fixes both sub-tasks and habits (same bug, habits just hadn't been reported yet).
- **Clustering:** added `getSubTaskClusterOffset()` and `getItemTopPosition()` helpers in script.js (next to `calculateTimelinePosition`). Sub-tasks now render at their parent's exact vertical position and get a horizontal stagger (alternating +45/-45/+90/-90... per sibling, ordered by creation) added to their normal due-date timeline x. Wired into all 4 places script.js computes item x and both places it sets top.

Updated docs/MECHANICS.md's "Sub-tasks" section to describe this and flag it doesn't match PROJECT_SPEC.md's original "parent grows/shrinks" idea — that's unresolved, logged as a Milestone 3 open question, not decided today.

Jeremy then refined the clustering ask: sub-tasks should bottom-align with the parent ("shoes lined up on the same level") rather than top-align, and the parent should always render above an overlapping sub-task. Fixed both:
- **Bottom alignment:** `getItemTopPosition` was returning the parent's `top` directly, which lines up the tops (heads), not the bottoms (feet), since sub-tasks are shorter (64px vs 128px). Now returns `parentTop + (parentHeight - itemHeight)`, reading the parent's actual rendered height so it still works if parent height ever changes dynamically (e.g. the still-unresolved shrinking-parent idea).
- **Stacking order:** sub-task DOM elements are added after their parent, so with no explicit z-index they were winning the default stacking order and could cover it. Added `z-index: 10` to the base `.enemy` rule and `z-index: 5` to `.zombie-subtask` in style.css — existing higher values (`.enemy:hover` = 20, `.enemy.high-priority` = 15) still take precedence over this baseline.

Jeremy playtested again and found the clustering itself was inconsistent — one sub-task rendered almost directly behind the parent, another far away. Root cause: the ±45/±90px offset was being added on top of each sub-task's OWN due-date timeline position, and that formula maps hours-until-due nonlinearly across the whole screen width, so even a small due-time difference from the parent could put the base position tens/hundreds of px away — swamping the small cluster offset. Jeremy's clarified intent: sub-tasks should track the PARENT's position by default and only show their own due-date position when it's due significantly earlier (meaningfully more urgent). **Fix:** added `calculateTimelineXWithClustering()` in script.js, replacing `calculateTimelinePosition(...) + getSubTaskClusterOffset(...)` at all 3 non-overdue call sites. It compares the sub-task's own timeline x to its parent's current x; only if the sub-task's own position is 150px+ closer to the base (`SUBTASK_AHEAD_THRESHOLD_PX`, a tunable starting guess) does it use its own position — otherwise it clusters at `parentTask.x + offset`. Overdue handling (both go to `BASE_WIDTH + offset`) was already correct and untouched.

Jeremy playtested yet again — "still not right, almost the exact same as before." Found a 4th overdue-positioning site that was missed: `addItemToGame()` (the function that runs at actual creation time) has its own separate "already overdue at creation" check that still set `x = BASE_WIDTH` with no offset at all — distinct from the two overdue checks already fixed (`uncompleteItem`, `updateActiveItems`). Since the test tasks were due "5:00 PM" today and testing happened after 5pm, they were overdue *at the moment of creation*, so this exact unfixed branch fired every time the clustering fix was tested — making it look like nothing had changed. **Fix:** same one-line pattern, `itemData.x = BASE_WIDTH + getSubTaskClusterOffset(itemData);` at script.js ~line 467. Verified via `grep '\.x = BASE_WIDTH'` that all 3 sites in the file now consistently include the offset — this was the last one.

Jeremy tested with 3 sub-tasks (`test2`/`test3` due same time as parent, `test4far` due later) — still saw the 1st sub-task directly behind the parent and the 2nd "too far ahead." This time it was the offset MAGNITUDE, not the branching logic: the flat ±45px/±90px guess never accounted for actual sprite sizes — a 45px offset on a 128px-wide parent still overlaps it almost entirely (45+64=109 < 128), so the "1st sub-task" could never look separated. **Fix:** rewrote `getSubTaskClusterOffset` to compute real, non-overlapping spans from actual widths — right side starts at `ENEMY_WIDTH + gap` (clears parent's right edge), left side starts at `-(SUBTASK_ENEMY_WIDTH + gap)` (clears parent's left edge), each further same-side sibling adds another `SUBTASK_ENEMY_WIDTH + gap`. With current sizes (128/64/8px gap) that's +136, -72, +208, -144, +280... — verified by hand and a quick Node script that none of these spans overlap the parent or an earlier same-side sibling. Added `SUBTASK_ENEMY_WIDTH` (64), `SUBTASK_CLUSTER_GAP_PX` (8), and moved `SUBTASK_AHEAD_THRESHOLD_PX` (150) into `js/config.js` as proper named constants (previously a local script.js const); `addItemToGame`'s sub-task sizing now also reads `CONFIG.SUBTASK_ENEMY_WIDTH` instead of a separate hardcoded `64`, so the render size and the offset math can't drift apart.

Jeremy then spotted the final layer himself: the zombie PNGs have big transparent margins (visible art ≈ middle of the box), so box-edge offsets still looked wrong. Measured all 16 PNGs' alpha-channel bounding boxes (Pillow/numpy): visible art is 44%–92% of box width depending on category, not centered. Added `CONFIG.ZOMBIE_VISIBLE_MARGINS` (per-category measured margins) + `getVisibleEdges()` helper; `getSubTaskClusterOffset` now chains each sibling's VISIBLE edge 8px (`SUBTASK_CLUSTER_GAP_PX`) from the previous member's visible edge, handling mixed categories. Simulation-verified: exact 8px visual gaps, zero overlap, in Jeremy's test case and a worst-case category mix. If art is redrawn, re-measure margins (method in DECISIONS.md).

Jeremy confirmed live: "that works great for now." Sub-task rendering thread is closed.

**Next:** Pick one: (a) continue Milestone 1 — localStorage persistence (docs/DATA_SCHEMA.md) or offline catch-up; (b) the `showForm` duplicate/shadowing bug (script.js ~line 229 vs ~3368 — a later "Legacy" stub silently overrides the real form-switching logic, so `showForm('task')`/`showForm('habit')` currently no-op — found today, not yet fixed); or (c) `uncompleteItem()` doesn't restore sub-task sizing/classes (a completed-then-restored sub-task comes back full-size) — found today, not fixed, lower priority.
**Watch out:**
- **This is the first time anyone has run `node --check` against the full script.js, and the first real browser playtest.** Treat "the monolith runs" claims in older HANDOFF entries as unverified history, not current fact, until more of the game has been clicked through.
- **Falsy-id pattern:** script.js checks `parentId` (and possibly other id-like fields) with plain truthy tests in ~10 places. The `itemIdCounter` start-at-1 fix patches the immediate symptom but the underlying fragility remains — worth a real cleanup pass (`!= null` everywhere) during Milestone 2 extraction rather than leaving it as a landmine.
- The `.git/index.lock` issue recurred again this session (a previous session's stale lock) — Jeremy deleted it manually before this session started. If it recurs: never run `git` from the Cowork sandbox at all against this repo (not even read-only) — read `.git/HEAD`/`.git/refs/**` as plain text instead.
- Sandbox `npm install` for jest/babel-jest kept getting interrupted (tool call timeout at 45s, then background/nohup processes don't survive across separate bash calls in this sandbox — no process persistence between tool calls). If a future session needs to run the real Jest suite from Cowork, expect to retry the install 2-3 times or accept partial/flaky results; verifying via `node --check` + targeted `node -e` smoke tests was more reliable today.
- `showForm` duplicate (see Next) is a real functional bug sitting in committed code, low urgency but worth fixing before relying on task/habit form switching in a demo.

## 2026-07-17 — Verified sub-task bug fix + merged branches + moved off OneDrive (Cowork session)
**Did:** Two Milestone 1 tasks this session.

Task 1 — sub-task duplication bug: Read SUBTASK_BUG_REPRODUCTION_REPORT.md and grepped script.js for `addItemToGame`, `showCreateSubTaskModal`, `sortAndRenderActiveList`, `createListItem`. Found the guard clauses described in the report's "Next Steps for Fix" already present in current script.js. `git show 38409ca -- script.js` confirmed the fix landed in commit `38409ca` ("Fix: prevent duplicate regular task when adding subtask", Jul 29 2025) — the SAME commit that added the repro report, so the report's "Next Steps" section is stale. Ran `test/subtask-creation.test.js` in a sandbox scratchpad (copied package.json/jest.config.js/js/test, added a missing `test/setup.js` stub + babel config) — 14/14 tests pass. No code changed.

Task 2 — branch merge: `main` was an exact ancestor of `feature/sprite-system-cleanup` (10 commits ahead, 0 behind) — a clean fast-forward. Actually getting the merge pushed took most of the session: hit a recurring stale `.git/index.lock` on the OneDrive-synced folder (root cause: any git operation, including Cowork's own read-only checks, can leave a lock behind that OneDrive's/the bridge's process can't clean up); then a GitHub push auth loop (Windows Git Credential Manager had no `credential.helper` set, so every push re-triggered a fresh browser login instead of caching it — fixed with `git config --global credential.helper manager`); then discovered the GitHub remote `schooley02/Deadline` had been deleted at some point since July 2025 (404, 0 repos on the account) — recreated it fresh and re-pushed both branches with `git push -u`. Finally moved the whole project out of OneDrive to `C:\Users\jscho\Projects\Deadline` to eliminate the lock issue permanently, at which point `git checkout main && git merge feature/sprite-system-cleanup && git push origin main` completed cleanly (Fast-forward, `3042c4b..7af6ccb`). Verified via `git branch -vv`: both `main` and `feature/sprite-system-cleanup` at `7af6ccb`, both tracking origin, both clean.

Updated CLAUDE.md (new project path, dropped stale OneDrive guardrails, added the "never run git via the device bridge" rule), docs/ROADMAP.md, docs/DECISIONS.md.
**State:** `main` is now the current branch and has everything (doc/memory system, sprite/mobile-CSS work, verified sub-task fix). Both `main` and `feature/sprite-system-cleanup` pushed to GitHub. Repo lives at `C:\Users\jscho\Projects\Deadline` now, not OneDrive. Not launched/played this session.
**Next:** Milestone 1 task 3 — create `js/config.js` and migrate gameplay constants out of script.js.
**Watch out:**
- **Project moved.** Any future Cowork session needs to request folder access to `C:\Users\jscho\Projects\Deadline`, NOT the old OneDrive path.
- **Never run `git` via the Cowork device bridge against this repo**, even read-only commands — it can leave a stale `.git/index.lock` that blocks Jeremy's own terminal. Read `.git/HEAD` and `.git/refs/**` directly as plain text instead when you need to check branch/commit state from Cowork.
- `package.json` in this repo is actually the visual-regression-test manifest (puppeteer/pixelmatch/pngjs) — it does NOT list `jest`/`babel-jest` as devDependencies even though `jest.config.js` and `test/*.test.js` exist and expect them. Also missing: `test/setup.js` (referenced by `jest.config.js`'s `setupFilesAfterEnv`) and a babel config. Worth a small follow-up task to fix this properly (not just sandbox-scratchpad stubs).
- `test/subtask-creation.test.js` doesn't `require` script.js — it contains its own copy-pasted mirror of `createTaskItemData`/`addItemToGame`. Currently matches script.js, but can silently drift. Consider refactoring so the test imports the real functions once Milestone 2 extracts them into `js/` modules.
- Puppeteer's postinstall tries to download Chrome and fails in the sandbox (403) — installing with `PUPPETEER_SKIP_DOWNLOAD=true` avoids it.
- `feature/sprite-system-cleanup` still exists on GitHub, now identical to `main` — fine to delete whenever, not urgent.

## 2026-07-16 — Repo moved to permanent home + memory system merged (Cowork session)
**Did:** Audited both old folders (May MPE in OneDrive "Baseline - Old", July 2025 Claude Code repo in C:\Users\jscho\Deadline). Copied the July repo here (Claude\Projects\Deadline) — git history verified intact (fsck clean, checksums match). Merged in the docs system, CLAUDE.md, commands, agents, balance-tuning skill — all updated to reflect THIS codebase. Recovered the "lost" July specs from PROJECT_SPEC.md (exponential pricing = base × 1.5^qty; frozen routine recovery rules) into docs/ECONOMY.md and docs/ROUTINES.md. Added .gitignore, untracked node_modules.
**State:** Monolith runs (`node server.js`). node_modules NOT copied — run `npm install` before `npm test`. Branch: feature/sprite-system-cleanup (synced with origin at last July push). Known bug: sub-task duplication (see SUBTASK_BUG_REPRODUCTION_REPORT.md — root cause already identified at script.js ~line 1005-1011: addItemToGame adds subtask to activeItems AND the parent list refresh re-creates it).
**Next:** STEP 0 (one-time, on Jeremy's machine — the Cowork sandbox couldn't write the git index through OneDrive): `git rm -r --cached node_modules` then `git add .gitignore CLAUDE.md docs sessions .claude` then `git commit -m "M0: add memory system; untrack node_modules"`. THEN Milestone 1, task 1: fix the sub-task duplication bug. Read SUBTASK_BUG_REPRODUCTION_REPORT.md, grep script.js for `addItemToGame` and `createSubTaskPrompt`, fix, `npm test`, commit.
**Watch out:**
- NEVER read script.js (3,420 lines) or PROJECT_SPEC.md (2,700 lines) in full — Grep for what you need.
- This folder is OneDrive-synced. If git shows lock errors or phantom modified files, pause OneDrive sync. Recommend right-click folder → "Always keep on this device".
- `git status` may show many files modified due to the 2026-07-16 move (line endings/mode bits) — diff before assuming real changes; consider `git config core.fileMode false`.
- Old copies of this project exist at C:\Users\jscho\Deadline (original — Jeremy to delete after verifying this one) and OneDrive\...\Baseline - Old\Deadline (May-era design assets — KEEP, art sources live there).
