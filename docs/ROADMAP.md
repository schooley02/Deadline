# Roadmap

Work ONE unchecked task per session. Check items off with the date. Ticket IDs reference ACTIONABLE_TICKETS.md.

## Milestone 0 — Restructure & Memory System ✅ (2026-07-16)
- [x] Audit May MPE + July 2025 codebase
- [x] Docs system, CLAUDE.md, commands, agents, balance-tuning skill merged into this repo
- [x] Repo moved to permanent home (Claude\Projects\Deadline); node_modules untracked, .gitignore added

## Milestone 1 — Stabilize ✅ (2026-07-17)
- [x] Fix sub-task duplication bug (2026-07-17, revised) — the `38409ca` guard clauses (`if (!item.parentId) createListItem(...)` etc.) were real but had an untested edge case: task IDs started at 0, and `0` is falsy, so a sub-task of the very first-ever task (id 0) tripped every `if (item.parentId)`/`if (!item.parentId)` check in the file and got rendered as both a nested sub-task AND a standalone top-level task. Only surfaced once script.js could actually parse and Jeremy playtested live (see below) — the old test suite is a self-contained mirror that never exercised id 0. Fixed by starting `itemIdCounter` at 1 instead of 0 (script.js `initGame()`). See DECISIONS.md.
- [x] Decide branch state with Jeremy (2026-07-17) — fast-forward merged `feature/sprite-system-cleanup` → `main` (10 commits, no conflicts), pushed both to GitHub. Also: project home moved from OneDrive to `C:\Users\jscho\Projects\Deadline`; GitHub repo had to be recreated (see DECISIONS.md).
- [x] Create `js/config.js` and move existing gameplay constants from script.js into it (2026-07-17) — moved GAME_TICK_MS, DAY_DURATION_MS, DAMAGE_INTERVAL_MS, OVERDUE_DAMAGE, MAX_BASE_HEALTH, XP_PER_TASK_DEFEAT, XP_PER_HABIT_COMPLETE, POINTS_PER_TASK, POINTS_PER_HABIT, HABIT_STREAK_BONUS_THRESHOLD/POINTS, LEVEL_XP_THRESHOLDS, ROUTINE_SLOTS_PER_LEVEL, ENEMY_WIDTH, HABIT_ENEMY_WIDTH into `CONFIG` object; script.js now reads them from `CONFIG.*` (values unchanged). Wired `js/config.js` into index.html load order (before IdCounter/TaskManager/script.js). Also fixed 4 pre-existing structural defects in script.js that made it fail to parse at all (duplicate itemIdCounter/gameIsOver declarations; 3 missing function-declaration headers — completeItem, uncompleteItem, showCreateSubTaskModal). `node --check script.js` now passes clean. See DECISIONS.md for full detail, including one more bug found but NOT fixed (showForm duplicate/shadowing — functional, not a parse error).
- [x] localStorage persistence (2026-07-17) — `js/persistence.js` (debounced save, flush on hide/close, strict ISO Date reviver, migration scaffold) + `restoreGameState()` in script.js; 13 explicit save hooks + 5s autosave safety net (`CONFIG.PERSISTENCE_AUTOSAVE_MS`). **schemaVersion 1 = current in-memory shapes**, not DATA_SCHEMA.md's target objects — reconcile via migrations during Milestone 2 (see DECISIONS.md 2026-07-17). Restored overdue items don't back-charge offline damage (deferred to offline catch-up task). **Live-verified by Jeremy** — playtested successfully. Committed + pushed (`f657ca8`).
- [x] **Habit-modal-doesn't-close bug (2026-07-17)** — root cause was two habit-unsafe references in `createListItem` (not the modal handler): `subTasks.forEach` on undefined (line 687) + phantom `itemNameContainer` (line 791). Both threw for habits, aborting `createHabitDefinition` before `closeModal()`; the pre-drawn sprite made it look like creation succeeded, so re-clicks duplicated. Fixed (`(itemData.subTasks || [])`; streak badge → `itemDetailsContainer`). Live-verified in Jeremy's browser via Cowork's Chrome control: modal now closes, no duplication, habits render as agenda rows for the first time, zero console exceptions. See DECISIONS.md.
- [x] **`createListItem` hardened into task/habit branches (2026-07-17, same-day follow-up)** — addressed the "task-first, habit-unsafe" pattern the bug above exposed. Shared shell (sprite/name/checkbox/due-date/category) + explicit `task`/`habit`/default branches; default branch warns instead of throwing for any future item type. Also found and fixed a third habit hazard: the edit pencil was hardcoded to the task editor for every row (harmless only because habit rows never used to render); habits now get a real `showEditHabitInstanceModal` → the existing habit-definition editor, and saving syncs the live instance so the agenda row updates immediately. Confirmed routines never reach `createListItem` (no routine branch needed). Regression test added (`test/create-list-item-branching.test.js`, mirror-style like the existing suite). Live-verified: task/habit rows render correctly distinct, both edit pencils open the right editor, habit edits sync live. See DECISIONS.md.
- [x] Offline catch-up on load (2026-07-17) — zombies animate (≤5s, ease-out) from saved to current-time positions on restore; offline overdue damage back-charged per item, capped at 12 HP (`CONFIG.OFFLINE_DAMAGE_CAP_PER_ITEM`) for the item's LIFETIME (not per restore/day — revised same-day after a design check). Companion fix: editing an overdue task's due date into the future now correctly clears its overdue state (previously kept ticking damage regardless of the new date). See DECISIONS.md. **Milestone 1 complete.**

## Milestone 2 — Modularize script.js + style.css ✅ (2026-07-18)
Order chosen so each step is small and testable. Tests pass before/after each; commit each.
- [x] Extract clock/time + timeline positioning (2026-07-17) — `calculateTimelinePosition`/`updateMidnightLine` moved to `js/clock.js` (dims passed explicitly, not closure); script.js keeps thin wrappers so call sites are unchanged. Offline catch-up deliberately NOT moved (deferred to the damage/base-health extraction below). See DECISIONS.md.
- [x] Extract enemy spawning + movement (2026-07-17) — `js/movement.js` (getSubTaskClusterOffset, calculateTimelineXWithClustering, getItemTopPosition + internal getVisibleEdges; explicit ctx/dims instead of closures, CONFIG+Clock as globals) and `js/spawning.js` (addItemToGame + pure resolveEnemyVisual, DOM collaborators injected via deps). script.js keeps thin wrappers so all call sites are unchanged. Both wired into index.html after clock.js. Tests: `test/movement.test.js` + `test/spawning.test.js`. Behavior-identical extraction.
- [x] Extract damage/base-health/game-over (2026-07-18) — `js/damage.js`: `damageBase`, `gameOver`, `updateBaseVisuals`, `computeDaysSurvived`, `runOfflineCatchUp`/`applyOfflineDamage`/`computeOfflineOverdueDamage` (the code deferred from clock.js) and `runLiveGapCatchUp`. Pure cores split out for DOM-free testing (`resolveBaseImage`, `computeGapCatchUpHits`, `computeCatchUpDuration`). First module to WRITE script.js-owned state (`baseHealth`, `gameIsOver`) — accessor deps rather than moving ownership; `markAsOverdue` stayed behind (habit-streak coupling). script.js keeps thin wrappers so all call sites are unchanged. `test/damage.test.js` (53 cases). See DECISIONS.md.
- [x] **Bugfix (2026-07-18, not a Milestone item): three overdue-damage bugs from Jeremy's overnight test** — one root cause (a far-past `lastDamageTickTime` makes the live loop pay one damage interval per 50ms tick; the offline cap only guarded the reload path). Fixed: suspended-loop gaps now route through the capped path (`runLiveGapCatchUp`, `CONFIG.LIVE_GAP_THRESHOLD_MS`); items created already-overdue start their clock at spawn; and "days survived" now derives from real elapsed time (`runStartedAtMs` + `CONFIG.MS_PER_REAL_DAY`) instead of the removed 60s-per-day `DAY_DURATION_MS` timer. Also added a dev-only bottom-left reset button. See DECISIONS.md.
- [x] **Bugfix (2026-07-18, not a Milestone item): overdue rows lost their red highlight on rebuild** — found by Jeremy playtesting extraction #3 (sprite showed the red box, agenda row didn't). `createListItem` never derived styling from `isOverdue`; the class came only from `markAsOverdue`, which early-returns when the item is already overdue, so any rebuild of an already-overdue row (edit due date, add sub-task, restore a sub-tasked parent) dropped it. Now derived in `createListItem`. Reproduced and re-verified live in Chrome; 6 regression tests added. See DECISIONS.md.
- [x] Extract progression (XP, levels, slots) (2026-07-18) — `js/progression.js`: pure `Progression.checkLevelUp(state, thresholds, slotsPerLevel, maxLevel)` walks every threshold crossed in one completion (replaces the old recursive `checkPlayerLevelUp` self-call). script.js keeps a thin wrapper applying the result + DOM side effects (`updatePlayerDisplays`, `showLevelUpMessage`, `updateRoutineDisplay`). `playerPoints` deliberately left in script.js — it's economy.js's future scope, and the shop (Milestone 3) isn't built yet. `test/progression.test.js` (9 cases). See DECISIONS.md.
- [x] **Bugfix (2026-07-18, not a Milestone item): routine tasks never spawned** — `createNewTaskInRoutine` stored a definition in `definedTasks` and nothing ever instantiated it (no task equivalent of `generateDailyHabitInstances` existed), so a task added via a routine appeared only in the routine window. Routine tasks now recur DAILY like routine habits (`getRoutineTaskInstanceDueTime` / `createRoutineTaskInstanceData` / `generateDailyRoutineTaskInstances`). Also: `definedTasks` is now persisted (it was omitted while `routine.taskDefinitionIds` was saved, leaving dangling ids on refresh), and the two routine-creation functions gained missing `saveGame()` calls. `test/routine-task-instances.test.js` (14 cases). See DECISIONS.md.
- [x] **Make routine `isActive` actually gate spawning** (found 2026-07-18, fixed 2026-07-18) — both daily generators now only spawn definitions belonging to a currently-active routine (orphaned/removed-from-routine definitions are inert the same way). Deactivating a routine also recalls its active habit/task instances from the board immediately (Jeremy's call — see DECISIONS.md), cascading any sub-tasks; recall is a pure removal (no XP/points/streak/damage effect). `toggleRoutineActive` now calls `saveGame()`. `generateDailyHabitInstances` still only handles `frequency === 'daily'` — left alone, still safe since the form offers no other option.
- [~] **Day-of-week / monthly scheduling for habits + routine tasks** (designed 2026-07-18; **step (a) DONE 2026-07-18 session 14**) — replaces bare `frequency` string with `schedule: { frequency: 'daily'|'weekly'|'monthly', daysOfWeek, dayOfMonth }`. Split into three sub-steps; the 2→3 migration also seeds habit `occurrenceHistory` (one bump for this + the rate bonus).
  - [x] (a) schema migration + generators (2026-07-18 session 14) — `js/schedule.js` (pure recurrence rules, monthly clamping); persistence `SCHEMA_VERSION=3` with v2→v3 migration (habits `frequency`→`schedule` + `occurrenceHistory:[]`; routine tasks gain a default daily `schedule`); `createHabitDefinition`/`createNewHabitInRoutine`/`createNewTaskInRoutine`/`editHabitInRoutine` write `schedule`; `selectHabitDefsToSpawn`/`selectTaskDefsToSpawn` gate on `Schedule.isScheduledForDay`. `test/schedule.test.js` + expanded migration/gating tests (17 suites, 329/329). Live-verified: real v2 save migrated to v3, daily habits still spawn, console clean. See DECISIONS.md.
  - [x] (b) scheduling UI (2026-07-18, session 15) — day-of-week checkbox row (shared across Daily/Weekly — Daily auto-checks all 7, editable; switching to Weekly clears the boxes ONLY if all 7 were still checked) + day-of-month field (Monthly) added to all 5 recurring-definition forms: standalone habit create (forms.js), routine habit create/edit, routine task create/edit (routineViews.js). `createHabitDefinition`, `createNewHabitInRoutine`, `createNewTaskInRoutine`, `editHabitInRoutine`, `editTaskInRoutine` (new schedule support — previously had none) all read/write real `schedule` objects now, replacing the daily-only stub. "At least one day" validated client-side for daily/weekly. `test/routines.test.js` +9 cases (17 suites, 338/338). Live-verified in Chrome: created a weekly habit (Mon/Wed/Fri) that correctly didn't spawn on Saturday; created a monthly routine task (day 18) that correctly spawned same-day; edited a migrated habit's schedule daily→monthly and confirmed persistence + reload. See DECISIONS.md.
  - [x] (c) rate-based bonus (2026-07-18, session 16) — `js/habits.js` gained the pure rate helpers (`recordOccurrence` upsert-by-date, `removeOccurrence`, `successRate`, `pointsMultiplier`, `occurrenceSuccess` polarity seam, `toOccurrenceDate`) and rewrote `applyHabitCompletion`/`applyHabitUncompletion` + new `applyHabitOverdue` to record occurrences and multiply points by the rolling-rate factor. `items.js` completion paths (`completeItem`/`uncompleteItem`/`markAsOverdue`) record occurrences; `config.js` gained `HABIT_RATE_WINDOW/MIN_SAMPLE/TIERS` and dropped `HABIT_STREAK_BONUS_POINTS` (threshold kept, now visual-only). Polarity-ready per Jeremy's call: `occurrenceSuccess` is the single seam P1-DATA-005 extends. `test/habits.test.js` +17 cases (17 suites, 355/355). Live-verified: completing a habit recorded a success occurrence + awarded 1× (below min sample), uncompleting refunded symmetrically and popped the entry. See DECISIONS.md.
- [x] Extract habits + streaks (2026-07-18) — `js/habits.js`: pure `getHabitInstanceDueTime`, `selectHabitDefsToSpawn` (isActive-gated spawn selection), `applyHabitCompletion`/`applyHabitUncompletion` (streak + points math), `resetStreakOnOverdue`, plus `createHabitInstanceData`/`generateDailyHabitInstances` (id/position/DOM-admission collaborators injected via deps, matching the damage.js/spawning.js pattern). script.js keeps thin wrappers at all five original call sites (`createHabitDefinition`, `completeItem`, `uncompleteItem`, `markAsOverdue`, `generateDailyHabitInstances`) — behavior-identical. `test/habits.test.js` (27 cases); `test/routine-active-gating.test.js`'s habit-selection mirror now requires the real module instead of duplicating it. See DECISIONS.md for two divergences found and deliberately deferred (streak-bonus asymmetry bug; streak-vs-rate bonus model design question).
- [x] **Bugfix (2026-07-18, not a Milestone item): standalone habits never spawned** — a habit created outside a routine (via the FAB) produced no sprite and no agenda row. `Habits.selectHabitDefsToSpawn` required every habit to belong to an active routine — a rule aimed at orphaned definitions in the isActive-gating session, which also swept up standalone habits. Habit definitions now carry `routineId` (null = standalone) per `docs/DATA_SCHEMA.md`'s Habit shape; gating applies only to habits that actually belong to a routine. schemaVersion bumped 1→2 with a migration inferring `routineId` from existing routine membership. Removing a habit from a routine now releases it to standalone (Jeremy's call). Verified live in Chrome against Jeremy's real save. See DECISIONS.md.
- [x] **Bugfix: streak-bonus refund asymmetry** (found 2026-07-18; **RESOLVED 2026-07-18 session 16**) — dissolved structurally by the rate-based bonus: the flat streak bonus is gone entirely, and `applyHabitUncompletion` now computes its refund from the same history state `applyHabitCompletion` awarded from (recompute-then-pop), so refunds mirror awards by construction. Live-verified a complete→uncomplete round-trip nets exactly 0. No standalone fix was needed.
- [x] **Design: habit streak bonus should be rate-based, not streak-linear** (raised 2026-07-18; **DECIDED 2026-07-18, Fable design session** — see DECISIONS.md) — streak becomes visual-only (badge + on-fire); points bonus = capped multiplier off rolling success rate over last 14 scheduled occurrences (≥90% → 1.5×, ≥70% → 1.25×, else 1×; 1× until ≥7 recorded; points only, never XP; uncompletion flips today's entry and recomputes). Ships with the Schedule 2→3 migration (one bump). Multiplier curve gets re-tuned against real shop pricing once Milestone 3's shop exists. Implementation NOT yet done — see the scheduling item above for sequence.
- [x] Extract routines (2026-07-18) — `js/routines.js`: routine-task spawn selection (mirrors habits.js's pattern), definition CRUD, and activation/deactivation with deactivation recall. DOM-rendering functions (renderDefinedRoutines, populateRoutinesWindow, etc.) deliberately left in script.js for the future UI extraction — see ARCHITECTURE.md. `test/routines.test.js` (33 cases); `test/routine-active-gating.test.js` and `test/routine-task-instances.test.js` now require the real module instead of keeping hand-maintained mirrors. See DECISIONS.md.
- [ ] **Extract UI** — planned 2026-07-18, sequenced into 11 sessions in `docs/UI_EXTRACTION_PLAN.md` (the original single line covered ~2,500 lines across 7 clusters — too big for one session). Work them in order; the plan doc has the function map, line ranges, and per-session hazards.
  - [x] 0. Delete the dead legacy inline form system (2026-07-18) — see DECISIONS.md and HANDOFF.md for full detail; not yet live-playtested
  - [x] 1. `js/ui/modal.js` (2026-07-18) — foundation: `closeModal` moved out of routine code (was at ~2602) + `exposeGlobal` window.* helper. Overlay CREATION stayed out of scope — no shared "open modal" function existed to extract; each cluster still inlines its own modal-overlay HTML. Not yet live-playtested.
  - [x] 2. `js/ui/hud.js` (2026-07-18) — updatePlayerDisplays/updateTaskCountDisplay/showLevelUpMessage/showDebugInfo, all via explicit deps objects. showDebugInfo confirmed unreferenced anywhere in script.js (dead code, extracted as-is, not removed).
  - [x] 3. `js/ui/fabMenu.js` + `js/ui/managementWindows.js` (2026-07-18)
  - [x] 4. `js/ui/forms.js` (2026-07-18) — showFormModal/createTaskFormHtml/createHabitFormHtml/createRoutineFormHtml/attachModalEventListeners. Reconciled the `createRoutineFormHtml` inline duplicate to call `Routines.createRoutineDefinition` — adds a previously-missing `saveGame()` call (real behavior change, see DECISIONS.md).
  - [x] 5. `js/ui/popups.js` (2026-07-18) — enemy click, task details, edit task, sub-task modal. showCreateSubTaskModal's debug logging deliberately left untouched given the historic sub-task-duplication bug.
  - [x] 6. `js/ui/agendaList.js` part 1 (2026-07-18) — `createListItem` (267 lines). `gameIsOver` is passed as a GETTER (`isGameOver: () => gameIsOver`), not a captured boolean — first UI extraction whose handlers outlive the call, so a snapshot would go stale. See DECISIONS.md. New `test/agenda-list.test.js` (16 tests) requires and executes the REAL module — the first UI extraction with genuine unit coverage rather than a hand-maintained mirror.
  - [x] 7. `js/ui/agendaList.js` part 2 (2026-07-18) — `sortAndRenderActiveList`, `resetAllSubTaskCheckboxes`, `renderCompletedItems`, `showEditHabitInstanceModal`. `agendaList.js` now complete. `completedItems`/`definedHabits` cross into the module as getters (both reassigned elsewhere in script.js — new-game reset, restoreGameState). Retired `test/create-list-item-branching.test.js` (Jeremy's call — fully superseded by `test/agenda-list.test.js` against the real module). Live-verified in Chrome. See DECISIONS.md.
  - [x] 8. `js/ui/routineViews.js` part 1 (2026-07-18) — `renderDefinedRoutines`, `showRoutineManagement`, `populateRoutineHabits`, `populateRoutineTasks`, `updateRoutineDisplay`. `definedRoutines`/`definedHabits` cross in as getters; `definedTasks` needed none (always the bare `window.definedTasks` global). Found `renderDefinedRoutines` is dead code (targets a DOM id absent from index.html) — preserved as-is, not fixed. Plan's mention of `populateRoutinesWindow` in this session was stale (already done in session 3). Live-verified in Chrome. See DECISIONS.md.
  - [x] 9. `js/ui/routineViews.js` part 2 (2026-07-18) — showCreateHabitForm/showCreateTaskForm/showEditHabitForm/showEditTaskForm (no-deps HTML builders), showAddItemToRoutineModal, attachRoutineManagementListeners, populateHabitSelectDropdown, saveNewHabit/saveNewTask/saveEditedHabit/saveEditedTask. `js/ui/routineViews.js` now COMPLETE (979 lines); script.js down to **1,608 lines**. The four save* handlers stay `window.*` in script.js (inline onclick strings need the window-level name) while their implementation moved into the module. Fresh Grep confirmed the plan's function inventory was accurate this time (no stale lines, unlike sessions 3/8). See DECISIONS.md.
- [ ] **script.js → boot/wiring only (<300 lines)** — RESCOPED 2026-07-18 (session 10 start). The UI plan's single "session 10" line assumed only DOM/wiring glue was left, but a fresh Grep found ~700-800 lines of never-extracted core game logic (item/task/habit completion lifecycle, game init/persistence orchestration) that the 11-session UI plan (clusters A-G) never covered — that's `state.js`/item-lifecycle scope from `ARCHITECTURE.md`'s target layout, not UI. Jeremy's call: extract it now rather than defer, split across 3 sessions to keep the one-system-per-session rule:
  - [x] 10. `js/items.js` (2026-07-18) — item/task/habit completion lifecycle: `completeItem`, `removeItem`, `uncompleteItem`, `markAsOverdue`, `recomputeOverdueStateAfterEdit`, `createTaskItemData`, all via a single `itemsDeps()`. `playerXP`/`playerPoints` get get/set accessor pairs (second module after damage.js to write script.js-owned numeric state). script.js down to **1,346 lines**. Live-verified in Chrome: habit complete/uncomplete round-tripped XP/points exactly; editing an overdue task's due date into the future correctly cleared its overdue state; new task creation spawned correctly end-to-end. Flagged, not fixed: `uncompleteItem` hand-duplicates the enemy-sprite DOM construction `spawning.js` already does correctly. See DECISIONS.md.
  - [x] 11. `js/state.js` (2026-07-18) — game lifecycle/persistence orchestration: `initGame`, `restoreGameState`, `getPersistableState`/`saveGame`, `buildDamageDeps` (was `damageDeps`), all via a single `stateDeps()`. script.js down to **1,244 lines**. Ownership of the state variables deliberately stayed in script.js (deps pattern, not the "state.js owns state" ARCHITECTURE.md target) — logged as a deferred, low-risk future migration in DECISIONS.md. Live-verified in Chrome: fresh load restored the exact prior session's stats; completed a habit, reloaded, confirmed the completion (and all stats/positions) persisted correctly through the full save→reload→restore cycle.
  - [x] 12. Final wiring cleanup (2026-07-18) — `js/loop.js` extracted (updateGame/updateActiveItems, the last real game logic; `test/loop.test.js`, 13 cases — suite now 16/302). Deleted the stale 53-line subtask call-chain comment, dead `getTodayAt5PM`, FAB debug logging, 3 unused CONFIG aliases. script.js down to **1,141 lines**. **`<300` goal REVISED, not met:** what remains is ~90 thin wrappers + deps builders (kept by design — "call sites unchanged") plus DOM consts/event wiring, which IS the boot/wiring target; sub-300 requires the deferred state-ownership migration (wrappers retired), its own future session. `docs/UI_EXTRACTION_PLAN.md` formally CLOSED. Live-verified in Chrome. See DECISIONS.md.
- [x] Split style.css by component (2026-07-18, session 18) — 12 files under `css/`, mapped to the `js/ui/*.js` cluster boundaries where natural; `responsive.css` linked last (order-sensitive, see ARCHITECTURE.md/DECISIONS.md). Old root `style.css` deleted. **Milestone 2 fully CLOSED.**

## Known bugs (found, not yet scheduled)
- [x] **Edit Task / Create Sub-task modals pre-fill due date+time in UTC, not local time** (found 2026-07-18, session 6 live verification; **fixed 2026-07-18**). `js/ui/popups.js` used `dueDateTime.toISOString()` to populate the date/time inputs while the save handler parses `${date}T${time}` as LOCAL — so an untouched Save shifted the due time forward by the UTC offset (and rolled the DATE for evening tasks). Fixed with local-getter formatters `formatDateInputValue`/`formatTimeInputValue` in popups.js (exposed for tests); companion one-liner in `js/ui/forms.js` `createTaskFormHtml` (same family — UTC default date, latent in CDT, wrong in positive-offset TZs). First test coverage of modal pre-fill: `test/popups-prefill.test.js` (12 tests incl. round-trip invariant + negative control). NOT fixed: the same stale pattern in `js/TaskManager.js` ~317 (parallel old extraction, not the live sub-task path) and the UTC date comparison in script.js:456 validation — noted, not scheduled. **Live-verified 2026-07-18** (session 7 start): Edit Task on the exact previously-broken row now pre-fills 12:00 PM matching the agenda row, and an untouched Save leaves it at 12:00 PM. See DECISIONS.md.
- [ ] **Uncompleting an item can leave its "Mark as Complete" checkbox visually checked** (found 2026-07-18, session 7 live verification; not fixed). `uncompleteItem` reuses the item's stale `listItemElement` rather than rebuilding it, so the checkbox — which is always checked at the moment completion fires — stays checked when the row re-enters the active list, until the next full rebuild (reload, or any edit that recreates the row). Cosmetic only: confirmed the underlying save data (XP/points/task-count) round-trips exactly on complete → uncomplete. Pre-existing, not a regression from the agendaList.js part 2 extraction. See DECISIONS.md.
- [ ] **Indulging a lurker, then reloading SAME-day, spawns a fresh lurker instance immediately** (found 2026-07-19, session 30 live verification; not fixed). `indulgeHabit` deliberately doesn't set `habitDef.lastCompletionDate` (a lapse isn't a completion), but `selectHabitDefsToSpawn`'s dedupe checks only that field + an existing active instance for today — so once the indulged instance is removed, nothing stops a same-day respawn. NOTE: the PRIOR-day variant is now handled by the day-advance mechanism (session 32); this remaining bug is specifically the SAME-day case (indulge + reload without a day boundary). Likely resolved when indulging records something the same-day dedupe consults (its occurrence entry is already recorded but dedupe doesn't check occurrenceHistory). See DECISIONS.md sessions 30/32.
- [x] **FAB → Routines popup doesn't live-refresh if left open under a stacked Manage Routine edit** (found 2026-07-19, session 38 live verification; **fixed 2026-07-19, session 40**). `editHabitInRoutine` (script.js wrapper) now mirrors `deleteRoutine`'s existing "refresh the FAB→Routines popup if open" `setTimeout` pattern. Live-verified in Chrome: froze a test routine, opened FAB→Routines (showed 🥶), drilled into Manage, edited the habit (real change), and the popup underneath updated to normal immediately without closing/reopening. 30 suites, 585/585 (unchanged). See DECISIONS.md session 40.
- [x] **No habit/task ever spawns a live instance in this Cowork Chrome dev environment** (found 2026-07-19, session 40 live verification; **RESOLVED 2026-07-19, session 41 — not a bug**). Root cause: the dev save had `sickDayDate: "2026-07-19"` (today) from session 39's own live-test of the Sick Day token — the global same-day spawn gate excused every habit for the rest of the day, exactly as designed. "Drink Water" additionally had `skipDayDate` = today from the Skip Day test. Verified by clearing both date gates (session-40 neutering protocol) and reloading: all 3 habits spawned sprites + agenda rows, zero app console errors. Two side findings: (1) session 40's `activeItems: []` probe was misleading — `window.activeItems`/`window.definedHabits` are stale mirrors; live state is closure-scoped and unreachable from devtools probes (DOM `.enemy` count is the reliable check). (2) The orphaned habit (`routineId` → deleted routine) DID spawn — orphans were supposed to be inert per the isActive-gating rule (2026-07-18); logged below as its own entry. See DECISIONS.md session 41.
- [x] **Orphaned habit definitions (routineId → deleted routine) spawn as if standalone** (found 2026-07-19, session 41; **fixed 2026-07-19, session 42**). Decided (Jeremy): migrate orphans to standalone (`routineId: null`), matching the existing "removed from a routine → standalone" precedent. New `Routines.releaseOrphanedHabits`, called from `deleteRoutine` (future deletions) and `state.js`'s `restoreGameState` (sweep on every load, heals pre-existing orphans). 32 suites, 639/639 (+4). Live-verified in Chrome: "Mindful Scrolling Check-in" (the exact orphan from session 41) now has `routineId: null` in the real save, zero console errors. See DECISIONS.md session 42.

## Milestone 3 — Core Feature Gaps (P1 tickets)
Ordering decided 2026-07-18 session 19 (Fable — see DECISIONS.md): 007 → 008 shop → 005 negative habits (+ frozen slots) → 006 heroes / 004 sub-tasks / run history.
- [x] [P1-DATA-007] Standardize points/currency system (2026-07-18, session 19) — rescoped: MPE half of the ticket is moot (reference-only variant); Root points were already consistent. Built `js/economy.js` (ARCHITECTURE.md target): `taskPoints` (the high-priority ×2 rule, de-duplicated from items.js's complete/uncomplete paths), `addPoints`/`subtractPoints` (0-floor kept — negative balances wait for P1-DATA-005 indulgence, see DECISIONS.md), and `shopPrice` (base × 1.5^owned per ECONOMY.md) ready for the shop. `test/economy.test.js` (18 suites, 380/380). Live-verified in Chrome. See DECISIONS.md.
- [x] **[P1-UI-008] Shop + repair kits + tokens with exponential pricing — CLOSED 2026-07-19 (sessions 20–24)** (see docs/ECONOMY.md + docs/SHOP_PLAN.md). v1 shipped: repair kits (buy/hold/use → base heal) + pushback (enemy-popup targeting, flat pricing) + inventory persistence (schema v4) + balance theory pass (habit tiers re-tuned to task parity; kit/pushback numbers validated). Day-tokens (cheat/sick/skip) deliberately deferred to [P1-DATA-005] negative habits. **Follow-up: re-check all session-24 balance numbers against REAL play data once Jeremy has played a while** (theory-pass yardstick: ~75–85 pts/day — if his real earn rate differs a lot, prices need another look).
  - [x] 1. State + config + `js/shop.js` pure core (2026-07-18, session 20) — `CONFIG.SHOP_ITEMS` (6 items), `js/shop.js` (pure getItem/heldCount/price/canAfford/purchase/consume, delegates to Economy), `playerInventory` state + accessor deps + persist/restore/reset, schemaVersion 3→4 migration seeding `inventory:{}`. `test/shop.test.js` + v3→v4 migration cases; 19 suites, 403/403. Live-verified migration + inventory round-trip in Chrome. See DECISIONS.md.
  - [x] 2. Shop UI frame (2026-07-19, session 21) — 4th FAB item + `#shopWindow` + `js/ui/shopView.js` catalog grid (live price/held-count/Buy) + `css/shop.css`; wired into `ManagementWindows.openManagementWindow`. Found + fixed a real bug live in Chrome: purchases were self-closing the window (event-target detachment mid-bubble into the "click outside closes window" listener) — fixed via `setTimeout(0)` deferring the rebuild. 19 suites, 403/403 (UI-only session, no new pure-core tests). Live-verified: FAB→Shop opens, buy deducts points/increments held/re-renders price in place, persists across reload. See DECISIONS.md.
  - [x] 3. Repair kit inventory + USE → `Damage.healBase` (2026-07-19, session 22) — each repair-kit card gets a "Use (+N HP)" button once held > 0 (`Shop.consume` + the existing `healBase(amount)` wrapper), disabled with "Base at full health" at `CONFIG.MAX_BASE_HEALTH`. Applied session-2's `setTimeout(0)` deferral pattern from the start. 19 suites, 403/403 (unchanged). Live-verified: heal + held decrement + price reset + reload persistence + full-health guard, all in Chrome. See DECISIONS.md.
  - [x] 4. Pushback items + enemy targeting (2026-07-19, session 23) — pushback lives in the enemy-click popup ("Push back this deadline" tiers, live prices, applies to any enemy); pays via `Shop.purchase`, shifts `dueDateTime` via pure `Shop.pushedBackDueDate`, `recomputeOverdueStateAfterEdit` un-camps overdue zombies, popup refreshes in place (stacking). Shop pushback cards show a "tap a zombie" hint, not a Buy button. **Pricing wrinkle resolved: FLAT** (Jeremy — per-run inflation deferred to session 5 to avoid a persistence change here). 19 suites, 407/407 (+4 `pushedBackDueDate`). Live-verified incl. overdue→un-camp + persistence. See DECISIONS.md.
  - [x] 5. Balance re-tune — THEORY pass (2026-07-19, session 24, Fable) — repair kits + pushback VALIDATED as-is; habit rate tiers re-tuned ≥90% → 2.0× (task parity, 10 pts) / ≥70% → 1.5× (8 pts). 19 suites, 407/407. Real-play re-check pending (see parent item). See DECISIONS.md.
- [x] **[P1-DATA-005] Positive/negative habit distinction (points loss, negative balance) — CLOSED 2026-07-19 (sessions 25–34)** — **SEQUENCED 2026-07-19 session 25 (Opus): `docs/NEGATIVE_HABITS_PLAN.md`.** Recon finding: much is already scaffolded (form toggle, `isNegative` on defs/instances, `.negative-habit` CSS, the polarity seam). Remaining = differentiated behavior: `'indulged'` event, inverted negative-overdue semantics, negative balance + debt UX, daily check-in, day-tokens. 5 sub-sessions + one Fable fork session (interaction model / debt depth / token scope), all now built (sub-session 5 / Cheat Day closed the ticket, session 34).
  - [x] 1. `'indulged'` event in `Habits.occurrenceSuccess` + pure `applyHabitIndulgence` (2026-07-19, session 25, Sonnet) — pure core only, not wired to items.js/UI/economy yet. 19 suites, 413/413.
  - [x] FABLE FORK (2026-07-19, session 26): A = A2 idle lurker (no base damage, check-in resolution); B = unbounded debt orthogonal to HP, encouraging UX; C = Cheat Day only, Sick/Skip → frozen-slots ticket. Full rationale in DECISIONS.md. Sub-sessions 2–5 unblocked + updated in NEGATIVE_HABITS_PLAN.md
  - [x] 2a. Non-threatening "lurker" — exclude negative habits from all 3 overdue/damage paths + fixed lurk position (2026-07-19, session 28, Sonnet). 20 suites, 434/434. Live-verified in Chrome
  - [x] 2b. Indulge / avoid popup actions (2026-07-19, session 30, Sonnet) — the rollover-hold guard piece was DESCOPED (see below + DECISIONS.md); 2b shipped as just the two-button binary.
  - [x] 3. Negative balance + debt visualization (2026-07-19, session 31, Sonnet) — `Economy.applyIndulgenceCost` (non-clamping) + red HUD balance with "complete N tasks to break even" nudge. See DECISIONS.md.
  - [x] 4. Daily check-in prompt (check-in surface only, NOT frozen slots) — BUILT 2026-07-19 session 33 (Opus plan → Sonnet execute). The previous day's negative-habit lurker now routes to `Items.markPendingCheckIn` instead of auto-avoiding; `js/ui/checkIn.js` prompts a binary "Successfully avoided"/"I indulged" card (+ 4-hour, non-persisted "I'll check this later" snooze) resolved by `Items.resolvePendingCheckIn`. Older stale days still auto-avoid via the existing path. 25 suites, 482/482 (+9). Live-verified in Chrome (avoided/indulged/snooze all confirmed). See DECISIONS.md session 33.
  - [x] 5. Day-tokens (Cheat Day) + application model + 4→5 migration — BUILT 2026-07-19 session 34 (Sonnet). `habitDef.cheatDayDate` (schemaVersion 5), `CONFIG.SHOP_ITEMS` cheat_day (200 pts, held-inventory pricing), targeted via a new "Use Cheat Day" button on the negative-habit lurker popup (tap-a-zombie pattern), excuses that day's indulgence for free (no debit/streak/occurrence change) via `Items.isCheatDayExcused` — checked in indulgeHabit, resolvePendingCheckIn, and a new rollover-fork branch (`Items.settleExcusedCheatDay`, checked first). Sick/Skip day tokens remain deferred to frozen slots. 26 suites, 498/498 (+16). Live-verified in Chrome (buy/use/free-indulge + silent rollover excuse). See DECISIONS.md session 34. **[P1-DATA-005] negative habit ticket now fully CLOSED** (all 5 sub-sessions + the Fable fork built).
- [x] **Day-advance mechanism (restore path)** (found session 30; BUILT 2026-07-19 session 32) — `js/dayRollover.js` (pure detection) + `Items.settleStaleRecurringInstance` + rollover wiring in state.js's restoreGameState. On reopen after midnight: prior-day negative lurker auto-avoided, positive habit miss-recorded + dropped, routine task dropped, one-off task untouched, `currentGameDate` advances, today's instances spawn. Also resolves the descoped-from-2b rollover-hold guard AND the same-day double-spawn is now moot for the prior-day case. See DECISIONS.md session 32.
- [ ] **Day-advance — LIVE mid-session rollover** (deferred from session 32, Jeremy's call) — detect midnight crossing in the running game loop so a tab left open past midnight rolls over live instead of waiting for the next reload. Restore path covers ~all real usage; this is the "left it running overnight, watching" edge. Adds day-mutation logic to loop.js's per-tick hot path. Lower priority.
- [x] **Frozen routine slots + recovery — CLOSED 2026-07-19 (sessions 35–39)** — SEQUENCED 2026-07-19 session 35 (Fable): `docs/FROZEN_SLOTS_PLAN.md` (mirrors NEGATIVE_HABITS_PLAN.md). 4 forks resolved (routine-scoped freeze that suspends the routine, non-destructively; Cheat/Sick/Skip-excused days transparent to both 3-day counts; minimal edit-change-tracking; Sick=global/Skip=per-habit). 5 sub-sessions, 2 schema bumps (6, 7). All 5 sub-sessions now built.
  - [x] 1. Pure freeze/recovery core + trigger wiring + schemaVersion 5→6 (2026-07-19, session 35, Sonnet) — `js/frozenSlots.js` + `routine.frozenState`/`habitDef.modificationHistory` + wired into all 5 occurrence-recording sites in `js/items.js`. 28 suites, 532/532. NOT yet built: spawn gating, frozen UI, edit-to-unfreeze, Sick/Skip tokens (sub-sessions 2-5). See DECISIONS.md.
  - [x] 2. Spawn gating + non-destructive suspension (2026-07-19, session 36, Sonnet) — `Habits.selectHabitDefsToSpawn`/`Routines.selectTaskDefsToSpawn` gate on new `FrozenSlots.isRoutineUsableForHabit`/`isRoutineSuspended` instead of a bare `isActive` check; a frozen routine's other habits/tasks stop spawning, the offending negative habit keeps lurking. Non-destructive (no recall). Routine-XP "no XP while frozen" confirmed a no-op — routine XP/leveling isn't built yet. 28 suites, 547/547. Live-verified in Chrome both directions (suspend + resume). NOT yet built: frozen UI, edit-to-unfreeze, Sick/Skip tokens (sub-sessions 3-5). See DECISIONS.md.
  - [x] 3. Frozen UI (2026-07-19, session 37, Sonnet) — greyed 🥶 routine card (`managementWindows.js`), detailed non-judgmental banner with LIVE recovery progress in the Manage modal (`routineViews.js`'s new `buildFrozenBannerHtml`), and a one-time trigger notice (new `js/ui/frozenNotice.js`, wired via a new optional `deps.onRoutineFrozen` in items.js). Found + fixed a real setTimeout(0) hazard (same class as session 21/34): the trigger site's `Modal.closeModal()` was deleting the notice in the same tick it was inserted. 29 suites, 552/552. Live-verified in Chrome, full cycle: freeze → notice → greyed card + 0/3 banner → 2/3 progress → real recovery click → normal card. NOT yet built: edit-to-unfreeze, Sick/Skip tokens (sub-sessions 4-5). See DECISIONS.md.
  - [x] 4. Recovery path 1 — edit-to-unfreeze + modificationHistory (2026-07-19, session 38, Sonnet) — `Routines.editHabitInRoutine` diffs the edit against the habit's current values, appends `{ timestamp, changedFields }` to `habitDef.modificationHistory` on any real change (no-op saves write nothing), and clears the owning routine's `frozenState` immediately (no 3-day wait) when the edited habit is the one holding the freeze, firing a new one-time `FrozenNotice.showRoutineUnfrozenNotice`. Only one save path existed to instrument (agenda-row edit and Manage Routine editor both funnel through the same `saveEditedHabit`/`editHabitInRoutine` chain). 29 suites, 560/560. `node --check` clean. Live-verified in Chrome: no-op save left frozenState/modificationHistory untouched and showed no notice; a real rename appended the modificationHistory entry, cleared frozenState, and fired the unfreeze notice with zero app console errors. Found (not fixed, logged above under Known bugs) a pre-existing FAB→Routines popup staleness unrelated to this feature's core logic. NOT yet built: Sick/Skip tokens (sub-session 5). See DECISIONS.md.
  - [x] 5. Sick Day (global) + Skip Day (per-habit) tokens + schemaVersion 6→7 (2026-07-19, session 39, Sonnet) — **[P1-DATA-005]/frozen-slots ticket now fully CLOSED.** 200 pts each (unchanged spec value), held-inventory exponential pricing like Cheat Day. "Clear immediately" model (Jeremy's call — resolved a mechanic gap the plan left open): using a token removes the matching already-spawned habit instance(s) from the board right away rather than waiting to excuse a later miss, so unlike Cheat Day there's no indulge/complete/rollover branch to wire — `Items.useSkipDayOnItem`/`useSickDayGlobally` just set the date marker + `removeItem`. `Habits.selectHabitDefsToSpawn` gained an optional `sickDayDate` param + a `skipDayDate` check as the same-day respawn guard (self-expires next calendar day, no explicit clearing). Skip Day targets one habit instance via its popup (Cheat-Day-style, but any habit type, not just negative); Sick Day is untargeted, applied from its own shop card. Piggybacked: backfilled a pre-existing gap where `routines.js`'s `createNewHabitInRoutine` never seeded `cheatDayDate` (session 34). 30 suites, 585/585 (+25). `node --check` clean. Live-verified in Chrome: Skip Day tapped from a POSITIVE habit's popup removed it immediately (streak/occurrenceHistory untouched, token consumed), same-day reload confirmed no respawn; Sick Day's shop-card Use swept the remaining habit instance off the board the same way, same-day reload confirmed the global gate too. Zero app console errors. See DECISIONS.md/FROZEN_SLOTS_PLAN.md.
- [~] [P1-UI-006] Hero/routine visual system (heroes in base, health, levels) — **SEQUENCED 2026-07-19 session 41: `docs/HEROES_PLAN.md`** (5 sub-sessions, 1 schema bump 7→8). Three forks resolved (CSS/emoji placeholder heroes — no sprite assets exist; health-0 = KO auto-deactivate with next-day revive; mechanics before visuals).
  - [x] 1. Pure hero core + 7→8 migration (2026-07-19, session 41, same session as the plan) — `js/heroes.js` (xpAmountFor/levelForXp/applyXpDelta/slotsForLevel/completionRate/starRating + pure health math), `routine.xp/level/health/createdAt/koState` (schemaVersion 8), XP wired at ALL completion sites gated by `FrozenSlots.isRoutineSuspended`, refund-exact via `item.routineXpAwarded` stamp + derived level. Star rate = habit members only in v1 (no honest task denominator exists). 32 suites, 635/635. Live-verified in Chrome (migration + award/refund round-trip). See DECISIONS.md session 41.
  - [x] 2. Routine health damage + KO/revive (2026-07-19, session 42, Sonnet) — base-damage tick path (live loop + both catch-up paths) also damages the breaching item's owning routine (`Items.damageRoutineForItem`); KO at 0 health auto-deactivates + recalls + fires a one-time notice; manual reactivation gated to the day after KO (`DayRollover.hasDayRolledOver`), reviving at `HERO_REVIVE_HEALTH` (50). Decided in-session: offline/gap catch-up damage CAN KO (capped per-item, same reasoning as base damage). 33 suites, 654/654 (+15). Live-verified in Chrome end-to-end (real offline catch-up → KO → notice → gated Revive button → backdated revive → health 50, koState cleared). See DECISIONS.md session 42.
  - [ ] 3. Hero rendering at the base (placeholder avatar chips, stars/level/health) (Sonnet)
  - [ ] 4. Hero management UI + slot enforcement — ask the grandfather question first (Sonnet)
  - [ ] 5. Polish: interaction visuals + ranking (Sonnet, optional)
- [ ] [P1-DATA-004] Sub-task hierarchy system (beyond bug fix: dependent due dates, shrinking parents)
- [ ] Run history + run review screen

## Milestone 4 — Polish (P2 tickets)
- [ ] [P2-UI-009] Streak visual effects (on-fire habits)
- [ ] [P2-GAME-010] Enemy acceleration mechanics
- [ ] [P2-UI-011] Management window unification
- [x] [P2-GAME-012] Base healing system (2026-07-18, session 17) — gradual regen BUILT: 1 HP/5min while alive (`js/loop.js`) + same rate offline/suspended-gap (`js/damage.js`'s `applyElapsedRegen`, applied after offline damage), clamped at 100; NO daily reset. Repair kits remain unbuilt (ties to the Milestone 3 shop). 17 suites, 370/370. Live-verified in Chrome. See MECHANICS.md Base + DECISIONS.md.
- [ ] [P2-UI-013] Routine transfer system
- [ ] Time slider (Today, then Week/Month scopes)
- [ ] Achievements & badges
- [ ] Mobile UX + accessibility pass; PWA
