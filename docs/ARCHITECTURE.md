# Architecture — Current State, Target, Refactor Rules

## Current State (July 2025 codebase)
- `index.html` (266 lines) + `script.js` (~3,420 lines) + `style.css` (~2,035 lines) — a working DOM-rendered monolith.
- `js/TaskManager.js`, `js/IdCounter.js` — first extractions already made.
- `server.js` — tiny static server (`node server.js` → http://localhost:8000). `npx serve .` also works.
- `test/` — Jest tests (subtask creation) + puppeteer visual tests. `npm install` then `npm test`.
- No persistence (localStorage is Milestone 1). No build step — keep it that way.

## Why we refactor
The 3,420-line script.js forced every Claude session to re-read one giant file — that's what degraded the July 2025 sessions. Modules mean a session loads only what its task touches.

## Refactor Strategy: incremental extraction (decided 2026-07-16; step 3 superseded 2026-07-17)
NOT a big-bang rewrite. One system per session:
1. Identify the functions for one system (Grep script.js — don't read it whole).
2. Move them to a module under `js/`, exporting what script.js needs. If the moved functions closed over script.js's module-scoped state (e.g. DOM-derived `let`s like `GAME_SCREEN_WIDTH`), take that state as explicit function parameters instead — don't recreate the closure across files. script.js can keep a thin wrapper at the original call sites so nothing else has to change.
3. Load via `<script>` tag order (index.html) — **the planned ES module conversion is CANCELLED (2026-07-17, see DECISIONS.md)**. Staying with the global + `module.exports` pattern already proven by `js/config.js`/`js/persistence.js`: no build step, and test files `require()` modules directly in Node.
4. `npm test` + manual smoke test pass before AND after. Commit each extraction.

## Target Layout
```
js/
├── config.js          # ALL gameplay numbers (create in Milestone 1)
├── state.js           # central state + mutation functions (only place state changes)
├── items.js           # item/task/habit completion lifecycle (IMPLEMENTED 2026-07-18 — session 10, added to target layout mid-session after being found during scoping, never covered by the UI extraction plan; see ROADMAP.md/DECISIONS.md). completeItem/removeItem/uncompleteItem/markAsOverdue/recomputeOverdueStateAfterEdit/createTaskItemData, all via a single itemsDeps() following the deps-injection pattern. activeItems is a plain reference (agendaListDeps() precedent); completedItems/definedHabits/gameIsOver are GETTERS (reassigned elsewhere / handlers outlive the call). playerXP/playerPoints get get/set accessor pairs — first module besides damage.js to WRITE script.js-owned numeric state, same ownership-stays-in-script.js reasoning. Habits/CONFIG called as bare stable globals inside items.js. FLAGGED, NOT FIXED: uncompleteItem hand-duplicates the enemy-sprite DOM construction that Spawning.addItemToGame already does correctly — pre-existing duplication, extracted verbatim; consolidating it is a separate future session (see DECISIONS.md).
├── persistence.js     # localStorage save/load + schemaVersion migrations
├── clock.js           # real time, timeline positions, midnight line (IMPLEMENTED 2026-07-17 — see DECISIONS.md). Offline catch-up deliberately stayed in script.js for now; moves to damage.js instead when that extraction happens (too tangled with damage/DOM animation to split off cleanly in clock.js's first session)
├── spawning.js        # enemy creation/admission (IMPLEMENTED 2026-07-17 — addItemToGame + pure resolveEnemyVisual; DOM collaborators injected via deps)
├── movement.js        # timeline-based positioning (IMPLEMENTED 2026-07-17 — getSubTaskClusterOffset/calculateTimelineXWithClustering/getItemTopPosition + internal getVisibleEdges; state/dims passed explicitly, CONFIG+Clock as globals)
├── damage.js          # overdue damage, base HP, game over, both catch-up paths (IMPLEMENTED 2026-07-18 — damageBase/gameOver/updateBaseVisuals/computeDaysSurvived + runOfflineCatchUp (moved here from clock.js's original scope, see clock.js note above) + runLiveGapCatchUp. Pure cores split out for testing: resolveBaseImage, computeGapCatchUpHits, computeOfflineOverdueDamage, computeCatchUpDuration, computeDaysSurvived. FIRST module to WRITE script.js-owned state (baseHealth, gameIsOver) — reached via accessor deps rather than moving ownership; see DECISIONS.md 2026-07-18. markAsOverdue stayed in script.js (it also resets habit streaks → belongs with the habits extraction) and arrives as a dep.
├── progression.js     # XP, levels, slots (IMPLEMENTED 2026-07-18 — pure Progression.checkLevelUp(state, thresholds, slotsPerLevel, maxLevel), walks every threshold crossed in one call instead of the old recursive self-call. playerPoints deliberately stayed in script.js — it's economy.js's future scope and the shop doesn't exist yet; see DECISIONS.md.)
├── habits.js          # habit instances, streaks, pos/neg logic (IMPLEMENTED 2026-07-18 — getHabitInstanceDueTime, selectHabitDefsToSpawn, applyHabitCompletion/applyHabitUncompletion, resetStreakOnOverdue are pure; createHabitInstanceData/generateDailyHabitInstances take id/position/DOM-admission collaborators via deps, same pattern as damage.js/spawning.js. markAsOverdue stayed in script.js — it also drives task overdue logic — but now delegates its streak-reset piece to Habits.resetStreakOnOverdue. See DECISIONS.md for a streak-bonus refund asymmetry found and deliberately preserved, and an open design question on streak- vs. rate-based bonuses routed to a future Fable session.)
├── routines.js        # heroes, slots (IMPLEMENTED 2026-07-18 — routine-task spawn selection (getRoutineTaskInstanceDueTime, selectTaskDefsToSpawn, createRoutineTaskInstanceData, generateDailyRoutineTaskInstances, mirroring habits.js's pattern), definition CRUD (createRoutineDefinition, deleteRoutine, add/removeHabitFromRoutine, add/removeTaskFromRoutine via createNewHabitInRoutine/createNewTaskInRoutine, editHabitInRoutine, editTaskInRoutine, addHabitToRoutine), and activation/deactivation (selectActiveItemIdsToClearForRoutine, clearActiveInstancesForRoutine, toggleRoutineActive). Deliberately does NOT include the DOM-rendering functions (renderDefinedRoutines, populateRoutinesWindow, showRoutineManagement, populateRoutineHabits/Tasks, createRoutineFormHtml, attachRoutineManagementListeners, showAddItemToRoutineModal, updateRoutineDisplay) — pure DOM construction with no reusable logic, left for the future "Extract UI" item. "Frozen recovery" isn't built yet (Milestone 3) so nothing existed to extract for it. script.js keeps thin wrappers at every original call site. See DECISIONS.md.)
├── economy.js         # points, shop, exponential pricing
├── TaskManager.js     # (exists) task CRUD
├── IdCounter.js       # (exists) id generation
└── ui/                # forms, agendaList, canvasView, hud, popups, fabMenu
    ├── modal.js       # modal-overlay teardown + window.* exposure helper (IMPLEMENTED 2026-07-18 — UI extraction session 1 of docs/UI_EXTRACTION_PLAN.md. closeModal moved out of routine code, the 18 inline onclick="closeModal()" sites no longer depend on routines.js. Overlay CREATION deliberately stayed out of scope — each form/popup cluster still builds its own modal-overlay HTML inline; that moves as each cluster is extracted. exposeGlobal(name, fn) centralizes the window.* attachment inline onclick= handlers require, in response to the deleteRoutine bug (2026-07-18) — not yet adopted at existing call sites, only available for future sessions.)
    ├── hud.js         # player displays, level-up message, task count, debug panel (IMPLEMENTED 2026-07-18 — UI extraction session 2. updatePlayerDisplays/updateTaskCountDisplay/showLevelUpMessage/showDebugInfo, all via explicit deps objects, no closures. showDebugInfo is unreferenced anywhere in script.js — dead code, extracted as-is per the cluster boundary, not removed.)
    ├── fabMenu.js     # FAB menu open/close toggle (IMPLEMENTED 2026-07-18 — UI extraction session 3. toggleFabMenu/closeFabMenu via deps {fabMenu, fabButton}. 6 leftover debug console.log lines removed during the move.)
    ├── managementWindows.js  # Tasks/Habits/Routines list windows + shared backdrop (IMPLEMENTED 2026-07-18 — UI extraction session 3. openManagementWindow/closeAllManagementWindows/closeManagementWindow/populateTasksWindow/populateHabitsWindow/populateRoutinesWindow, all via deps. Two dependencies threaded through deps rather than assumed: FabMenu.closeFabMenu (cross-module, this same session) and showRoutineManagement/toggleRoutineActive (still script.js-scoped — showRoutineManagement belongs to cluster F, not extracted until sessions 8/9).)
    ├── forms.js       # FAB modal creation forms — task/habit/routine (IMPLEMENTED 2026-07-18 — UI extraction session 4. showFormModal/createTaskFormHtml/createHabitFormHtml/createRoutineFormHtml/attachModalEventListeners. Modal and Routines called as bare stable globals (like CONFIG/Clock in movement.js); script.js closure values (createTaskItemData, addItemToGame, saveGame, etc.) arrive via deps. RECONCILED the routine-creation branch's inline duplicate of Routines.createRoutineDefinition — now calls it directly, which also adds a previously-missing saveGame() call (real behavior change: routine creation now saves immediately instead of waiting up to 5s for autosave). See DECISIONS.md. Debug console.log tracing removed; console.error on genuine failure paths kept.)
    ├── agendaList.js  # agenda row construction — COMPLETE (IMPLEMENTED 2026-07-18 across sessions 6-7). Session 6: `createListItem` — the shared row shell plus task/habit/unknown branches and sub-task rows. Session 7: `sortAndRenderActiveList`, `resetAllSubTaskCheckboxes`, `renderCompletedItems`, `showEditHabitInstanceModal`. DEPENDENCY DIVERGENCE, deliberate: `gameIsOver` arrives as a getter (`deps.isGameOver()`), NOT a flat boolean like sessions 3-5 used — `createListItem` is the first UI extraction that attaches handlers OUTLIVING the call, so a snapshotted boolean would let rows built before game-over keep spawning sub-tasks afterward (follows the `isGameOver: () => gameIsOver` precedent in js/spawning.js). Session 7 extends the same reasoning to `completedItems` and `definedHabits`: both arrive as GETTERS because they're REASSIGNED elsewhere in script.js (new-game reset, restoreGameState), not just mutated in place — a plain reference would go stale across a reset. `activeItems`/`categoryStyles`/`activeItemsListUL` stay plain references (stable bindings); `uncompleteItem`/`showEditHabitForm` are passed as plain function references (called, not read; `showEditHabitForm` is still script.js-scoped, cluster F, not yet extracted). The retained `DEBUG:` sub-task-checkbox and resetAllSubTaskCheckboxes console.log lines are a deliberate carve-out, same reasoning as popups.js. FIRST UI module with real unit coverage: test/agenda-list.test.js requires and executes the actual module rather than mirroring it — session 7 retired the now-redundant hand-maintained mirror (test/create-list-item-branching.test.js), see DECISIONS.md.)
    ├── popups.js      # enemy click, task details, edit task, sub-task creation modals (IMPLEMENTED 2026-07-18 — UI extraction session 5. handleEnemyClick/showTaskDetailsPopup/showEditTaskModal/createSubTaskPrompt/showCreateSubTaskModal, all via deps for script.js closure state/functions; Modal called as a bare global. Two dead unused `today` locals dropped (verified unreferenced by Grep, zero behavior effect). showCreateSubTaskModal's debug console.log lines deliberately LEFT UNCHANGED — this is the exact function at the center of the historic sub-task-duplication bug; cleaning up its diagnostic logging is a separate decision from moving the code, not made here.)
    └── routineViews.js  # routine rendering + forms — COMPLETE (IMPLEMENTED across sessions 8-9, 2026-07-18). Session 8 (rendering half): `renderDefinedRoutines`, `showRoutineManagement`, `populateRoutineHabits`, `populateRoutineTasks`, `updateRoutineDisplay`. PLAN CORRECTION: the plan's session-8 row also listed `populateRoutinesWindow`, but that was already extracted into managementWindows.js during session 3 (cluster E) — nothing left to move for it. Session 9 (form half): `showCreateHabitForm`/`showCreateTaskForm`/`showEditHabitForm`/`showEditTaskForm` (pure HTML-string builders, no deps), `showAddItemToRoutineModal`, `attachRoutineManagementListeners`, `populateHabitSelectDropdown`, and `saveNewHabit`/`saveNewTask`/`saveEditedHabit`/`saveEditedTask` (script.js keeps these four as `window.save*` thin wrappers, not plain functions, because the form HTML's inline `onclick="saveNewHabit(...)"` etc. needs the window-level name — same reasoning as `window.closeModal`/`window.deleteRoutine`). `definedRoutines`/`definedHabits` cross in as GETTERS (reassigned elsewhere in script.js), extending the agendaList.js part 2 (session 7) precedent. `definedTasks` has NO local declaration in script.js at all — always the bare `window.definedTasks` global — so the module reads it directly, no dep needed. `definedRoutinesListUL`/`activeRoutineCountDisplay`/`managementWindows` are stable const DOM refs. `activeItems` (needed by `saveEditedHabit`'s live-instance sync) is a plain reference, matching agendaListDeps()'s established "stable binding" precedent. `Modal.closeModal()` is called as a bare stable global throughout part 2 (Modal is fully extracted, guaranteed loaded first), matching popups.js/forms.js convention. Session 9 also flipped part 1's six form-half plain-function deps (showEditHabitForm, populateHabitSelectDropdown, showCreateHabitForm, showEditTaskForm, showCreateTaskForm, attachRoutineManagementListeners) to module-internal calls now that they live in the same file; `showRoutineManagement` forwards its whole deps object into `attachRoutineManagementListeners` rather than picking out a subset, since it doesn't need to distinguish which keys belong downstream. **Discovery, not fixed (session 8):** `renderDefinedRoutines` targets `#definedRoutinesList`, an element that does not exist anywhere in index.html — it's a leftover from the pre-modal routine UI (predates the modal-based `routinesWindowList` this game actually uses) and the function has always silently no-op'd via its `if (!definedRoutinesListUL) return` guard. Preserved as-is (behavior-identical extraction); candidate for deletion in a future cleanup pass. See DECISIONS.md.)
```
`script.js` ends as boot/wiring only (<300 lines). `style.css` splits per component after JS is done.

## Conventions
- Balance numbers only in `js/config.js`. UI never mutates state directly. Max ~300 lines/file.
- Real timestamps in logic; accelerated demo time only behind a config flag.
- No new dependencies without a DECISIONS.md entry.
- Old prototype `Deadline-MPE/` is frozen reference — never modify.

## Testing
- Jest for logic; keep tests running through every extraction. Add tests for each extracted module (movement math, damage ticks, pricing curve, streak resets, persistence round-trip).
- `playtester` agent runs `npm test` + `node --check` on changed files before handoff.
