# Architecture — Current State, Target, Refactor Rules

## Current State (July 2025 codebase)
- `index.html` (~280 lines) + `script.js` (~3,420 lines) — a working DOM-rendered monolith (JS side; CSS split out, see below).
- `css/` (split 2026-07-18, session 18 — see the target-layout note near the bottom of this file) — `base.css`, `gameCanvas.css`, `enemySprites.css`, `enemyStatus.css`, `agendaList.css`, `fabMenu.css`, `managementWindows.css`, `forms.css`, `routineViews.css`, `modal.css`, `popups.css`, `responsive.css` (**must load LAST** — see its own header comment + DECISIONS.md). `index.html` links all 12 in that exact order. The old root `style.css` monolith (~2,095 lines) is deleted; recoverable from git history if ever needed.
- `js/TaskManager.js`, `js/IdCounter.js` — first extractions already made.
- `server.js` — tiny static server (`node server.js` → http://localhost:8000). `npx serve .` also works.
- `test/` — Jest tests (subtask creation) + puppeteer visual tests. `npm install` then `npm test`.
- No persistence (localStorage is Milestone 1). No build step — keep it that way.

## Why we refactor
The 3,420-line script.js forced every Claude session to re-read one giant file — that's what degraded the July 2025 sessions. Modules mean a session loads only what its task touches.

## Refactor Strategy: incremental extraction (decided 2026-07-16; step 3 superseded 2026-07-17)
NOT a big-bang rewrite. One system per session:
1. Identify the functions for one system (Grep script.js — don't read it whole).
2. Move them to a module under `js/`, exporting what script.js needs. If the moved functions closed over script.js's module-scoped state (e.g. DOM-derived `let`s like `GAME_SCREEN_WIDTH`), take that state as explicit function parameters instead — don't recreate the closure across files. script.js can keep a thin wrapper at the original call sites so nothing else has to change.
3. Load via `<script>` tag order (index.html) — **the planned ES module conversion is CANCELLED (2026-07-17, see DECISIONS.md)**. Staying with the global + `module.exports` pattern already proven by `js/config.js`/`js/persistence.js`: no build step, and test files `require()` modules directly in Node.
4. `npm test` + manual smoke test pass before AND after. Commit each extraction.

## Target Layout
```
js/
├── config.js          # ALL gameplay numbers (create in Milestone 1)
├── state.js           # game lifecycle + persistence orchestration (IMPLEMENTED 2026-07-18 — session 11. initGame/restoreGameState/getPersistableState/saveGame/buildDamageDeps, all via a single stateDeps(). DELIBERATE DEVIATION from this line's original description: state.js does NOT yet own the state variables ("central state... only place state changes") — baseHealth/playerXP/activeItems/etc. all stay as script.js `let`s, reached via accessor deps, extending the damage.js/items.js precedent. Combining the function move with a storage-ownership migration in one session would have stacked two risky changes onto the module that guards persistence. The migration is deliberately deferred — and is script.js-only when it happens, since every other module already receives accessor *functions*, never the raw bindings (see DECISIONS.md 2026-07-18 for the full reasoning and a suggested future session scope).
├── items.js           # item/task/habit completion lifecycle (IMPLEMENTED 2026-07-18 — session 10, added to target layout mid-session after being found during scoping, never covered by the UI extraction plan; see ROADMAP.md/DECISIONS.md). completeItem/removeItem/uncompleteItem/markAsOverdue/recomputeOverdueStateAfterEdit/createTaskItemData, all via a single itemsDeps() following the deps-injection pattern. activeItems is a plain reference (agendaListDeps() precedent); completedItems/definedHabits/gameIsOver are GETTERS (reassigned elsewhere / handlers outlive the call). playerXP/playerPoints get get/set accessor pairs — first module besides damage.js to WRITE script.js-owned numeric state, same ownership-stays-in-script.js reasoning. Habits/CONFIG called as bare stable globals inside items.js. FLAGGED, NOT FIXED: uncompleteItem hand-duplicates the enemy-sprite DOM construction that Spawning.addItemToGame already does correctly — pre-existing duplication, extracted verbatim; consolidating it is a separate future session (see DECISIONS.md).
├── persistence.js     # localStorage save/load + schemaVersion migrations (v3 as of 2026-07-18 session 14: v2→v3 adds `schedule` to habit + routine-task defs and seeds habit `occurrenceHistory`; migration writes literal shapes rather than calling schedule.js so it stays a stable historical transform)
├── schedule.js        # recurrence rules — daily/weekly (day-of-week filter) + monthly (day-of-month, clamped to month length) (IMPLEMENTED 2026-07-18 session 14). Pure, no DOM/CONFIG. defaultSchedule/fromLegacyFrequency/normalize/isScheduledForDay. Both habits.js (selectHabitDefsToSpawn) and routines.js (selectTaskDefsToSpawn) call isScheduledForDay as the recurrence gate, replacing habits' old `frequency==='daily'` check and routine tasks' no-check. normalize() tolerates a bare legacy `frequency` string, so an unmigrated in-memory def still gates correctly. test/schedule.test.js. Loaded in index.html right after config.js.
├── clock.js           # real time, timeline positions, midnight line (IMPLEMENTED 2026-07-17 — see DECISIONS.md). Offline catch-up deliberately stayed in script.js for now; moves to damage.js instead when that extraction happens (too tangled with damage/DOM animation to split off cleanly in clock.js's first session)
├── spawning.js        # enemy creation/admission (IMPLEMENTED 2026-07-17 — addItemToGame + pure resolveEnemyVisual; DOM collaborators injected via deps)
├── movement.js        # timeline-based positioning (IMPLEMENTED 2026-07-17 — getSubTaskClusterOffset/calculateTimelineXWithClustering/getItemTopPosition + internal getVisibleEdges; state/dims passed explicitly, CONFIG+Clock as globals)
├── timeSlider.js      # 24h preview-slider math, Today scope only (IMPLEMENTED 2026-07-20 — session 63, part of "Time slider (Today, then Week/Month scopes)"). Pure: getDayBounds/minutesOfDayToTime/timeToMinutesOfDay/formatLabel/getLurkerPreviewX; Clock+CONFIG as bare globals, clock.js/movement.js precedent. No separate "not-yet-spawned" ghost logic — Today's future-due items are already active (spawned at day start, positioned off-screen right), so re-running Clock.calculateTimelinePosition/Movement.calculateTimelineXWithClustering at a scrubbed previewTime is sufficient; Week/Month scope (unbuilt) will need real cross-day ghosting. getLurkerPreviewX rides the midnight line once on-screen (>=8pm) instead of the live loop's fixed lurk-post anchor — preview-only, live loop.js positioning of lurkers is untouched. **Same session, follow-up:** ticksSinceOverdue/damageTicksDelta/regenTicksDelta/projectBaseHealth/projectRoutineHealthDeltas — pure "what would HP be at previewTime" projection, a DELTA from "now" (not simulated from scratch) using the live loop's own damage/regen tick constants, so it can never disagree with what would actually happen; symmetric for rewind. See UI_UX.md/MECHANICS.md/DECISIONS.md session 63.
├── damage.js          # overdue damage, base HP, game over, both catch-up paths (IMPLEMENTED 2026-07-18 — damageBase/gameOver/updateBaseVisuals/computeDaysSurvived + runOfflineCatchUp (moved here from clock.js's original scope, see clock.js note above) + runLiveGapCatchUp. Pure cores split out for testing: resolveBaseImage, computeGapCatchUpHits, computeOfflineOverdueDamage, computeCatchUpDuration, computeDaysSurvived. FIRST module to WRITE script.js-owned state (baseHealth, gameIsOver) — reached via accessor deps rather than moving ownership; see DECISIONS.md 2026-07-18. markAsOverdue stayed in script.js (it also resets habit streaks → belongs with the habits extraction) and arrives as a dep.
├── progression.js     # XP, levels, slots (IMPLEMENTED 2026-07-18 — pure Progression.checkLevelUp(state, thresholds, slotsPerLevel, maxLevel), walks every threshold crossed in one call instead of the old recursive self-call. playerPoints deliberately stayed in script.js — it's economy.js's future scope and the shop doesn't exist yet; see DECISIONS.md.)
├── habits.js          # habit instances, streaks, pos/neg logic (IMPLEMENTED 2026-07-18 — getHabitInstanceDueTime, selectHabitDefsToSpawn, applyHabitCompletion/applyHabitUncompletion, resetStreakOnOverdue are pure; createHabitInstanceData/generateDailyHabitInstances take id/position/DOM-admission collaborators via deps, same pattern as damage.js/spawning.js. markAsOverdue stayed in script.js — it also drives task overdue logic — but now delegates its streak-reset piece to Habits.resetStreakOnOverdue. See DECISIONS.md for a streak-bonus refund asymmetry found and deliberately preserved, and an open design question on streak- vs. rate-based bonuses routed to a future Fable session. **Updated 2026-07-19 (frozen-slots sub-session 2):** selectHabitDefsToSpawn's owning-routine check now calls FrozenSlots.isRoutineUsableForHabit(routine, habitDef.id) instead of a bare `r.isActive` test — a routine that's active but frozen is usable ONLY for the negative habit that caused the freeze, so that lurker keeps spawning while the routine's other habits are suspended. FrozenSlots referenced as a bare global, loaded just before this file.)
├── routines.js        # heroes, slots (IMPLEMENTED 2026-07-18 — routine-task spawn selection (getRoutineTaskInstanceDueTime, selectTaskDefsToSpawn, createRoutineTaskInstanceData, generateDailyRoutineTaskInstances, mirroring habits.js's pattern), definition CRUD (createRoutineDefinition, deleteRoutine, add/removeHabitFromRoutine, add/removeTaskFromRoutine via createNewHabitInRoutine/createNewTaskInRoutine, editHabitInRoutine, editTaskInRoutine, addHabitToRoutine), and activation/deactivation (selectActiveItemIdsToClearForRoutine, clearActiveInstancesForRoutine, toggleRoutineActive). Deliberately does NOT include the DOM-rendering functions (renderDefinedRoutines, populateRoutinesWindow, showRoutineManagement, populateRoutineHabits/Tasks, createRoutineFormHtml, attachRoutineManagementListeners, showAddItemToRoutineModal, updateRoutineDisplay) — pure DOM construction with no reusable logic, left for the future "Extract UI" item. "Frozen recovery" isn't built yet (Milestone 3) so nothing existed to extract for it. script.js keeps thin wrappers at every original call site. See DECISIONS.md. **Updated 2026-07-19 (frozen-slots sub-session 1):** `createRoutineDefinition` seeds the new `frozenState: null` field; `createNewHabitInRoutine` seeds the new `modificationHistory: []` field. The freeze/recovery LOGIC itself lives in js/frozenSlots.js + js/items.js, not here — routines.js just owns the shape. **Updated 2026-07-19 (sub-session 2):** selectTaskDefsToSpawn's active-routine collection now calls FrozenSlots.isRoutineSuspended(routine) instead of a bare `!routine.isActive` check — a frozen routine suspends its tasks with NO exception (a routine task can never be the negative habit that caused a freeze, unlike habits.js's habit-aware sibling predicate).)
├── loop.js            # per-tick game loop (IMPLEMENTED 2026-07-18 — session 12. updateGame (the setInterval callback: gap detection → tick → autosave) + updateActiveItems (position/overdue-transition/damage sweep), the last real game logic left in script.js. Single loopDeps() rebuilt per call; lastLoopTickMs/lastAutosaveMs cross as get/set pairs (damage.js baseHealth precedent), gameIsOver/offlineCatchUpActive as getters, activeItems as a plain reference (safe: deps rebuilt every tick), CONFIG as a bare global. test/loop.test.js (13 cases) exercises the real module.)
├── economy.js         # points, shop, exponential pricing (IMPLEMENTED 2026-07-18 session 19 — pure taskPoints/addPoints/subtractPoints/shopPrice; playerPoints ownership stays in script.js accessor-deps; shop UI itself still unbuilt [P1-UI-008])
├── frozenSlots.js     # frozen routine slots — freeze trigger + avoidance-recovery counting + spawn-gating predicates (IMPLEMENTED 2026-07-19 across sub-sessions 1-2 of the "Frozen routine slots + recovery" ticket, see docs/FROZEN_SLOTS_PLAN.md session 35 Fable. Pure: trailingRun/shouldFreeze/shouldRecoverByAvoidance/avoidanceProgress/buildFrozenState (sub-session 1), all reading habitDef.occurrenceHistory — no schema of its own beyond routine.frozenState/habitDef.modificationHistory (schemaVersion 6). Wired into items.js's five occurrence-recording sites (indulgeHabit, resolvePendingCheckIn, completeItem, settleStaleRecurringInstance) as a bare stable global, same convention as Habits/Economy/CONFIG there. definedRoutines threaded through itemsDeps() as a new GETTER — OPTIONAL at the deps level (items.js falls back to an empty array if omitted) so no existing test file needed touching. Sub-session 2 added isRoutineUsableForHabit/isRoutineSuspended — the spawn-gating predicates habits.js/routines.js call (see their entries below); loaded before both in index.html (right after shop.js). The frozen-state UI, edit-to-unfreeze, and Sick/Skip Day tokens are NOT yet built — see ROADMAP.md/FROZEN_SLOTS_PLAN.md sub-sessions 3-5.)
├── TaskManager.js     # (exists) task CRUD
├── IdCounter.js       # (exists) id generation
└── ui/                # forms, agendaList, canvasView, hud, popups, fabMenu
    ├── modal.js       # modal-overlay teardown + window.* exposure helper (IMPLEMENTED 2026-07-18 — UI extraction session 1 of docs/UI_EXTRACTION_PLAN.md. closeModal moved out of routine code, the 18 inline onclick="closeModal()" sites no longer depend on routines.js. Overlay CREATION deliberately stayed out of scope — each form/popup cluster still builds its own modal-overlay HTML inline; that moves as each cluster is extracted. exposeGlobal(name, fn) centralizes the window.* attachment inline onclick= handlers require, in response to the deleteRoutine bug (2026-07-18) — not yet adopted at existing call sites, only available for future sessions. **EXPANDED into the unified window/modal behavior layer, [P2-UI-011] Stage 1, session 61, 2026-07-19:** added closeTopmost (topmost-only teardown; closeModal keeps close-ALL semantics — existing call sites rely on the nuke-all), getTopmostOverlay (DOM order = stacking order, shared z-index), initDismissHandlers(deps) (document-level ESC topmost-first-then-windows/FAB + backdrop-click + outside-click — replaces the two inline script.js handlers and the redundant per-popup backdrop listeners popups.js used to attach), initFocusManagement (MutationObserver on body childList — overlays are ALWAYS direct children of body, an invariant this depends on — auto-applies role/aria-modal/aria-label, focuses .modal-content, restores focus to the recorded opener on close), and trapTab (Tab cycle inside topmost overlay). Management-window focus (win.focus() on open, restore-to-FAB-when-stranded on close) lives in managementWindows.js. First jsdom test file: test/modal-behavior.test.js (jest-environment-jsdom devDependency, per-file docblock; global testEnvironment stays 'node'). **Stage 2 SEQUENCED 2026-07-20 (Cowork session, Opus): docs/MODAL_STAGE2_PLAN.md, 5 sub-sessions. Sub-session 1 (this session) shipped `Modal.open(html, {dedupeSelector, defer})` — inserts a caller-built `.modal-overlay` string as the last child of body and returns it synchronously (or no-ops/returns null on a `dedupeSelector` match; `defer:true` schedules via setTimeout(0) and returns undefined, for the closeModal-same-tick hazard sites). Does not template content or absorb per-cluster listener wiring — pure insertion primitive, same "small core + caller-side complexity" convention as the rest of the codebase. checkIn.js migrated as the pilot (dedupeSelector replaces its old hand-written `.check-in-overlay` guard). +7 tests in test/modal-behavior.test.js. **Sub-session 2 (2026-07-20, same session batch): all 6 `js/ui/frozenNotice.js` functions migrated onto `Modal.open(html, {dedupeSelector: '.frozen-notice-overlay', defer: true})`, replacing their hand-rolled `setTimeout(fn, 0)` + manual `querySelector` dedupe guards — first production validation of the `defer` path (the pilot only exercised it in tests). `showAchievementUnlockNotice` keeps its own queue/batch/poll wrapper (a genuinely different concern, layered on top of `open()`, not absorbed into it per the plan's Fork 2) but its final insert now goes through `Modal.open()` too. No new tests (frozenNotice.js has never had dedicated unit coverage — DOM-heavy, live-verified in Chrome instead, same precedent as checkIn.js pre-Stage-2); live-verified the real closeModal-same-tick hazard end to end (indulged a habit through the actual UI to trip a live routine freeze — the notice survived the synchronous `closeModal()` race) plus dedupe and achievement-batching via console. The other 3 clusters (popups.js, forms.js, routineViews.js) still insert directly — unchanged until their own sub-session.**)
    ├── hud.js         # player displays, level-up message, task count, debug panel (IMPLEMENTED 2026-07-18 — UI extraction session 2. updatePlayerDisplays/updateTaskCountDisplay/showLevelUpMessage/showDebugInfo, all via explicit deps objects, no closures. showDebugInfo is unreferenced anywhere in script.js — dead code, extracted as-is per the cluster boundary, not removed.)
    ├── fabMenu.js     # FAB menu open/close toggle (IMPLEMENTED 2026-07-18 — UI extraction session 3. toggleFabMenu/closeFabMenu via deps {fabMenu, fabButton}. 6 leftover debug console.log lines removed during the move.)
    ├── managementWindows.js  # Tasks/Habits/Routines list windows + shared backdrop (IMPLEMENTED 2026-07-18 — UI extraction session 3. openManagementWindow/closeAllManagementWindows/closeManagementWindow/populateTasksWindow/populateHabitsWindow/populateRoutinesWindow, all via deps. Two dependencies threaded through deps rather than assumed: FabMenu.closeFabMenu (cross-module, this same session) and showRoutineManagement/toggleRoutineActive (still script.js-scoped — showRoutineManagement belongs to cluster F, not extracted until sessions 8/9).)
    ├── forms.js       # FAB modal creation forms — task/habit/routine (session 15, 2026-07-18: added the shared scheduleFieldsHtml/wireScheduleFieldsToggle/readScheduleFromFields day-of-week/day-of-month widget to createHabitFormHtml + the habit save handler — see DECISIONS.md. IMPLEMENTED 2026-07-18 — UI extraction session 4. showFormModal/createTaskFormHtml/createHabitFormHtml/createRoutineFormHtml/attachModalEventListeners. Modal and Routines called as bare stable globals (like CONFIG/Clock in movement.js); script.js closure values (createTaskItemData, addItemToGame, saveGame, etc.) arrive via deps. RECONCILED the routine-creation branch's inline duplicate of Routines.createRoutineDefinition — now calls it directly, which also adds a previously-missing saveGame() call (real behavior change: routine creation now saves immediately instead of waiting up to 5s for autosave). See DECISIONS.md. Debug console.log tracing removed; console.error on genuine failure paths kept.)
    ├── agendaList.js  # agenda row construction — COMPLETE (IMPLEMENTED 2026-07-18 across sessions 6-7). Session 6: `createListItem` — the shared row shell plus task/habit/unknown branches and sub-task rows. Session 7: `sortAndRenderActiveList`, `resetAllSubTaskCheckboxes`, `renderCompletedItems`, `showEditHabitInstanceModal`. DEPENDENCY DIVERGENCE, deliberate: `gameIsOver` arrives as a getter (`deps.isGameOver()`), NOT a flat boolean like sessions 3-5 used — `createListItem` is the first UI extraction that attaches handlers OUTLIVING the call, so a snapshotted boolean would let rows built before game-over keep spawning sub-tasks afterward (follows the `isGameOver: () => gameIsOver` precedent in js/spawning.js). Session 7 extends the same reasoning to `completedItems` and `definedHabits`: both arrive as GETTERS because they're REASSIGNED elsewhere in script.js (new-game reset, restoreGameState), not just mutated in place — a plain reference would go stale across a reset. `activeItems`/`categoryStyles`/`activeItemsListUL` stay plain references (stable bindings); `uncompleteItem`/`showEditHabitForm` are passed as plain function references (called, not read; `showEditHabitForm` is still script.js-scoped, cluster F, not yet extracted). The retained `DEBUG:` sub-task-checkbox and resetAllSubTaskCheckboxes console.log lines are a deliberate carve-out, same reasoning as popups.js. FIRST UI module with real unit coverage: test/agenda-list.test.js requires and executes the actual module rather than mirroring it — session 7 retired the now-redundant hand-maintained mirror (test/create-list-item-branching.test.js), see DECISIONS.md.)
    ├── popups.js      # enemy click, task details, edit task, sub-task creation modals (IMPLEMENTED 2026-07-18 — UI extraction session 5. handleEnemyClick/showTaskDetailsPopup/showEditTaskModal/createSubTaskPrompt/showCreateSubTaskModal, all via deps for script.js closure state/functions; Modal called as a bare global. Two dead unused `today` locals dropped (verified unreferenced by Grep, zero behavior effect). showCreateSubTaskModal's debug console.log lines deliberately LEFT UNCHANGED — this is the exact function at the center of the historic sub-task-duplication bug; cleaning up its diagnostic logging is a separate decision from moving the code, not made here.)
    ├── frozenNotice.js  # one-time frozen-routine notice modal (IMPLEMENTED 2026-07-19, sub-session 3 of the "Frozen routine slots + recovery" ticket — see docs/FROZEN_SLOTS_PLAN.md session 37). Small dedicated module, same size/scope precedent as checkIn.js rather than growing the already-1000+-line routineViews.js. showFrozenRoutineNotice(routineName, habitName) fires from a new OPTIONAL deps.onRoutineFrozen collaborator in items.js's maybeFreezeRoutine, wired in script.js's itemsDeps(). Modal called as a bare stable global. Hazard found + fixed: the trigger site (popups.js's "I indulged" button) calls Modal.closeModal() synchronously right after firing this notice — closeModal() removes ALL .modal-overlay elements, so inserting the notice synchronously meant it was deleted in the same tick before painting (same bug class as session 21's shop bug / session 34's Cheat Day popup rebuild). Fixed by deferring the insertion via setTimeout(0), landing after the click handler's closeModal() call. No dedicated unit test (DOM-heavy, matches checkIn.js's own precedent — live-verified in Chrome instead).
    ├── timeSliderView.js  # DOM wiring for the 24h preview slider (IMPLEMENTED 2026-07-20, session 63 — see js/timeSlider.js above for the pure math). init/syncHandle only exposed; scrub ('input')/release ('change'/'pointerup'/'touchend'/'blur') lifecycle is internal. Base/routine-HP projection deps (getBaseHealth/getLastRegenTickMs/baseElement/baseHealthDisplayEl/resolveBaseImage/getDefinedRoutines/getRoutineIdForItem/renderHeroesAtBase/heroBaseZoneEl) are ONE optional-collaborator group, gated on `typeof deps.getBaseHealth === 'function'` — omitting them (as every position/scrub test does) skips damage/HP projection entirely, matching loop.js's optional-collaborator tolerance. script.js's renderHeroesAtBase() gained an optional routinesOverride param this session so the SAME HeroesView render path can draw projected health during a scrub — no changes to HeroesView itself.
    └── routineViews.js  # routine rendering + forms — COMPLETE (IMPLEMENTED across sessions 8-9, 2026-07-18). Session 8 (rendering half): `renderDefinedRoutines`, `showRoutineManagement`, `populateRoutineHabits`, `populateRoutineTasks`, `updateRoutineDisplay`. PLAN CORRECTION: the plan's session-8 row also listed `populateRoutinesWindow`, but that was already extracted into managementWindows.js during session 3 (cluster E) — nothing left to move for it. Session 9 (form half): `showCreateHabitForm`/`showCreateTaskForm`/`showEditHabitForm`/`showEditTaskForm` (pure HTML-string builders, no deps), `showAddItemToRoutineModal`, `attachRoutineManagementListeners`, `populateHabitSelectDropdown`, and `saveNewHabit`/`saveNewTask`/`saveEditedHabit`/`saveEditedTask` (script.js keeps these four as `window.save*` thin wrappers, not plain functions, because the form HTML's inline `onclick="saveNewHabit(...)"` etc. needs the window-level name — same reasoning as `window.closeModal`/`window.deleteRoutine`). `definedRoutines`/`definedHabits` cross in as GETTERS (reassigned elsewhere in script.js), extending the agendaList.js part 2 (session 7) precedent. `definedTasks` has NO local declaration in script.js at all — always the bare `window.definedTasks` global — so the module reads it directly, no dep needed. `definedRoutinesListUL`/`activeRoutineCountDisplay`/`managementWindows` are stable const DOM refs. `activeItems` (needed by `saveEditedHabit`'s live-instance sync) is a plain reference, matching agendaListDeps()'s established "stable binding" precedent. `Modal.closeModal()` is called as a bare stable global throughout part 2 (Modal is fully extracted, guaranteed loaded first), matching popups.js/forms.js convention. Session 9 also flipped part 1's six form-half plain-function deps (showEditHabitForm, populateHabitSelectDropdown, showCreateHabitForm, showEditTaskForm, showCreateTaskForm, attachRoutineManagementListeners) to module-internal calls now that they live in the same file; `showRoutineManagement` forwards its whole deps object into `attachRoutineManagementListeners` rather than picking out a subset, since it doesn't need to distinguish which keys belong downstream. **Discovery, not fixed (session 8):** `renderDefinedRoutines` targets `#definedRoutinesList`, an element that does not exist anywhere in index.html — it's a leftover from the pre-modal routine UI (predates the modal-based `routinesWindowList` this game actually uses) and the function has always silently no-op'd via its `if (!definedRoutinesListUL) return` guard. Preserved as-is (behavior-identical extraction); candidate for deletion in a future cleanup pass. See DECISIONS.md. **Session 15 (2026-07-18):** added its own copy of the scheduleFieldsHtml/wireScheduleFieldsToggle/readScheduleFromFields widget (duplicated from forms.js, not shared — this file's established per-cluster convention) to all four recurring-definition forms (showCreateHabitForm/showEditHabitForm/showCreateTaskForm/showEditTaskForm) and their four save handlers; `editTaskInRoutine`/`createNewHabitInRoutine` in routines.js gained/extended schedule support to match. See DECISIONS.md.)
```
`script.js` ends as boot/wiring only. **Line-count goal revised 2026-07-18 (session 12):** the original `<300 lines` target predates the deps/thin-wrapper pattern every extraction used. script.js sits at ~1,140 lines, but that is almost entirely wrapper scaffolding (one 2-4-line wrapper per extracted function, plus the deps builders) kept deliberately so call sites never changed. `<300` becomes reachable only via the deferred state-ownership migration (state.js owning the variables, wrappers retired in favor of direct module calls) — a future dedicated session, see DECISIONS.md 2026-07-18.

**`style.css` split by component (2026-07-18, session 18 — Milestone 2's last item, now DONE):** 12 files under `css/`, one per `js/ui/*.js` cluster where a natural mapping existed (`agendaList.css`↔`agendaList.js`, `fabMenu.css`↔`fabMenu.js`, `managementWindows.css`↔`managementWindows.js`, `forms.css`↔`forms.js`, `routineViews.css`↔`routineViews.js`, `modal.css`↔`modal.js`, `popups.css`↔`popups.js`), plus `base.css`/`gameCanvas.css`/`enemySprites.css`/`enemyStatus.css` for pre-UI-extraction rules and `responsive.css` for the `@media` overrides. Pure mechanical split — no rule content changed. Two real hazards found and fixed during the split (see DECISIONS.md 2026-07-18 session 18 for the full account — worth reading before ever re-touching CSS file boundaries):
1. **Relative `url()` paths break on move.** `background-image: url('Assets/Zombies/...')` and `url('icons/...')` resolve relative to the CSS file's own location, not the HTML page. Moving those rules from root-level `style.css` into `css/` broke every sprite/icon until rewritten to `url('../Assets/...')` / `url('../icons/...')`.
2. **`@media` blocks are order-sensitive like any other rule.** `responsive.css`'s overrides only win because it's linked LAST in `index.html`, after every file whose base (non-media) rules it overrides. Linking it earlier would silently defeat mobile/desktop responsiveness at every equal-specificity conflict, with no error or warning — it would just look wrong. Never reorder `css/*.css` `<link>` tags in `index.html` without re-checking which selectors each file's rules target.

## Conventions
- Balance numbers only in `js/config.js`. UI never mutates state directly. Max ~300 lines/file.
- Real timestamps in logic; accelerated demo time only behind a config flag.
- No new dependencies without a DECISIONS.md entry.
- Old prototype `Deadline-MPE/` is frozen reference — never modify.

## Testing
- Jest for logic; keep tests running through every extraction. Add tests for each extracted module (movement math, damage ticks, pricing curve, streak resets, persistence round-trip).
- `playtester` agent runs `npm test` + `node --check` on changed files before handoff.
